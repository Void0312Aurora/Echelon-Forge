# Lightweight constraints for CI smoke/lint/coverage dependency resolution.
#
# This is not a full lockfile. Keep this file limited to packages installed by
# consuming smoke/lint/coverage workflows and the Python build backend packages
# that may be needed when an explicit package build/install smoke is exercised.
#
# Typical use:
#   python -m pip install --upgrade pip -c requirements/constraints-smoke.txt
#   python -m pip install -c requirements/constraints-smoke.txt pytest numpy ruff gymnasium jsonschema

# Current CI smoke/lint/coverage direct installs.
pytest>=7.4,<9
coverage>=7.4,<8
gcovr>=8,<9
ruff>=0.6.9,<0.7
numpy>=1.26,<3
gymnasium>=0.29,<2
jsonschema>=4.18,<5

# Build frontend/backend packages relevant to explicit package build smoke.
pip>=23.3,<26
scikit-build-core>=0.10,<1
nanobind>=1.9,<3
