# Lightweight constraints for CI/smoke dependency resolution.
#
# This is not a full lockfile. Keep this file limited to packages installed by
# the smoke workflow and the Python build backend packages that may be needed
# when an explicit package build/install smoke is exercised.
#
# Typical use:
#   python -m pip install --upgrade pip -c requirements/constraints-smoke.txt
#   python -m pip install -c requirements/constraints-smoke.txt pytest numpy ruff

# Current CI smoke/lint direct installs.
pytest>=7.4,<9
ruff>=0.6.9,<0.7
numpy>=1.24,<2; python_version < "3.9"
numpy>=1.26,<3; python_version >= "3.9"

# Build frontend/backend packages relevant to explicit package build smoke.
pip>=23.3,<26
scikit-build-core>=0.10,<1
nanobind>=1.9,<3
