"""GENERATED by tools/maintenance/dto_schema — edit the schema, not this file."""
# Freshness is enforced by tools/maintenance/dto_schema/generate.py --check.

from __future__ import annotations


FIELD_NAMES: tuple[str, ...] = ('header', 'fuze_type', 'armed', 'triggered', 'failure_reason', 'delay_s', 'reliability', 'sample', 'expected_detonation_probability', 'sampled_outcome', 'trigger_radius_m', 'contact_surface_distance_m', 'contact_penetration_depth_m', 'contact_surface_tolerance_m', 'contact_inside_hitbox', 'sensor_opportunity_source', 'sensor_opportunity_score', 'terminal_track_valid', 'target_detected', 'target_detection_source', 'target_detection_confidence', 'target_detection_threshold', 'detonation_point_source', 'mechanism_coverage_score', 'direct_hitbox_intersection')

WRITABLE_FIELD_NAMES: tuple[str, ...] = ('header', 'fuze_type', 'armed', 'triggered', 'failure_reason', 'delay_s', 'reliability', 'sample', 'expected_detonation_probability', 'sampled_outcome', 'trigger_radius_m', 'contact_surface_distance_m', 'contact_penetration_depth_m', 'contact_surface_tolerance_m', 'contact_inside_hitbox', 'sensor_opportunity_source', 'sensor_opportunity_score', 'terminal_track_valid', 'target_detected', 'target_detection_source', 'target_detection_confidence', 'target_detection_threshold', 'detonation_point_source', 'mechanism_coverage_score', 'direct_hitbox_intersection')

READONLY_FIELDS: frozenset[str] = frozenset()

FIELD_DEFAULTS: dict[str, tuple[str, str]] = {
    'header': ('object', "'{}'"),
    'fuze_type': ('str', '\'"unknown"\''),
    'armed': ('bool', 'False'),
    'triggered': ('bool', 'False'),
    'failure_reason': ('str', "'{}'"),
    'delay_s': ('float', '0.0'),
    'reliability': ('float', '1.0'),
    'sample': ('float', '1.0'),
    'expected_detonation_probability': ('float', '0.0'),
    'sampled_outcome': ('bool', 'True'),
    'trigger_radius_m': ('float', '0.0'),
    'contact_surface_distance_m': ('float', '0.0'),
    'contact_penetration_depth_m': ('float', '0.0'),
    'contact_surface_tolerance_m': ('float', '0.0'),
    'contact_inside_hitbox': ('bool', 'False'),
    'sensor_opportunity_source': ('str', '\'"none"\''),
    'sensor_opportunity_score': ('float', '0.0'),
    'terminal_track_valid': ('bool', 'False'),
    'target_detected': ('bool', 'False'),
    'target_detection_source': ('str', '\'"none"\''),
    'target_detection_confidence': ('float', '0.0'),
    'target_detection_threshold': ('float', '0.0'),
    'detonation_point_source': ('str', '\'"unknown"\''),
    'mechanism_coverage_score': ('float', '0.0'),
    'direct_hitbox_intersection': ('bool', 'False'),
}

_SENTINEL = object()


def assign_from_object(
    dto: object,
    source: object,
    fields: tuple[str, ...] | None = None,
) -> int:
    """Copy same-name writable attributes from source onto dto.

    Read-only fields are always skipped, even when explicitly listed in
    fields, so this can never attempt to set a read-only binding property.
    Only fields present on source are assigned. Returns the assigned count.
    """
    target_fields = WRITABLE_FIELD_NAMES if fields is None else fields
    count = 0
    for name in target_fields:
        if name in READONLY_FIELDS:
            continue
        val = getattr(source, name, _SENTINEL)
        if val is not _SENTINEL:
            setattr(dto, name, val)
            count += 1
    return count
