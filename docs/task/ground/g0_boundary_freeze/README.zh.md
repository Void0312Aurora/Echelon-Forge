# G0 边界冻结

状态：`2026-06-02` 归档指针。完整证据包已移入
[archive/g0_boundary_freeze](../archive/g0_boundary_freeze/README.zh.md)。

G0 已接受 ground boundary freeze：`ground` 是维护中的 specialization 名称，
`army` 与 `land` 是可接受别名，`platoon` 是第一批 tight-loop 单元，
`move / occupy / support` 是第一任务族默认值。

本路径仅保留轻量摘要。新工作应从 [../README.zh.md](../README.zh.md) 和新的
follow-on package 继续。
