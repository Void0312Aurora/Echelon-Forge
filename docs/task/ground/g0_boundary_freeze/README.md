# G0 Boundary Freeze

Status: `2026-06-02` archived pointer. The full evidence package was moved to
[archive/g0_boundary_freeze](../archive/g0_boundary_freeze/README.md).

G0 accepted the ground boundary freeze: `ground` is the maintained
specialization name, `army` and `land` are accepted aliases, `platoon` is the
first tight-loop unit, and `move / occupy / support` is the first task-family
default.

This path is a lightweight summary only. New work should continue from
[../README.md](../README.md) and a fresh follow-on package.
