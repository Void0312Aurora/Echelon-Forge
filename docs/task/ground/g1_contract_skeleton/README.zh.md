# G1 合同骨架

状态：`2026-06-02` 归档指针。完整证据包已移入
[archive/g1_contract_skeleton](../archive/g1_contract_skeleton/README.zh.md)。

G1 已接受窄范围 Python-profile-only contract slice：`army`、`ground`、`land`
与 `ServiceProfile.Army` 均规范化为 `ground`。C++ DTO 壳、binding、runtime
行为和 scenario-loader 行为仍保持 held。

本路径仅保留轻量摘要。新工作应从 [../README.zh.md](../README.zh.md) 和新的
follow-on package 继续。
