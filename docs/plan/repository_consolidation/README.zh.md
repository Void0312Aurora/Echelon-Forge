# 仓库精简与整合路线图

语言：
- 英文规范页：[README.md](README.md)
- 中文配套页：`README.zh.md`

Document kind: `plan`
Lifecycle: `maintained`
Canonical: `docs/plan/repository_consolidation/README.md`
Owner: `repository consolidation workline`
Last verified: `2026-07-18`

状态：在分支 `codex/redundancy-consolidation` 上执行中的仓库精简路线图。

## 目标

减少全仓冗余代码、重复权威、兼容残留、过大的导航表面和文档漂移。证据充分时允许
调整架构，但必须保留维护中的行为、明确的兼容承诺、测试、provenance 和有界能力
claim。

所有精简迭代都保留在同一分支。每轮迭代产生一个可独立审阅的提交；同一表面只有在
上一轮验收后才能进入下一轮修改。

## 迭代协议

每轮严格按以下顺序执行：

1. **分析**
   - 检查当前工作区和调用者；
   - 找出重复 owner 或过时路径；
   - 明确必须保留的行为和证据；
   - 冻结 write set 和非目标。
2. **实施**
   - 做出能够收敛到单一维护 owner 的最小完整架构变更；
   - 删除兼容表面或历史前先迁移调用者；
   - 不混入无关清理。
3. **验证**
   - 为每个受影响行为运行 focused test；
   - 按风险运行结构、链接、lint、build 或 smoke gate；
   - 审阅前记录命令和结果。
4. **独立审阅**
   - 派发一个没有编写该变更的独立 subagent；
   - 对冻结 diff 检查行为损失、被删除功能、调用者存活、兼容、文档权威、测试充分性，
     以及是否重复已有 owner 或堆砌不必要的平行机制；
   - reviewer 只读，返回 blocking finding、non-blocking finding 和 verdict。
5. **修复与复审**
   - 修复 blocking finding 并重跑受影响验证；
   - 实质修复后必须进行最终独立复核。
6. **提交**
   - 最终 diff 没有未解决 blocking finding 后才能提交；
   - 每轮一个提交，并在下方迭代登记中记录证据；
   - 不通过 amend 旧迭代来隐藏后续修复。

最终审阅后发生的实现改动，未经再次审阅不得进入提交。

## 必需验收证据

每轮验收交接必须包含：

- 精确 write set 和非目标；
- 被删除/移动表面的 caller 或 consumer 清单；
- focused validation 命令和结果；
- 生产行为变更时的更广 maintained smoke/build 结果；
- 文件/行数变化和仍存在的重复 owner；
- 独立 reviewer 身份、被审 revision/diff、findings 和 verdict；
- 对保留行为和残余的明确说明；
- 提交建立后的 commit hash。

纯文档迭代还必须证明受影响入口的本地链接有效、强制双语 companion 一致，并通过
`git diff --check`。

## 候选优先级

下表是路由顺序，不是预先授权。每个候选开始迭代时都必须重新审计源码和调用者。

| 优先级 | 候选 | 预期结果 | 主要风险 |
| --- | --- | --- | --- |
| P0 | 重复表示暴露出的正确性缺陷 | 在机械删除前修复错误行为。 | 保持兼容的补丁仍可能改变默认语义。 |
| P1 | 不可达实现和完全相同的 helper | 删除 dead body，建立唯一 runtime/test helper owner。 | 隐藏构造或 import 方式可能绕过正常调用者。 |
| P2 | 文档生命周期与精简治理 | 建立可执行的分类、审阅、归档和 evidence 规则。 | 只写政策而不索引、不执行，会增加新的闲置权威。 |
| P3 | 维护导航与兼容 shim | 修复入口链接，迁移内部 shim 调用者，保留有界外部兼容 facade。 | 调用者迁移前移动/删除 shim 会破坏测试不可见用户。 |
| P4 | Python taxonomy、geometry、objective、C2/ROE 和 bootstrap helper 重复 | 建立单一语义 owner，并用 scalar/tensor 或 runtime/compiler parity test 约束。 | 表面相似 helper 可能有有意语义差异。 |
| P5 | World-batch、step-evaluation、effects-event 和配置 schema 重复 | 用共享 payload 或显式 adapter 取代字段逐项同步。 | public binding、ABI、序列化和报告 schema 可能改变。 |
| P6 | Python 包循环与 C++ target/layer 边界 | 强制依赖方向和更小的 build/runtime owner。 | 广泛 import、链接、初始化顺序和性能回归。 |
| P7 | 文档压缩、archive 规范化和 evidence manifest | 缩小维护导航；完成 provenance 映射后再折叠重复 archive 层。 | 路径迁移会破坏工具、测试、历史引用和权利证据。 |
| P8 | 最终全仓残余审计 | 把每个剩余重复项分类为有意、held 或收益不足。 | 扫描可能漏掉行为重复，或高估文本相似性。 |

## 迭代登记

| 迭代 | 状态 | Commit / 分支证据 | 范围与结果 | 验证 / 审阅证据 |
| --- | --- | --- | --- | --- |
| I1 Runtime 与测试基础设施整合 | `accepted` | `aaec45882173d57c679e3e7233a81980ee9d8fdc`，分支 `codex/redundancy-consolidation` | 修复 missile tuning 稀疏覆盖语义，删除不可达 `UniversalEnv` body，统一 `ef_py` runtime bootstrap 和 suite-manifest 解析，合并重复测试 helper。净变化为新增 1,413 行、删除 2,390 行。 | Focused regression 与 maintained smoke suite 通过；最终迭代收口记录为 `361 passed`、`41 subtests passed`。独立审阅在提交前未留下 unresolved blocking finding。 |
| I2 文档生命周期与精简治理 | `accepted` | `c844bd900856682f18d6dc72fcb442b95e75c18a`，分支 `codex/redundancy-consolidation` | 建立生命周期与精简权威、唯一维护文档范围规则、严格链接审计、选择性双语登记刷新，并在不使用 baseline allowlist 的前提下完成链接安全修复。严格登记现覆盖 76 对文档。最终 diff 为 57 个文件、新增 1,960 行、删除 427 行。 | 默认审计检查 155 份文档和 2,592 个内部链接，零问题；治理聚焦测试 `15 passed`；maintained smoke 为 `371 passed`、`41 subtests passed`；Ruff 与 `git diff --check` 通过。登记结果为 70 对同步、6 对保留的既有分歧、1 个既有缺失英文 companion。`iteration2_independent_review` 发现两个 archive 权威 blocker，均已修复；最终 `bilingual_registry_audit` 复审没有遗留 blocker。 |
| I3 文档去重与双语残余闭环 | `accepted` | `d0dbf0d7ee68643baa30d41e66e3955407d3a3ba`，分支 `codex/redundancy-consolidation` | 否决了为根 README 增建登记特例的方案；把重复的 realism-authority 标准压成复用既有 owner 的兼容路由对；删除 air-combat 上级索引复制的归档实现细节；补回缺失的 review 路由；刷新四个已有证据的旧 baseline。最终 diff 为 17 个文件、新增 184 行、删除 192 行（净删除 8 行）。 | 登记结果为 77/77 同步，无缺失 peer 或漂移；链接审计检查 156 份文档、2,590 个链接，零问题；治理聚焦测试 `25 passed`；maintained smoke 为 `372 passed`、`41 subtests passed`；Ruff 与 `git diff --check` 通过。独立审阅发现 authority 与 registry gate blocker，均已修复；最终复审以零 blocker 批准候选。 |
| I4 Scenario compiler 兼容 owner 整合 | `accepted` | `afe03257e26f9355013293fb0bace77cfeb4091b`，分支 `codex/redundancy-consolidation` | 把只绑定 64 个名称的不完整手工 facade 改为由 canonical `__all__` 驱动的 86 项完整转发；15 个维护中调用方迁到 `python.scenario.compiler`；测试与 archive 保留兼容入口。结构门禁阻止维护代码重新依赖 facade。最终 diff 为 22 个文件、新增 77 行、删除 86 行（净删除 9 行）。 | 直接 star-import 校验 86/86 导出且对象身份一致；scenario、world-batch、多 agent 与架构聚焦测试 `81 passed`；maintained smoke 为 `374 passed`、`41 subtests passed`；Ruff 与 `git diff --check` 通过。独立审阅发现一个平行扫描 blocker；复用既有全仓扫描器并采用 AST import 检查后，最终复审以零 blocker 批准候选。 |
| I5 MissionCommand shared-core owner 整合 | `accepted` | `9c999b81`，分支 `codex/redundancy-consolidation` | 让 `MissionCommandCore` 成为可直接判等的 shared owner；重复 directive struct 改为 alias；projection 与 maintained-contract 回填折叠为值复制；episode-state 等价判断直接消费 owner equality。该变更修复手写比较漏掉的四个已序列化字段，同时保留 Python 类型名称；完整 umbrella 以及 umbrella/core 两个方向的混合 equality 均被显式删除，避免静默忽略领域 slice。最终 diff 为 9 个文件、新增 65 行、删除 74 行（净删除 9 行）。 | 已重建 `ef_core`、`ef_py`、`ef_test`，`ef_test_all` 通过；架构、binding、episode、world-batch 与领域聚焦回归 `119 passed`、`1 skipped`、`6 subtests passed`；maintained smoke 为 `375 passed`、`45 subtests passed`；Ruff 与 `git diff --check` 通过。独立审阅先发现继承产生的不完整 umbrella equality 和测试缺少正向 baseline，复审又发现两个 umbrella/core 混合比较方向；全部 finding 均已用编译与运行时门禁修复，最终复审以零 blocker 批准候选。 |
| I6 Execution runtime 行为 owner 整合 | `accepted` | `codex/redundancy-consolidation` 上的 `143ee4e9` | 删除重复的 `execution_frame_runtime.cpp`，把保留的 Frame 兼容符号迁入 Episode 所属实现，并让两个 API 共享唯一内部公共产品路径与批处理调度器。公开 Frame/Episode DTO、Python 名称、名义类型分离、64 项并行阈值、顺序与异常传播均保持不变。最终 diff 为 13 个文件、新增 147 行、删除 151 行（净删除 4 行）。 | 已重建 `ef_core`、`ef_py`、`ef_test`，`ef_test_all` 通过；scalar/batch/fallback/architecture 聚焦回归 `46 passed`、`226 subtests passed`；维护中的 multi-agent/world-batch 调用者 `71 passed`、`1 skipped`、`5 subtests passed`；maintained smoke 为 `375 passed`、`45 subtests passed`。Registry 为 77/77 synced；链接审计检查 156 份文档、2,592 个链接，零问题；Ruff 与 `git diff --check` 通过。独立复审以零 blocker 批准候选。 |
| I7 模式选项面 owner 整合 | `accepted` | `codex/redundancy-consolidation` 上的 `587df736` | 把 `python.mission_obs_taxonomy`（任务观测模式）与 `python.env_config`（action、execution-step-runtime、step-info、flight-shaping 模式）确立为模式面的唯一有序 owner。训练/评估 CLI 的选项清单与校验集合改为从 owner 元组派生，移除 `python/training/cli.py`、`python/env_config.py`、`tools/eval/eval_utils.py`、`tools/eval/sb3_eval_base.py` 中五处手写字面量副本。导出名、选项内容与顺序保持不变；新增 parity 测试把每个派生面钉在其 owner 上。最终 diff 为 9 个文件、新增 111 行、删除 27 行。 | env-config/taxonomy、training-bootstrap 与评估 CLI 聚焦回归 `48 passed`、`2 skipped`、`20 subtests passed`；maintained smoke 为 `380 passed`、`45 subtests passed`（基线 375 加五个新 parity 守卫）。Ruff、`git diff --check`、双语 registry 审计（77 对）与维护链接审计通过。独立评审以零 blocker 批准候选。 |
| I8 标量辅助与模式字面量 owner 整合 | `accepted` | `codex/redundancy-consolidation` 上的 `aa5b537c` | 确立零依赖 owner：`python.angles`（有符号/罗盘 wrap、保号 heading error、方位角、平面距离）与 `python.coercion`（`coerce_nonnegative_int`），约十九处维护中的角度辅助调用点与全部五处 coercion 副本经薄别名迁移到 owner，所有公开名保持可导入。刻意变体留在调用侧并有测试钉住：1e-9 归零 wrap、退化方位 fallback、以及与 owner 形式实测有约 1e-13 度位级差异的纯 `%360` 方位公式。运行时模式字面量残留（ScenarioLoader backend 校验、world-batch 与 cooperative vec-env 校验、scenario-loader 两个 normalize、诊断 benchmark CLI）现从 `python.env_config` 中语义匹配的元组派生；batch observation 与 visual backend 即使当前取值相同，也与 flight shaping 保持不同 owner。此前报告的 `universal_env.py` 残留在当前基线上不存在。`ModeChoiceSurfaceParityTests` 补充 `ACTION_MODES` 内容 pin 与不分引号风格的负向守卫。最终 diff 为 33 个文件、新增 560 行、删除 119 行（代码与测试面为 27 个修改文件 +117/−115 加三个新文件；其余为本登记更新与 registry 刷新）。 | 新增 25 用例 parity 套件把 owner 与被删公式的内嵌副本逐位对拍（覆盖 ±180/360/负角/非有限值）并纳入 CI smoke 清单。naval/world-batch/multi-agent/leader/场景编译器/导航/评估聚焦回归 `325 passed`、`3 skipped`、`51 subtests passed`。maintained smoke 为 `406 passed`、`45 subtests passed`（基线 380 加 26 个新守卫）。Ruff 与 `git diff --check` 通过。独立评审用独立的 1.4 万点位级网格复现数值 parity 零失败，以零 blocker 批准候选。 |
| I9 训练/评估入口整合 | `accepted` | `codex/redundancy-consolidation` 上的 `c20d2366` | 把根目录 `train.py` 的实现主体（1,008 行降至 331 行）下沉到 `python/training/`：延迟 SB3/torch 依赖加载（`deps.py`）、安全动作偏置初始化（`action_bias.py`）、vec-env 选型与构造（`vec_env_factory.py`）。`train.py` 保留薄 `main()`、兼容 re-export（含修复的 `apply_global_seed`——`tools/diagnostics/trace_training_nonfinite_source.py` 依赖此导入）与 WP24 UniversalEnv 退役门禁字符串。Checkpoint 加载现由无 bootstrap 副作用的 `python.rl.policy_checkpoint.load_sb3_policy` 负责；`tools/eval/sb3_eval_base` 在其诊断工具本地 fail-closed bootstrap 之后保留兼容 re-export。如实记录一处有意的行为增强：此前在 `evaluate.py` 加载失败的历史 HMoE/Squashed checkpoint 现经共享的历史 policy-class 探测成功加载。两个端到端入口 smoke 测试钉住 evaluate CLI 与 `train.py --test_only` 路径，后续守卫另行验证导入共享 loader 不会检查本地构建配置。最终 diff 为 14 个文件、新增 1251 行、删除 762 行（代码与测试面为 6 个修改文件 +121/−758 加五个新文件；其余为本登记更新与 registry 刷新）。 | 独立评审以 AST 归一化逐函数对比核实行为保持（动作偏置数值、全部 vec-env kwargs、`main()` 编排与日志文案），实测 `import train` 不加载 torch，并将四份 `--help` 输出与基线逐字节对比。training/policy/eval/architecture 聚焦回归 `251 passed`、`2 skipped`（一个基线即红的 GBK 控制台用例已在未打补丁基线复现）。落地树 maintained smoke 为 `406 passed`、`45 subtests passed`。Ruff 与 `git diff --check` 通过。独立评审以零 blocker 批准候选，并裁决保留两个新入口 smoke 测试。 |
| I10 飞行塑形字段税消除 | `accepted` | `codex/redundancy-consolidation` 上的 `c1a8b2f4` | 把 `FlightShapingRuntimeInputs` 与 `StepEvaluationBatchConfig` 共享的 89 个 config-static 塑形字段提为单一 X-macro 清单（`src/core/mission/runtime/detail/flight_shaping_shared_fields.inc`）；两个结构体字段块、batch-prepare 逐字段拷贝与 FlightShaping `def_rw` 绑定改由该唯一 owner 宏展开。字段类型、名称、默认值、顺序与含 118 个属性的 Python input 表面保持不变；两个任务动态 `target_*` 字段保持手写以保成员顺序。最终 diff 为 8 个文件、新增 136 行、删除 373 行（代码面 5 个文件 +128/−367、净删除 239 行；其余为本登记更新与 registry 刷新）。 | 在隔离 worktree 重建 `ef_core`、`ef_py`、`ef_test`，`ef_test_all` 通过。字段审计 118/118 与 112/112 逐项一致（类型/名称/默认值/顺序）；新旧构建 parity 探针输出 546 行逐字节相同。execution/facade/world-batch/architecture 聚焦回归 `282 passed`、`1 skipped`（window-loop 字符串风格断言与本机 winsock 链接两类既有失败已在未打补丁基线上原样复现）。maintained smoke 为 `380 passed`、`45 subtests passed`。`git diff --check` 通过。独立评审发现一项登记口径 blocker；登记修复后，最终复核以零 blocker 批准候选。 |
| I11 EffectsEvent 字段面 owner 整合 | `accepted` | `codex/redundancy-consolidation` 上的 `cc3bdf1a` | 把 135 字段的 `EffectsEvent` 清单提为 X-macro 列表 `src/runtime/contracts/detail/effects_event_fields.inc`（48 个事件独有 + 87 个与 `EffectsResult` 重叠条目），契约结构体、`apply_effects_result_fields` 投影与 Python `def_rw` 绑定改由该唯一 owner 展开。重叠计数修正了此前 88 字段的估计：`destroy_missile` 是 result 侧控制标志，从未被拷贝。`weapon_launch_adapter.h`（`EffectsEventSnapshot`、`make_effects_event`）确认编译不可达（仅文本形状测试引用），登记为待删候选、本轮未动。契约形状测试改为文本展开 include，使字段从清单移除时断言仍会失败。最终 diff 为 8 个文件、新增 197 行、删除 433 行（代码与测试面为 4 个修改文件 +39/−429 加 150 行清单；其余为本登记更新与 registry 刷新）。 | 独立评审对照改前源码逐项核实全部 135 字段（类型/名称/默认值/顺序）与 87/48 划分，复跑 204 项 engagement/bindings/air-combat/architecture 定向测试与 `ef_test`（113 用例、18,753 断言）全绿，并复现跨构建 parity 探针（dir() 序列与逐字段默认值与主线全等）；结论 approve、零 blocker。在合并落地树（I8+I9+I10+I11）上增量重建通过 `ef_test_all`，engagement/execution 回归 `87 passed`、`226 subtests passed`，maintained smoke 为 `406 passed`、`45 subtests passed`。`git diff --check` 通过。 |
| I12 运行时引导 import 面迁移 | `accepted` | `codex/redundancy-consolidation` 上的 `a1c33d43` | 把 167 个维护面调用者（tests 116、tools 42、python 8、examples 1；共 174 处 direct import，为该基线上唯一存在的 import 形式）从兼容壳 `python.testing.runtime` 迁移到 canonical owner `python.runtime_bootstrap`。兼容壳保留为 archive 专用 re-export facade 并更新 docstring；`tools/archive/` 的十一个调用者按 I4 先例留在兼容路径。新增 AST 治理门禁（`tests/architecture/governance/test_runtime_bootstrap_owner.py`，已入 smoke 清单）禁止维护面再 import 兼容壳。经三方合并落到 I13 头上（相邻 import 块编辑）；字节码缓存噪声已从移植中剔除。最终 diff 为 173 个文件、新增 253 行、删除 183 行（代码与测试面为 169 个修改文件 +181/−179 加门禁测试；其余为本登记更新与 registry 刷新）。 | `compileall` 通过；两个新治理门禁在落地树上通过。落地树 maintained smoke 为 `410 passed`、`45 subtests passed`（基线 409 加新门禁）。流 worktree 全量回归 `2095 passed`、`3 skipped`，十三项已知基线红名单全部复现；其余失败均非 import 路由回归——`ef_py` 导入错误是与并发 worktree 重建的瞬时竞争，artifact 路径与 worktree 路径参数化失败属 worktree 环境效应，`weapon_guidance_realism` 套件在迁移前基线树（匹配构建）上以完全相同的 45 例失败复现，判定为维护 smoke 门禁之外的本机基线红。Ruff 与 `git diff --check` 通过。按所有者指示未派发独立评审代理；由编排方核验移植并复跑落地门禁。 |
| I13 world-batch vec-env 兼容壳调用面收口 | `accepted` | `codex/redundancy-consolidation` 上的 `86bd0fcd` | 把维护中的调用面（生产 `python/` 3 个模块、`tools/diagnostics` 5 个脚本、8 个普通 import 测试文件、3 个 monkeypatch 型 `tests/world_batch` 文件）从 `python.rl.runtime.world_batch_vec_env` 迁移到 canonical 的 `python.rl.runtime.world_batch.vec_env` 与 `_observation_mixin` owner，每个 monkeypatch 点按符号真实 owner 重新定向。`examples/viz/runtime/viz_session.py`（被架构测试钉住）、`tests/gpu` 中的 subprocess 字符串与归档面刻意留在兼容壳上。维护调用者归零后，兼容壳从带七项可变转发表的 `types.ModuleType` 子类收缩为纯 re-export 薄壳；新增三用例 AST 门禁（`tests/architecture/runtime_facade/test_world_batch_owner_imports.py`，已入 smoke 清单）禁止维护面再 import 兼容壳并钉住其 `__all__` 与实际 re-export 一致。配置键字符串 `runtime.world_batch_vec_env=true` 未触碰。最终 diff 为 25 个文件、新增 165 行、删除 63 行（代码与测试面为 21 个修改文件 +50/−59 加 89 行门禁测试；其余为本登记更新与 registry 刷新）。 | world-batch/multi-agent/facade/training/policy 聚焦回归 `291 passed`、`1 skipped`、`104 subtests passed`；全部 world-batch monkeypatch 测试（含三个重定向文件）通过。其余六个失败为该 worktree 本机既有的 flecs 链接/构建产物缺口，已按基线问题复现归因。落地树 maintained smoke 为 `409 passed`、`45 subtests passed`（基线 406 加三个新门禁测试）。Ruff 与 `git diff --check` 通过。按所有者指示未派发独立评审代理；由编排方核验逐字节移植并复跑落地门禁。 |
| I14 编译不可达死接口面删除 | `accepted` | `codex/redundancy-consolidation` 上的 `c04da9a8` | 删除编译不可达的 `src/core/engine/weapon_launch_adapter.h`（503 行，`EffectsEventSnapshot` 与 `make_effects_event`）及其 5 项、332 行的文本/迁移形状测试，并移除 CI smoke 清单中的对应条目。全树引用与符号审计确认无生产 include、CMake、工具、Python 或契约 spec 消费者，仅存文本级测试读取。另两个孤儿候选以具体证据保持 `held`：`information_transform_contracts.h` 被九个架构片段探针编译且被活跃 review 文档点名；`same_window_edge_validation.h` 被六个片段探针编译。最终 diff 为 6 个文件、新增 6 行、删除 840 行（代码与测试面为 3 个文件 −836；其余为本登记更新与 registry 刷新）。 | 在流 worktree 重建 `ef_core`、`ef_py`、`ef_test`，`ef_test_all` 通过。落地树 maintained smoke 为 `405 passed`、`45 subtests passed`（基线 410 精确减去被删的五项形状测试）。流侧大范围回归（`663 passed`、`12 failed`、`5 errors`）复现的全部是未触碰表面的本机既有基线项（路径分隔符匹配、GBK/PDF 探针、fragility 数值基线、陈旧 allowlist 与 facade 文本形状断言），与 I12/I13 已记录的本机基线清单一致；均与被删表面无关。`git diff --check` 通过。按所有者指示未派发独立评审代理；由编排方核验移植、suite 清单合并与落地门禁。 |
| I15 测试归档退役与测试墙裁决 | `accepted` | `codex/redundancy-consolidation` 上的 `3342c435` | 逐文件引用扫描确认零功能消费者后，退役整个 `tests/archive` 树（52 个文件：历史 raw-`UniversalEnv` 回归规范、scripted-bridge 规范与遗留 leader 契约）——套件清单、conftest 与 runner 无引用，三个提及该路径的测试仅将其用作排除模式或合成 tmp fixture；按域核对了维护面的覆盖替代。双语 `tests/` 与 `tests/contracts/unit/training/` README 改为记录退役并指向 git 历史，不再携带断链。两项裁决以证据记为 `held`：weapon-guidance 权威表模块含实质测试逻辑（内核调用、fixture、断言链）而非纯数据表；契约 spec 参数化不被维护 runner 支持，均未强行纳入本轮。最终 diff 为 59 个文件、新增 10 行、删除 4,210 行。 | 提及归档路径的三个门禁/审计测试在落地树上通过（`11 passed`）。maintained smoke 为 `405 passed`、`45 subtests passed`，与基线持平，证实归档对 smoke 无贡献。`git diff --check` 通过。按所有者指示未派发独立评审代理；由编排方复跑引用扫描与落地门禁。 |
| I16 tools 脚手架收敛 | `accepted` | `codex/redundancy-consolidation` 上的 `aa1ec513` | 把 diagnostics、geometry 与 maintenance 七个证据包（约 78 个脚本）中手写的 `REPO_ROOT`/`sys.path` 引导块收敛到 `python.runtime_bootstrap`（`ensure_repo_imports`/`repo_root`/`resolve_repo_path`），保留直接以文件路径运行所需的两行 path hint 与依赖 chdir 的脚本语义。同构标量/IO helper 提升到 `tools/diagnostics/common.py`（`finite_float`、`finite_float_or_none`、`mean_finite`、`native_stdout_to_stderr`、参数化 `write_json_output`），分块文件/文本哈希复用 `tools/maintenance/retained_artifacts/manifest_integrity` owner；全部诊断能力、CLI 面与输出格式保持不变。语义不等价的变体保留本地并注释（三种 `_mean` 空输入约定、非 resolve 的路径展示 helper、weapon-probe schema 的 `_finite_float`），未触碰任何 `write_retained_*` 输出路径。最终 diff 为 87 个文件、新增 991 行、删除 1,923 行（代码面 84 个文件 +985/−1919；其余为本登记更新与 registry 刷新）。 | 聚焦回归与改前失败集逐项一致（`tests/tools` 7 failed/94 passed；fire-timing 契约 19 passed；process-probe 41 passed；damage_model 4 failed/247 passed/5 errors 含已知 GBK 项）。maintained smoke 为 `405 passed`、`45 subtests passed`。三个区域八处 CLI `--help` 冒烟通过。Ruff 与 `git diff --check` 通过。按所有者指示未派发独立评审代理；由编排方核验移植（剔除流侧误触的跟踪态字节码噪声）并复跑落地门禁。 |
| I17 I8-I11 复审修复 | `accepted` | `codex/redundancy-consolidation` 上的 `1cfca742` | 修复 I8-I11 验收后复审发现的三项可执行问题。SB3 checkpoint 加载迁到无 bootstrap 副作用的 `python.rl.policy_checkpoint` owner，使根入口 `evaluate.py` 保留 installed-wheel 回退；`tools/eval/sb3_eval_base.py` 则继续保留诊断工具本地 fail-closed bootstrap 与兼容 re-export。Batch observation 和 visual backend 现在分别拥有语义独立的 owner，并同时驱动 runtime normalizer 与 world-batch benchmark CLI，不再借用 `FLIGHT_SHAPING_BACKENDS`。I9 的 `train.py` 基线从误记的 927 行修正为 1,008 行，I10 错误的 `118/15` 属性表述修正为 118 属性的 input 表面。最终 diff 为 11 个文件、新增 143 行、删除 60 行。 | env-config、evaluate、历史 checkpoint 与训练入口聚焦回归为 `27 passed`、`1 skipped`。八个变更 Python 文件通过内存语法编译与禁用缓存的 Ruff 检查。维护双语审计为 77/77 文档对同步，`git diff --check` 通过。 |
| I18 diagnostics episode/env 构造收敛 | `accepted` | `codex/redundancy-consolidation` 上的 `abed924c` | 将三个 diagnostics 采集器（`event_credit_head` offline/online、fire-timing `real_update`）共有的 reset/step/终止/缓冲调度/close 壳收敛到 `tools/diagnostics/common.py`，域缓冲留在调用方，online value bootstrap 以 episode-end 钩子表达。weapon-employment process probe 与 post-launch assessment benchmark 的标准单世界构造路径改由 `build_single_world_batch_execution_runtime` 承担，全部非默认参数与注入式 VecEnv 兼容缝保持不变。多世界/依赖 VecEnv 私有面的 benchmark、语义不同的 rollout 循环与 kernel 直构探针按设计保留本地。本轮以如实记录的净增行数换取采集壳的单一 owner：含登记与 registry 刷新共 9 个文件、新增 426 行、删除 200 行（代码面 6 个文件 +420/−196）；后续新增采集器复用壳而非复制循环。 | 改前改后聚焦回归逐项一致（fire-timing 19；process-probe 41；event-timing 7；world-batch 113 passed、1 skipped、20 subtests）。offline/real 两步真实采集报告改前改后逐字节相等。七个 CLI `--help` 冒烟、Ruff 与 `git diff --check` 通过。落地树 maintained smoke 为 `407 passed`、`45 subtests passed`（I17 基线向 smoke 内文件新增了两个 env-config 守卫）。按所有者指示未派发独立评审代理；由编排方核验逐字节移植并复跑落地门禁。 |
| I19 retained 写出单一 owner 收敛 | `accepted` | `codex/redundancy-consolidation` 上的 `33501818` | 在 `tools/maintenance/retained_artifacts/manifest_integrity.py` 新增 `write_and_hash_json()`，把七个证据包中 24 处 `write_retained_*` 的 23 处收敛为委托共享 owner 做 JSON 序列化与 SHA-256 哈希的薄壳，函数名、签名、返回值与字节级输出约定全部保持（含 Windows `\r\n` 写出行为与 `candidate_artifacts` 的 `_sha256_text` manifest 哈希路径）。唯一残余 `release_governance/source_release_signoff` 因其三遍 manifest 稳定化语义独立而保留本地实现。新增 260 行对拍套件（`tests/tools/test_retained_write_parity.py`）内嵌被删实现作参照，逐函数逐 payload 证明字节相等。最终 diff 为 28 个文件、新增 487 行、删除 164 行（代码与测试面为 24 个修改文件 +205/−160 加对拍套件；其余为本登记更新与 registry 刷新）。 | 对拍套件 `69 passed`、`4 skipped`（skip 为被删实现自身在 GBK locale 下即崩溃的 Unicode 用例；共享 owner 显式 UTF-8 属严格改进）。`tests/tools` 失败集不变（7 failed，含对拍新增后 163 passed）。`tests/architecture/damage_model` 的 retained/manifest/sha256 子集不变（2 failed、38 passed、1 error，均为既有本机项）。落地树 maintained smoke 为 `407 passed`、`45 subtests passed`。Ruff 与 `git diff --check` 通过。按所有者指示未派发独立评审代理；由编排方核验逐字节移植并复跑落地门禁。 |
| I21 契约 spec extends 继承与近重复对收敛 | `accepted` | `codex/redundancy-consolidation` 上的 `3c86574a` | 解除 I15 中「契约 spec 参数化缺乏 runner 支持」的 `held`：`python/testing/contracts/common.py` 现支持 `extends`——本文件目录优先、仓库根回退的路径解析，递归复用既有 `_deep_merge`，循环检测与四层链深限制；`extends` 键不进入有效 spec。naval threat-ROE 对收敛至 `tests/contracts/_base/naval_screen_threat_roe_base.json`，scripted-takeoff 对收敛至 `_base/scripted_takeoff_base.json`；四份有效 spec 经内嵌金测证明与收敛前基线逐键相等；base spec 位于 runner 收集 glob 之外（`collected_base_specs=0`）。wrappers 簇因叶级相似度仅 0.79–0.96 保持本地。最终 diff 为 11 个文件、新增 409 行、删除 104 行（代码与测试面为 5 个修改文件 +63/−100 加两份 base spec 与 extends 测试；其余为本登记更新与 registry 刷新）。 | 有效 spec 对拍 4/4 逐键相等。四个收敛契约与 10 项 CI contract suite 全部通过；runners/scenario 回归 `73 passed`。落地树 maintained smoke 为 `408 passed`、`45 subtests passed`。Ruff 与 `git diff --check` 通过。按所有者指示未派发独立评审代理；由编排方以暂存 blob 一致性核验移植，并复跑契约套件、extends 测试与落地 smoke。 |
| I20 探针 CLI 共享参数组 | `accepted` | `codex/redundancy-consolidation` 上的 `2ca09145` | 在 `tools/diagnostics/common.py` 落地 `add_probe_run_args`/`add_model_load_args`/`add_json_out_arg` 构造器：canonical dest 固定 underscore，underscore/hyphen 双注册（别名用 `help=SUPPRESS`，保证每个 `--help` 面逐字节不变）。迁移七个满配探针 CLI（weapon-employment 过程探针、event-credit offline/online、fire-timing real_update/chain_breakpoint/learnability_audit/window_position_sweep），删除 57 处手写 `add_argument`；部分匹配脚本列入后续批次。与 I18 同理，本轮以净增行数投资共享 owner，使后续探针不再复制脚手架。最终 diff 为 11 个文件、新增 314 行、删除 68 行（代码面 8 个文件 +308/−64；其余为本登记更新与 registry 刷新）。 | 七个迁移脚本改前改后 `--help` 输出逐字节相等；三处双别名解析 dest 一致。聚焦回归 `60 passed`（fire-timing 契约 19、process-probe 41）。落地树 maintained smoke 为 `408 passed`、`45 subtests passed`。Ruff 与 `git diff --check` 通过。按所有者指示未派发独立评审代理；由编排方以暂存 blob 一致性核验移植并复跑落地门禁。 |
| I22 DTO 单源生成器骨架 | `accepted` | `codex/redundancy-consolidation` 上的 `db97d478` | 新增纯标准库的声明式 DTO schema 包（`tools/maintenance/dto_schema/`）：带扩展位（预留 `python_name`/`readonly`/`hidden`/`json_key`）的 schema 模型、X-macro 解析器、`--check`/`--write` 生成器 CLI 与机器可读 manifest，以及描述新 DTO 家族三步接入流程的 README。以既有两个 X-macro 清单为试点（89 字段 flight-shaping、135 字段 EffectsEvent 含 48/87 分组）：再生成逐字节一致、`--write` 幂等、注入默认值漂移会被非零退出与 unified diff 捕获。新鲜度架构门禁已入 maintained smoke 清单。此为 DTO 单源化分期方案（episode、world-batch、engagement、command、GPU 家族）的地基。最终 diff 为 13 个文件、新增 958 行、删除 4 行（代码与测试面为 10 个新文件 +952；其余为本登记更新与 registry 刷新）。 | 生成器 `--check` 在落地树上报告两个试点 up-to-date，新鲜度门禁通过（`1 passed`）。maintained smoke 为 `408 passed`、`45 subtests passed`（基线 407 加新鲜度门禁）。流 worktree 的 execution/engagement 聚焦回归 `82 passed`、`226 subtests passed`。Ruff 与 `git diff --check` 通过。按所有者指示未派发独立评审代理；由编排方以暂存 blob 一致性核验移植，并复跑生成器检查、门禁与落地 smoke。 |
| I23 episode evaluation DTO 单源化 | `accepted` | `codex/redundancy-consolidation` 上的 `9d23b947` | 统一体系蓝图 W2 关键期迭代。十二个新 schema（233 字段）把 safety/termination、conditional-objective、mission-nav/observation/step-info 与 reward 余量家族纳入单源治理：四个 mission-runtime 头文件的字段块与 `bindings_episode.cpp` 对应段改由生成的 X-macro 清单展开，十四个生成 Python builder（加包 init）并入统一新鲜度命令（`generate.py --check`，29 个产物，含污染检测）。`safety.py` 的十六处机械配置赋值迁移到生成 builder；派生/条件赋值刻意保留手写。只读 products DTO 不生成赋值器，混合类赋值器跳过只读字段。登记残余：execution 组合家族的三个连续块（8 字段 outcome、8 字段输入前缀、9 字段产品前缀）留待后续。最终 diff 为 54 个文件、新增 2,393 行、删除 560 行（代码面 10 个修改文件 +315/−554 加 41 个生成/schema/测试文件；其余为本登记更新与 registry 刷新）。 | 独立评审（本关键期专门派发）逐项核实 233 个宏化字段与改前头文件精确一致（类型/名称/默认值/成员顺序）、687 属性绑定面经静态与运行时双重比对精确一致、safety 迁移映射正确；其两项 blocking（builder 新鲜度未入主门禁、只读 products 生成必崩赋值器）修复后，最终复核以零 blocker 批准。execution/mission 聚焦回归 `136 passed`、`234 subtests passed`；`ef_test_all` 通过；落地树 maintained smoke 为 `408 passed`、`45 subtests passed`。Ruff 与 `git diff --check` 通过。 |
| I24 tasking-contracts 中立层抽取 | `accepted` | `codex/redundancy-consolidation` 上的 `70e75454` | W2 关键期迭代（运行时基座 B-1 步）。把 `gym_envs` 对 `python.rl` 任务派发纠缠中与 profile 无关的切面抽到零依赖新包 `python/tasking_contracts/`（任务指令/阶段词表、bridge 视图与命令链同步 seam、`LeaderDecisionState`、计时归一、脚本化控制器）。全部迁移符号经 `assertIs` 套件证明旧路径 re-export 壳与新定义同一对象。十一个原纠缠 `gym_envs` 文件已零 `python.rl` 引用，`gym_envs.leader_env` 导入期不再拉入 `python.rl.runtime.leader_window_runtime`；十四个文件保留治理台账残余（必须触达 `python.rl` 内部 air/ground/naval 适配器的 profile 派发函数），由「精确允许清单」AST 门禁锁定——新增引用与清单过期均判红。两处既有 bridge 治理断言改指向搬迁后的规范位置且未弱化。登记后续项：残余派发的依赖倒置或包 init 懒化（B-2）、评审指出的 AST 门禁盲区（相对/动态导入形式）。最终 diff 为 44 个文件、新增 1,986 行、删除 1,285 行（代码面 30 个修改文件 +257/−1,281 加 11 个新文件共 1,723 行；其余为本登记更新与 registry 刷新）。 | 独立评审（本关键期专门派发）核实迁移定义与改前源码逐行一致、十四文件残余台账与独立 AST 扫描精确一致、认可「不强拆派发」的裁决（依赖倒置注册表并非更低风险的替代）、确认治理断言改写保持原意且 raw-write 检查更强；结论 approve、零 blocker、三条非阻塞（门禁盲区、私有符号口径、统计修正）。聚焦回归：leader+scenario `120 passed`，world-batch `113 passed, 1 skipped`，runtime 失败集与基线逐字节一致。落地树 maintained smoke 为 `426 passed`、`45 subtests passed`（基线 408 加 18 个新门禁/兼容测试）。Ruff 与 `git diff --check` 通过。 |
| I25 统一架构计划立项 | `accepted` | `d5e4b7cf`，随后由设计原则修订提交补充（同一分支） | 文档迭代。确立[统一架构计划](../unified_architecture_program/README.zh.md)为剩余架构级统一工作的冻结路线图（轨道 T1-T7：DTO 单源化收尾、运行时基座统一、C++ 结构边界、exact-runtime 对齐、声明式配置、测试基建理性化、终局残余审计），本登记表保留为单一迭代台账。把该计划对纳入严格双语面（`STRICT_PLAN_SUBTREES`）与 `docs/plan` 索引，并补上索引中此前缺失的整合计划条目。最终 diff 为 8 个文件、新增 183 行、删除 7 行，含登记与 registry 刷新。 | 双语 registry 现覆盖 78 对，全部同步；维护链接审计：158 份文档、2,608 个链接、零问题。maintained smoke 不变。Ruff 与 `git diff --check` 通过。纯文档迭代；落地门禁由编排方执行。计划修订以后续提交落地：`44627be9`（设计原则与性能边界）、`c55175ff`（全局不变量与基线批判）、`72a52cf0`（轨道重推导）、`779a821b`（SCAL 衍生适配轨道 T8-T11）。 |
| I27 gym-env 包 init 懒化与导入门禁加固 | `accepted` | `0f83a2a5`，分支 `codex/redundancy-consolidation` | 统一架构计划 T2（B-2）。把三个 eager 的 `gym_envs` 包 init（`scenario_loader` 11 名、`universal_env_parts` 44 名、`leader_env_parts` 52 名）改为 PEP 562 懒解析并保留 `__dir__`/`__all__`；把台账内十四个文件的模块级 `python.rl` 残余引用降为函数内延迟导入；经零消费者扫描删除未使用的 `ScriptedC2TaskManager` 模块别名。gym-env 入口面的 import-only `python.rl` 模块数降为零，真实 profile 派发仍在首次使用时懒加载（air 17、naval 18 且确认 `naval_adapter` 加载）。两个导入门禁经评审重放的绕过构造加固：runtime-bootstrap 门禁移除字面量预筛改为全量 AST + PEP 328 相对导入换算，跨行动态导入改为「能力标记×目标字符串」文件级组合告警并配显式核验允许清单；tasking 门禁增加常量折叠、`import_module` 别名跟踪、默认参数表达式的模块级归类；十三个源自评审构造的夹具钉住覆盖面。如实记录一处语义复原：`AdaptiveKLPPO` 导入错误恢复立即抛出而非被吞入 PPO 回退。副作用时点说明：裸导入 `universal_env_parts` 包不再执行 `configure_repo_imports()`/`set_log_level`，二者在首次懒访问 `common` 时执行，且不存在依赖裸包副作用的消费者。最终 diff 为 21 个文件、新增 1,147 行、删除 283 行（代码与测试面 18 个文件 +1,141/−279；其余为本登记更新与 registry 刷新）。 | GPT 评审第一轮：needs-repair（三项门禁绕过 blocking：相对导入预筛跳过、跨行动态导入、常量折叠/别名/默认参数盲区）；全部绕过转为夹具后由评审者重放自身构造复核——最终结论 approve、零 blocker，门禁成本实测 10.8s（约占 smoke 7%）判定可接受。门禁+兼容套件 `25 passed`；world-batch/leader 回归 `160 passed`、`1 skipped`、`22 subtests`（与基线一致）；assertIs 兼容套件 `299 passed`、`1 skipped`、`26 subtests`。落地树 maintained smoke 为 `435 passed`、`45 subtests passed`（基线 426 加九个 tasking 门禁夹具）。Ruff 与 `git diff --check` 通过。 |
| I26 world-batch DTO 家族单源化（六子族之五） | `accepted` | `8af00df2`，分支 `codex/redundancy-consolidation` | 统一架构计划 T1，关键期。把 `world_batch_contracts.h`、`runtime_facade_types.h`、`platform_capability_contracts.h` 与 `bindings_runtime.cpp` 之间逐字段镜像的 world-batch/facade DTO 词汇迁入 `tools/maintenance/dto_schema` 单源：完成六个计划子族中的五个（platform capability/spawn 3 类/32 字段、typed spawn/reset 3/24、world setup/assignment 10/66、runtime capability/config/meta 7/86、runtime window/experiment 15/132）——合计 38 类、340 字段；facade packets/views 子族（约 211 字段）延后并留有范围清单。头文件字段块与对应 `def_rw` 绑定段由 38 个新 schema 展开；8 个类因既有绑定注册顺序与头文件声明序不一致而保留手写绑定（各有 `NOTE(I26)` 标记、`dir()` parity 已核验），`RuntimeWindowActionRequest` 整类跳过（含从未绑定的隐藏成员且注册序本就分歧）。四个持有文件净减 437 行（+313/−750）；新增 115 个文件共 4,034 行（76 个生成产物 +2,461、38 个 schema 源 +1,502、一个共享测试助手 +71）。七个既有源码文本扫描测试文件随之适配（六处经新助手 `tests/support/xmacro_text.py` 做等价展开修复——该助手复用 schema 解析器，另一处为 freshness 注册表扩充）；一个生成 include 命名为 `resident_device_output_descriptor.inc` 以规避 GPU 边界守护的纯 include 路径误报，守护语义未动。代码与生成物载荷：127 个文件、新增 4,643 行、删除 758 行；落地提交另含本登记条目与 registry 刷新。 | GPT 评审第一轮：approve、零 blocker。评审者未采信实现者产物而独立重推 ABI 证据：340 个字段声明与改动前头文件机械比对（顺序、类型 token、默认值表达式在去空白后逐字节等价；184 个裸声明渲染为 `= {}` 者全部为类类型、默认构造语义等价），确认 8 个手写保留类恰为旧注册序分歧集合，用自写探针独立复现跨构建 `dir()`+默认值 parity 38/38，复跑 `generate.py --check`（105/105）、聚焦回归（`7 failed, 308 passed, 1 skipped, 25 subtests`——七处均为既有本机红，其中三处在干净 HEAD 工作树复现）、全量 smoke（779a821b 基线 426/45）与 ruff。三条非阻塞注记：实现者报告的生成物统计（91 文件/+2,947）不实、本登记已按核实值更正；「七个扫描测试修复」精确口径为六处展开修复加一处注册表扩充；展开助手把五个显式花括号初始化字段渲染为裸声明，属外观差异、当前无消费者。落地树 maintained smoke：`435 passed`、`45 subtests passed`。 |
| I28 本机基线红修复与 weapon-realism 残差治理 | `accepted` | `45e0d5e9`，分支 `codex/redundancy-consolidation` | 统一架构计划 T6。纯测试面迭代，两轮修复（19 个文件，+459/−63，全部位于 `tests/`）。第一轮修复九个文件的测试基建基线红：compatibility-quarantine 允许清单匹配器改为路径分隔符归一化，winsock harness 链接行新增 `-lws2_32`（flecs 候选路径解析 HEAD 已存在，链接旗标是唯一真正新增项），修复 GBK 探针解码失败（不声称 PDF 历史项，该项在本基线无对象）。第二轮在**与本树完全匹配的构建**下（`CMO_BUILD_DIR` 验证加载本工作树 `ef_py`；失败集三轮复跑逐条一致）裁定 `weapon_guidance_realism` 长期存在的 45 条 junit 失败：归并为 33 个唯一方法、六个组（主响应选择漂移、邻近投影扩散、跨子系统溢伤——如 G3 hitboxes 用例 `mil_thrust_n` 读数 64510 对预期持平 76310、损失状态升级/饱和、气动/引信响应漂移、机理校准漂移），全部裁定为产品行为/校准漂移，测试基建可修项为零（固定种子、三轮全同、语义级差异）。治理为逐方法、零整包 skip：25 个方法标 `pytest.mark.xfail(strict=True)` 并附组名、根因与 `registered residual, owner: unified architecture program T6 ledger` 指针；4 个全失败 subTest 方法叠加 `unittest.expectedFailure` 折叠；4 个混合通过 subTest 方法用纯 `expectedFailure`（pytest 9 会把通过的 subTest 判 XPASS(strict)），探针证明 unexpected success 仍硬失败。 | GPT 评审第一轮：needs-repair（四条 blocking：分类必须在匹配构建下进行、45 条失败需完整裁定、治理必须逐方法、两处措辞过度声称）。修复复核：approve——评审者独立验证匹配构建 pyd 加载、复跑 weapon 全量（`167 passed, 33 xfailed, 217 subtests`、零失败）、对三个组用 `--runxfail` 揭开标记确认底层失败签名与裁定吻合、重放反红探针（unexpected success 硬失败）、逐一核对登记数字。三条 residual 观察（均不阻塞）：四个纯 `expectedFailure` 节点的理由只在源码注释中（T6 台账应索引这四个节点 ID）；`xfail(strict)` 只在行为恢复时报警、接受漂移为新基线需人工改期望（T6 台账流程责任）；推力漂移示例属 G3 hitboxes 用例而非组内首红（本登记已按此更正）。源工作树验证：weapon 匹配构建 `167 passed, 33 xfailed, 217 subtests passed`；配套组合 `23 passed, 1 skipped`/tools `170 passed, 4 skipped`/support `21 passed`/clock+boundary `9 passed`；779a821b 基线 maintained smoke `426 passed, 45 subtests`；ruff 与 `git diff --check` 干净。落地树验证见本条下文：weapon 套件对 I26 匹配构建与 maintained smoke 均绿。 |
| I29 SCAL 符合性普查与基线修订（T0） | `accepted` | `67f64782`，分支 `codex/redundancy-consolidation` | 统一架构计划 T0。只读普查加基线修订起草；零代码改动。新增双语普查文档 `docs/plan/unified_architecture_program/scal_conformance_census_20260720.md`/`.zh.md`（文档 kind 为 `reference`：描述性普查登记、非独立评审、不含裁定），逐条记录维护树对 G1-G6 不变量的符合性。修正后的 G1 结论：维护面直构旁路为零——`python/rl/runtime/world_batch/adapter.py` 中的 facade 构造是唯一维护面跨界构造点，`UniversalEnv` 已是 fail-fast 壳并由 WP24 门禁钉住，raw-kernel 注入仅存活于 `python/testing/contracts/` 契约夹具；真正的 G1 残余缺口是未类型化的 `ScenarioLoader.sim` 接缝（宽调用面待契约化）。SCAL 基线文档落入源自计划批判的六项修订：SS1.5 实验面、SS6.1 阶段契约修订、SS7.1 阶段契约执行、SS15 内核不变量、SS16 表示策略、SS17 学习面架构说明；计划 README 配对补挂普查链接。载荷：6 个文件、新增 398 行、删除 2 行（4 份修订文档 +209/−2 加两份普查文件）；最终提交另含本登记行与 registry 刷新。 | GPT 评审第一轮：needs-repair（P1：普查误读已降级的 `UniversalEnv`、把维护面 G1 旁路夸大为两条路径；P2：普查文档 kind 误标为 `review`）；修复后评审者对每条修正后表述逐点对照代码证据复核（rg 构造扫描、WP24 门禁存在性、facade 唯一构造、loader 接缝），最终结论 approve、零 blocker。纯文档迭代：registry 刷新后双语审计与维护链接审计干净；`git diff --check` 通过；落地树 maintained smoke 复跑绿。 |
| I30 类型化实验定义与空战矩阵单源 | `accepted` | `913abb32`，分支 `codex/redundancy-consolidation` | 统一架构计划 T5 第一切片，对齐 SS1.5 实验面修订。新增单一 owner 包 `python/experiment/`（纯标准库、零 bootstrap 副作用：裸导入不加载 `ef_py`/`gymnasium`/SB3）：`definition.py` 冻结 `Experiment = ScenarioRef × ConfigComposition(base+delta) × SeedSpec × EvaluationProtocol`、构造即 fail-fast，`ExperimentRegistry` 作为 G5 注册插座（重复注册与悬空 base/协议引用立即报错；悬空场景路径仅做语法校验、存在性由 freshness 门禁钉住）；`composition.py` 持有确定性合并（base 键保序、delta 新键按 delta 序追加、映射递归、非映射整体替换）；`air_combat_matrix.py` 把 `examples/config/training/active/air_combat/` 的 24 文件矩阵表达为一个基座 `air_combat_1v1_hmoe_execution_v1` 加 24 份 delta，注册表只覆盖实际用到的维度（六个场景、空种子集、协议 `smoke`×2/`probe`×22——无预铺字段）。生成器 `tools/maintenance/experiment_matrix/generate.py`（`--check`/`--write`/`--manifest`）首跑即逐字节复现全部 24 个文件（24/24 up-to-date，零归一化、零豁免）；达成 parity 需要建模现存文件的三项序列化方言（十个条目的 `learning_rate` 写作十进制 `0.00003`、三个条目标量数组逐元素展开、四条尾部键规则加 CRLF checkout）。freshness 门禁 `tests/architecture/governance/test_experiment_matrix_freshness.py`（check、24 输出钉死、目录↔注册表全等、隔离副本漂移注入、复原）已加入 smoke manifest。命名有意偏离计划草稿：`.gitignore` 第 85 行对任意层级忽略 `experiments/`，故 owner 位于单数 `python/experiment/`、生成器位于 `tools/maintenance/experiment_matrix/`。载荷：一个已跟踪文件增一行（smoke manifest），另七个新文件共 1,890 物理行（owner 包 1,254、生成器 218、两个测试文件 418；按非空行口径为 1,670）。矩阵 README 五个先存过时文件名未触碰（超出范围，记为残差）。 | GPT 评审第一轮：approve、零 blocker。独立验证：首跑 24/24 byte parity 加 SHA-256 抽查与隔离副本漂移注入变红；三项方言确认为对现存文件的观测（十进制字面量普查 10/14、恰好三个展开数组、尾键规则 24/24），矩阵相对基线提交无改动；裸导入纯净性、深冻结与 fail-fast 语义、G5 尺度（无预铺字段）、场景配对对照 README/训练命令/场景元数据；新测试 `41 passed`、加 runners `86 passed`；smoke `436 passed, 45 subtests passed`（基线 435 加 freshness 门禁）；ruff 与 `git diff --check` 干净；写集与 I26 落地零交集。一项声明评为部分属实并已在本登记中改述（悬空 `ScenarioRef` 仅语法校验）。三条非阻塞加固点记为残差：生成器未对对象键做 `json.dumps` 转义（含引号的假想键会产出非法 JSON）、literal 覆写比较继承 Python `True == 1` 相等性（bool 与 int 漂移可能漏过）、门禁钉住场景存在性但未钉死完整实验→场景映射（两个合法场景互换可通过）。落地树 maintained smoke 见下文。 |
| I31 world-batch DTO 家族单源收尾（子族 F：facade packets/views） | `accepted` | `4aeb64b0`，分支 `codex/redundancy-consolidation` | 统一架构计划 T1，收官 world-batch/facade DTO 词汇六个计划子族中的最后一个。逐类核验更正了 I26 移交的范围口径（「约 211 字段」是移交时的粗估、非普查）：子族 F 实为 17 类、123 字段。16 类/108 字段迁入 `tools/maintenance/dto_schema` 单源（11 个 facade 归属加 5 个 contracts 归属——后者物理定义在 `src/runtime/contracts/runtime_dto_contracts.h`，生成 include 按既有惯例入 `contracts/detail/`）；头文件字段块与对应 `def_rw` 绑定段由 16 个新 schema 同步展开，且 16 类的绑定注册序均与头文件声明序一致，无需任何手写绑定例外。`ExecutionBatchStepResult`（15 字段）登记为 held：其 `std::vector<std::array<double, 4>>` 字段的尖括号内逗号会被 C 预处理器误拆为额外宏参数（预处理器只配对圆括号），类型别名规避会破坏类型 token 逐字节等价纪律，故整类保留手写。`ObservationSchemaVersionParts` 为从未绑定的内部辅助结构，不纳入 schema 化。四处源码文本边界测试经既有 `tests/support/xmacro_text.py` 展开助手适配、断言载荷不变；四处 `nb::class_<T>(m, "T")` 精确 marker 收窄为 `nb::class_<T>` 前缀、各自仍唯一锚定。载荷：57 个文件、新增 1,814 行、删除 295 行（9 个修改文件 +280/−295 加 48 个新文件：16 个 schema 源、16 个生成 include、16 个生成 Python builder）。 | GPT 评审第一轮：approve、零 blocking、零 non-blocking——十项声明全部核验属实。评审者机械重推 ABI 证据（16 类旧声明对 schema 与生成 include 逐字段全等；旧绑定注册序=声明序 16/16），用自做预处理器实验复现 held 类论断（宏声明三参实收四参；别名可编译但改变类型 token），确认从未绑定的辅助结构，自写子进程隔离探针对拍两个构建产物（19/19 类：16 迁移加 3 哨兵，`dir()` 与递归展开默认值全等），复跑 `generate.py --check`（全量 up-to-date）、增量构建、`ef_test_all`、聚焦回归（`6 failed, 309 passed, 1 skipped, 25 subtests`，六处红全部在隔离的改动前基线复现：五处 flecs 静态库链接签名加一处既有 `common.ef_py` 属性缺口），并在改动树与隔离基线各复跑一次 maintained smoke（均 `435 passed, 45 subtests`，与 45e0d5e9 基线一致）。写集确认与 I30 落地零交集；`runtime_dto_contracts.h` 的写集扩展由五个实体定义与既有 `DTO_HEADER` 测试常量证明正当。落地树 maintained smoke 见下文。 |
| I32 WorldBatchCore 阶段契约基座与 execution 模式插件化（第一切片） | `accepted` | `591dff16`，分支 `codex/redundancy-consolidation` | 统一架构计划 T2 第一切片。新增 `python/rl/runtime/world_batch/core.py`（+573 物理行/500 非空行）：八个批步阶段契约携带 SS6.1 完整字段集——权威 `P<n> <Name>` 语义阶段标识并如实声明跨阶段（`command_sync` 跨 P2 TaskingIntent/P3 CommandDelivery；`reward_episode` 跨 P10 ObservationExport/P1 WorldSetup 并声明后发射评估与回合 autoreset 两个事件驱动子阶段），读写集对照 `step_wait` 实码核定，含 clock domain、G4 六层信息声明与扩展点。契约真实承载而非装饰：`step_wait` 每阶段一个唯一 `# [stage:<name>]` 锚点、源码结构测试钉住锚点序列与声明顺序逐项相等，导入期校验交叉核对各阶段扩展点与插件 hook 绑定（三个失败分支各有负向测试）。`ExecutionModePlugin` 加 G5 注册表（重复/未知模式 fail-fast）；`StandardExecutionPlugin` 真实路由三个实测 execution 模式差异点（mainline 与 standard 行为更新、mainline 命令链同步跳过、空战 hybrid 事件终结）；domain finalizer 由 `vec_env.py` 在插件构造期注入，`core.py` 保持零 `gym_envs` 导入节点（AST 遍历测试钉住）、六个 hook 体零 import 指令（反汇编钉住）。cooperative 与 leader 模式注册为薄壳存根，其内部逻辑迁移上插件接口为已登记的下一 T2 切片（实测重复约 350 行）。载荷：4 个文件、新增 1,199 行、删除 21 行（`__init__.py` +22、`vec_env.py` +23/−21、新增 core 与其 72 测试套件 +581 物理行）——按计划既定经济学属基础设施投资切片。 | GPT 评审第一轮：needs-repair、三项 P1 blocking——行为漂移（hybrid `truth_before=None` 时跳过了旧内联代码无条件执行的事件终结调用，且新测试反而钉住了漂移）、阶段契约用自造下划线阶段名且读写集失真、未与 `step_wait` 接线（装饰性数据）、core 热路径逐步懒导入 `gym_envs` 违反 G2 单向层环。同轮还更正了实现者的 smoke 证据口径："2218 passed"是手工扩大的近全量 `tests/` 套件、其红项全为基线既有，真正的 manifest smoke 为 `436 passed, 45 subtests`。修复复核：approve——评审者独立 mock 复测 None 路径（恢复无条件调用、参数与旧内联逐字一致），对照 `step_wait` 核验权威阶段词汇与三处读写集修正，确认锚点序列测试与导入期扩展点校验为真实接线，重新反汇编六个 hook（零 import 指令），并复跑全部门禁（聚焦套件 `270 passed`=基线 198+新增 72/同一组 5 处既有 flecs.h 环境红/1 skipped/22 subtests；manifest smoke `436 passed, 45 subtests`；ruff 与 `git diff --check` 干净）。落地树 maintained smoke 见下文。 |
| I33 engagement DTO 家族单源化（T1） | `accepted` | `48c86c4b`，分支 `codex/redundancy-consolidation` | 统一架构计划 T1。逐类普查纠正了计划继承的口径（"~445 字段/29 类"恰等于 `engagement_contracts.h` 手写字段总数、已排除更早单源化的 `EffectsEvent`，但漏了物理上与 world-batch 家族共处 `runtime_facade_types.h` 的两个 engagement 类）：engagement 家族真实规模为 30 类、481 字段（`engagement_contracts.h` 28 类/445 字段，加 `EngagementBatchRequest`/`EngagementEventPacket` 2 类/36 字段）。30 类一次性（未分批）迁入 `tools/maintenance/dto_schema` 单源，注册表增至 98 个 schema 模块/197 个生成物：逐类核验 `bindings_runtime.cpp` 原有绑定注册序均=头文件声明序，零手写绑定例外，全程沿用 I26/I31 确立的宏+`.inc`+具名局部变量范式。`EngagementEventPacket` 两个 `InformationStateSource` 溯源字段复用 I31 确立的多参数 `make_information_state_source(...)` 默认值模式。按 SCAL 修正案 (b)，`stage`/`owner_stage` 词汇字段仅作普通字符串迁移并附说明注释标注为未来阶段契约锚点，未引入机制。两处相邻项如实未动：`RecentEngagementEvents`（`src/core/engine/engagement_event_types.h`，14 字段，形态同样干净）在本轮写集边界外、记录为下一轮自然候选；`engagement_event_recorder.h` 的 13 个类型经核实从未 Python 绑定、正确地不属跨边界 DTO 词汇。源文本边界测试适配三个文件且各不相同：`test_engagement_contract_shape.py` 改用共享展开助手并将结构体正文正则尾部由 `\n\}};` 放宽为 `\n?\}};`；`test_facade_engagement_evidence_gates.py` 仅新增展开调用；`test_runtime_dto_contracts.py` 作为评审驱动修复套用同款放宽。放宽根因：`expand_header_field_incs` 吞掉 `#include` 行自身换行，全宏化结构体以"末字段紧贴 `};`"收尾，严格式会跳过本结构体并贪婪吞入邻居。T6 遗留登记：该 helper 换行缺陷，及仅因头文件布局侥幸保绿的另两处严格正则（`test_typed_platform_spawn_contracts.py`、`test_policy_contract_shape.py`）。载荷：98 个文件、新增 4,166 行、删除 1,150 行（8 个修改文件 +473/−1,150 含三处测试适配；90 个新文件共 3,693 行：28 个 contracts `.inc`+2 个 facade `.inc`+30 个 schema 源+30 个生成 Python builder）。 | GPT 评审第一轮：needs-repair——一项 blocking 加登记更正。blocking 回归：`test_runtime_dto_contracts.py::test_device_resident_output_descriptor_stays_additive_export_only_surface`（4aeb64b0 基线绿、改动树确定性红，且在本迭代聚焦/smoke 选集之外，故此前全绿声明与该红并存）——两个 facade 类宏化后严格正则对 `RuntimeCapabilities` 正文的过匹配扩至 14,642 字符、吞入 `DeviceResidentOutputDescriptor` 及违禁 token。同轮机械核验了 30 类全量 ABI（声明序/类型 token/默认值逐字节等价）与注册序、自写探针复现 parity 32/32，并更正两处登记数字（98 schema 非 120；新文件 3,693 行非 3,153）。修复复核：approve——匹配体从 14,642 收敛至 1,214 字符、以本结构体边界收尾，隔离副本注入违禁 token 的反例证明守护仍有效，目录 4 处 `test_wp22_*` 红在隔离干净基线工作树逐条同名复现（既有本机红），登记数字全部通过终审。聚焦回归 `345 passed, 1 skipped, 25 subtests`、6 处既有本机红不变；maintained smoke `436 passed, 45 subtests`；`generate.py --check` 全量 up-to-date；`ef_test_all` 8/8；ruff 与 `git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I34 cooperative/leader 共性重复下沉至 world-batch 基座 | `accepted` | `c2952d61`，分支 `codex/redundancy-consolidation` | 统一架构计划 T2 第二切片。复测 I32 重复台账后，把 cooperative/execution 共性重复下沉至新建 `python/rl/runtime/world_batch/_shared_ops.py`（253 物理行）：四链命令差分+提交内核（`diff_single_entity_command_chain`/`submit_command_chain_assignments`，参数化承接 execution 的 handle 标量与 cooperative 的 world 字典两种迭代风格）、观测字典组装、`normalize_seed`/`save_obs_to_buffer`（逐字孪生）、后端模式解析（参数化；execution 侧逐 loader 的 `use_compiled_execution_step_runtime` 检查保留在其 mixin 覆写中，两个调用点的可用性语义差异得以保留）。修改面净删 64 行（cooperative −52、vec_env −43、visual mixin −3、core +28、`__init__` +4、兼容壳 +2）；生产净 +189。`CooperativePlugin`/`LeaderPlugin` 替换 I32 存根注册，但如实定性为为后续迁移铺位的命名升级：钩子等于基类默认、当前无生产调用点消费（cooperative `step_wait` 仍内联 `update_behaviors`；leader 组从不调用 `resolve_execution_mode`）——docstring 如实声明。三项 residual 如实保留未统一：step 评估批准备（跨 slot 均匀检查与逐 slot time-step 来源）、VecEnv attr/method 接口（world 回退逻辑）、步进内层+奖励/回合管理（cooperative latching 语义根本不同）。`project_world_*` 的 monkeypatch 接缝由 vec_env/cooperative 模块属性迁至 `_shared_ops` 模块属性（晚绑定、patch 有效）；本落地含两处评审抓出的接缝修复——`world_batch_vec_env` 兼容壳的 `project_world_*` 再导出改指 canonical 定义模块 `command_chain_cache`（壳此前断导入 ImportError，波及维护面 `examples/viz` 导入链），以及 cooperative 观测测试补丁面适配。载荷：11 个文件、新增 536 行、删除 263 行（净 +273）；测试 +84 行、12 个新契约测试（core 套件 72→84）。 | GPT 评审第一轮：needs-repair、两项实跑证实的 blocking，均在实现者聚焦选集之外——兼容壳 `world_batch_vec_env` 导入即 ImportError（其 `project_world_*` 再导出指向已不再导入它们的 `vec_env`），以及 `tests/runtime/multi_agent/test_cooperative_vec_env_observation.py` 补丁已被移除的 cooperative 模块属性（AttributeError 红）。同轮逐函数核验行为等价（四链差分顺序与守卫、cooperative 无条件字典回写在两个读点语义等价、后端解析调用方差异保留、观测组装 reshape 为 no-op），并对插件声明如实定级（已注册、钩子=默认、无生产消费者）。修复复核：approve——壳 diff 确认最小（导入改指 canonical `command_chain_cache`、`__all__` 未动、壳属性与 canonical 函数 `is` 同一对象、`examples/viz` 导入链恢复），multi_agent `40 passed` 全绿，三处 docstring 与代码事实吻合，全仓 `project_world_` 终扫十个引用点全部落位，`leader_phase_manager_approach_arm` 契约失败经隔离基线检出复现（既有环境红；`tests/gpu` 导入顺序测试同为 `build-gpu` 缺失的既有环境红）。门禁独立复跑：聚焦 `282 passed`（基线 270+新增 12）/同组 5 处 flecs 红/1 skipped/22 subtests；manifest smoke `436 passed, 45 subtests`；契约门 10/10；ruff 与 `git diff --check` 干净。本登记数字按评审终审修正口径转录。落地树 maintained smoke 见下文。 |
| I36 T6 残差加固包 | `accepted` | `009aaa67`，分支 `codex/redundancy-consolidation` | 统一架构计划 T6，一次性收敛三条已登记评审残差外加一条 retained 存档污染缺陷。（A）`tools/maintenance/experiment_matrix/generate.py`：对象键改走 `json.dumps` 序列化（原为裸 `f'"{key}"'` 拼接，含引号键产出非法 JSON）；literal 覆写比较改为递归、类型同一性感知的 `_strict_json_equal`（裸 `!=` 在 `True == 1` 下会静默放过 bool 型覆写对 int 值）；freshness 门禁新增 24 条 experiment→scenario 全表断言（仅存在性检查抓不住两个合法场景互换）。三条修复各配专属负向测试，均实测证明对修复前生成器报红、修复后转绿。（B）`test_release_signoff_gate_cli_writes_default_artifacts` 是该文件唯一不用 `tmp_path` 的测试：无参 CLI 默认值直写真实 `res001_release_signoff_20260531` retained 目录，CRLF checkout 下 sha256 重算致每次运行改写三个文件——正是此前两次工作树"污染"误报的根因。改为走 CLI 已有 `--output-dir`/`--report` 参数重定向，另加零 IO 静态断言钉住 `DEFAULT_OUTPUT_DIR`/`DEFAULT_REPORT_PATH` 常量；`source_release_signoff.py`（I19 保留的 Windows CRLF 写入约定）未触碰。（C）新建双语 `reference` 台账 `docs/plan/unified_architecture_program/t6_residual_ledger.md`/`.zh.md` 并自计划 README 挂链，索引：I28 的 33 方法/六组 weapon-realism 清单并点名四个纯 `expectedFailure` 节点 ID、I33 的 xmacro 换行缺陷与两处仍潜伏的严格正则、I31 的 `ExecutionBatchStepResult` held、I33 的 `RecentEngagementEvents` 移交（候选记载见 I33 登记行；I35 迁移进行中）、（B）修复本身、以及在 c2952d61 复核过的本机五类环境红清单。载荷：7 个文件、新增 642 行、删除 6 行（5 个修改文件 +177/−6 加两份台账文档）；四处评审要求的台账文字修正已在落地时施加（见评审栏）。 | GPT 评审第一轮：approve、零 blocker、四处非阻塞台账文字修正（均已在落地时施加）：台账把两组 `expectedFailure` 四元组的装饰器形态写反（混合通过型四个是纯 `expectedFailure`；全失败型四个才叠加 strict-xfail——已按 I28 登记行原文改正）、一处已修复文件路径目录写错（`build_system` 更正为 `runtime_facade`）、`RecentEngagementEvents` 出处句消歧为点名 I33 登记行、zh 版一处中英混排修正。评审者独立回放修复前生成器（复现 JSONDecodeError 与 DID-NOT-RAISE）、用十二个边界用例探测 `_strict_json_equal`（仅保守方向拒绝、无反向漏检）、以 SHA-256 抽查对照其 I30 评审记录验证 24/24 byte parity、逐字符重构 CRLF sha 漂移链（`822c496d`→`809dfe67`）、连跑两次 signoff-gate 文件确认 retained 哈希稳定、对台账事实（含四节点 ID 与分组表）逐条实跑核验、并复现 smoke 代数（`438 passed, 1 failed, 45 subtests`，唯一失败即本次落地 registry 刷新所解决的预期双语标记——评审者已在内存中模拟注册后转绿）。registry 刷新后的落地树 maintained smoke 见下文。 |
| I35 command/tasking 维护批处理契约家族单源化暨 RecentEngagementEvents 收编（T1 收官） | `accepted` | `7299e6be`，分支 `codex/redundancy-consolidation` | 统一架构计划 T1，收官最后一个 DTO 桶。逐类普查纠正了计划继承的范围认知：MissionCommand/TaskOrder/LeaderIntent/PilotReport 的 ECS 组件定义与 Core/Air/Naval/Ground 域切片在 `src/components/**`、本轮写集边界之外；真正可动的家族是 `src/runtime/contracts/world_batch_contracts.h` 的维护批处理契约传输层——15 类/82 字段（四个 `*MaintainedBatchContract` 聚合、四个独立 Air/Naval directive 结构体、七个三字段 `World*Assignment` 载体）——加 I33 移交的 `RecentEngagementEvents`（`src/core/engine/engagement_event_types.h`，14 字段，绑定于 `bindings_core.cpp`）：16 类/96 字段一次性迁入 `tools/maintenance/dto_schema` 单源，注册表增至 114 个 schema 模块/229 个生成物。十二个类两侧全宏化（绑定注册序=声明序）；四个 `*MaintainedBatchContract` 是新型 held 变体：其绑定从来恰好缺失末尾 ground 字段（`ground_static_task`/`ground_static_status`；考古确认旧绑定文本零引用），故 schema 持有完整字段清单（`.inc` 与 ABI 完整）而绑定在 `NOTE(I35)` 标记下保持手写、不新增任何 Python 可见属性。GPU packed views 经普查在 `src/gpu/**` 零 command/tasking 引用；`mission_command_codec.h/.cpp` 作为 T1"JSON codec 别名"风险的具体实例经通读仅含 JSON 自由函数、非 schema 对象。三个源文本边界测试适配（shell-guard 增加展开并把九处字段行断言改述为展开助手的裸声明惯例；四处绑定风格 marker 收窄为仅类型前缀；freshness 门禁十六条登记）；同轮修复一处因新绑定写法引起的 wp24 文本扫描红。落地字节口径载荷：57 个文件、新增 1,803 行、删除 208 行（9 个修改文件 +255/−208，加 48 个新文件共 1,548 行：16 schema 源、16 生成 include、16 生成 builder）。 | GPT 评审第一轮：approve、零 blocker、三条非阻塞更正（均已反映于本登记）。评审者机械核验 16 类全量 ABI（头/schema/`.inc` 三方逐字节等价，含别名 token 与枚举默认值），独立确认四个 held 类在两侧构建的 `dir()` 均不可见 ground 字段而 `.inc` 携带完整字段清单，复核"从未绑定"考古，在全量重建的 `ef_py` 上复现 parity 18/18（16 迁移+2 哨兵，枚举折叠探针），并复跑 `ctest`（8/8）、smoke（48c86c4b 基线 `436/45`）、ruff 与 `git diff --check`。更正：实现者"127 个产物"与任何真实口径不符、改述为 114 schema/229 生成物；登记行数按落地字节复算（评审者在其中断窗口观察到 `bindings_runtime.cpp` 有 +2 行良性漂移，已按当前字节全量重审，落地按恰为该字节做逐文件核对）；超集回归多暴露一处台账之外的既有本机红（`test_wp12_runtime_facade_does_not_gain_a_second_maintained_injection_api`，纯净基线工作树复现），本次落地一并补入 T6 残差台账。落地树 maintained smoke 见下文。 |
| I37 xmacro 展开助手换行修复与严格正则统一 | `accepted` | `745ce1ce`，分支 `codex/redundancy-consolidation` | 统一架构计划 T6，收敛 I33 评审登记的 helper 缺陷。修复本体是单点正则改动：`tests/support/xmacro_text.py` 的 `_INC_INCLUDE_RE` 不再吞掉 `#include` 行自身换行，全宏化结构体的渲染与手写结构体一致（`};` 前有换行），`expand_header_field_incs` 与 `expand_binding_field_incs` 共用该正则同步修复。新增四个针对性单测（`tests/support/test_xmacro_text.py`，围绕真实 `engagement_entity_ref.inc` 构造），各自实证修复前红（含"邻居结构体被吞"的精确复现）、修复后绿。旧行为迫使的两处正则放宽（`test_engagement_contract_shape.py`、`test_runtime_dto_contracts.py`）恢复严格式 `\n\}};`；I35 在 shell guard 的裸声明断言经核实与换行修复正交、未动；两处潜伏严格正则点（`test_typed_platform_spawn_contracts.py`、`test_policy_contract_shape.py`）从"因头文件布局侥幸保绿"变为构造性恒正确、零改动复验绿；全仓 Python 扫描 `\n?\}};` 零残留。T6 台账第 2 节标注"已于 I37 修复"并新增证据小节，台账配对的双语 registry 哈希同轮刷新。载荷：7 个文件、新增 355 行、删除 32 行（6 个修改文件加 107 行新单测文件）。 | GPT 评审第一轮：approve、零 blocker、两条非阻塞转录更正（均已在落地时施加）。评审者在隔离探针树独立重放红/绿构造（修复前 4 测全红、修复后全绿；绑定侧由第四个单测钉扎）、复跑九个受影响边界文件（`64 passed, 1 failed`）、复扫严格式统一、按官方归一化口径核验台账/registry 同步（其最初的原始字节比对不匹配系评审方工具口径错误）、复跑 smoke（`439 passed, 45 subtests`，零下降）与治理套件（20/20）。唯一失败驱动了本轮评审的关键谱系甄别：`test_wp24_python_command_chain_business_writes_use_maintained_contracts` 在 48c86c4b 绿、自 c2952d61 起红——系 I34 引入的守护适配缺失（命令链写调用下沉 `_shared_ops.py` 而守护合成扫描集未覆盖；行为测试 23/23 绿；`adapter.py` 在各提交均含 token，故实现者"未接入 adapter.py"表述与"既有环境红"定性均已在落地时于台账更正，并作为台账第 6 节记于 I34 名下、修复方向为扩守护扫描集）。落地树 maintained smoke 见下文。 |
| I38 C++ include 方向门禁与 facade 结果投影去重（T3 第一切片） | `accepted` | `71cdb12c`，分支 `codex/redundancy-consolidation` | 统一架构计划 T3 第一切片：为物理 link 单元拆分建立可执行的边界事实。新增 `tools/architecture/cpp_include_graph.py` 按编译器查找序（含入文件同目录优先、后 `src/` 根）扫描 `src/**` 全部 `#include` 边（387 文件、1,065 条维护内边、零未分类），归入 16 个细粒度分组——地面真相是各 `src/**/README.md` 自身的 Allowed/Forbidden/Dependency Direction 原文——再折叠为 T3 粗层。初版粗算 19 条违规经逐边对照 README 原文收敛为六条真实既存违规，全部为真实结构耦合而非 include 路径笔误（`components/` DTO 按值持有 Kalman 导引头滤波器状态；`WorldBatchRuntime` 持有 `ExecutionEpisodeController` 批量状态并三处直连 `gpu/` 可视化/粗筛头文件；一个 contracts DTO 经 X-macro 拼接借用 mission 的批量准备类型——其中一条静态看似可删、增量构建验证即失败、还原后与其余一并 ratchet）。六条全部以"文件+行+include 文本"三元指纹登记于 `tests/architecture/fixtures/cpp_include_direction_allowlist_20260720.json`，各附归因与物理拆分切片指向。新门禁 `tests/architecture/governance/test_cpp_include_direction.py`（7 测试，已入 smoke manifest）执行零新增违规断言加三条粗层断言（facade 零入边、content 方向限制、tools/tests 零入边）、允许清单健全性校验与合成树负向自测；另做过真实文件"注入红→还原绿"演练。facade 结果投影重复经全部八个 `runtime/facade/*.cpp` 测量：`runtime_facade_packet.cpp` 两条 export 路径现共享 `for_each_distinct_export_world_index` 与 `filter_valid_world_refs` 两个 helper（该文件净 +11 行——收益在单源逻辑而非行数）；两处更大候选测量后明确搁置——双 typed-platform-spawn materializer（约 255 行）被既有 WP20/WP22 边界守护刻意锁定为文本互斥，counterfactual parent/branch 镜像（约 110 行）位于该轨道自己点名风险的 replay 关键路径。零 DTO/ABI/公开头改动。载荷：5 个文件——2 个修改（+55/−44）加 3 个新文件共 746 行（444/233/69）。 | GPT 评审第一轮：approve、零 blocker、三条非阻塞更正（均已在落地时施加）。评审者独立复算普查（387/1,065/0，四个自建解析案例全对），对照 README 原文抽验五处"已授权"排除，逐条代码核实六条违规的结构性定性（含 X-macro 拼接的间接依赖），复跑门禁（7/7，约 6-8 秒、判定约占 smoke 6% 可接受），评估行号指纹脆性为有界维护成本的刻意 ratchet 设计，重放负向自测，逐行验证两个投影 helper 语义等价（首见序保持、谓词一致、仅匿名命名空间），确认 WP22 互斥守护确实禁止两个 materializer 共享实现，并复跑 ctest 8/8 与 smoke `446 passed, 45 subtests`（基线 439 加七个门禁测试）。更正：新文件行数为 746 非 665；落地时删除了指向未提交一次性普查脚本的悬空 docstring 引用；三处 T6 台账未覆盖的既有红经对账后于落地时补入（diagnostics 脚本治理红，加两处空战校准漂移红——评审者用 2026-07-18 改动前旧二进制直接复现，比同二进制 stash 对照更强的固有性证明）。落地树 maintained smoke 见下文。 |
| I39 wp24 命令链守护扫描集适配 | `accepted` | `b618971f`，分支 `codex/redundancy-consolidation` | 统一架构计划 T6，收敛台账第 6 节（记于 I34 名下的守护适配缺失）。`test_wp24_python_command_chain_business_writes_use_maintained_contracts` 现于测试函数内部把 `python/rl/runtime/world_batch/_shared_ops.py` 拼入其两个本地扫描变量（vec_env 合成与 cooperative），附 `NOTE(I39)` 注释记录 I34 下沉事实——共享的 `helpers.py` 源表未动、其唯一另一消费者不受影响，断言集合逐字节未变（负向 legacy token 循环现也扫描 `_shared_ops.py`，守护严格增强）。负向自证：树外演练脚本仅拦截 `_shared_ops.py` 读取、用内存副本把一处 maintained 写降级为 legacy 形式，守护如期变红且工作树字节未动。台账第 6 节标注"已于 I39 修复"并新增 6.1 证据小节。落地载荷：4 个文件——守护测试（+16/−2）、两份台账文档、以及按迭代任务书由落地方执行的双语注册表哈希刷新。 | GPT 评审第一轮：needs-repair、单一发现——最终写集未刷新台账配对的双语注册表哈希，源工作树 manifest smoke 实测 `438 passed, 1 failed`（红项即台账编辑自身触发的注册表标记；隔离 745ce1ce 基线上该门绿），而实现者报告的 `439/45` 测于台账编辑之前。因注册表刷新按迭代任务书本就是落地方职责，未派返修轮：落地时执行刷新、把 6.1 验证块的 smoke 行改为如实口径、并在落地树复验绿。其余全部经评审第一手核实：三段扫描集结构、两个变量拼接正确且零放松（边界分析：换行分隔、无假阳性/假阴性边缘，`_shared_ops.py` 含全部六个 maintained token 且零 legacy token）、`helpers.py` 未动且另一消费者绿（两文件合计 14 passed）、独立重构的负向演练（不同破坏方式、守护红、无残留）、目标文件 `3 passed`、`runtime_facade` 目录 `67 passed, 4 failed`（已知 wp22 四件套、零新增）、wp12 不变、ruff 与 `git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I40 cooperative/leader 真实接线到 execution 模式插件 | `accepted` | `4ae20e39`，分支 `codex/redundancy-consolidation` | 统一架构计划 T2 第三切片，收敛 I34 残差"插件已注册但无生产消费者"。cooperative `step_wait` 与 leader `step_indices` 的后步进行为更新现经 `_mode_plugin.update_post_step_behavior` 路由、后行为命令链同步经 `skip_post_behavior_command_sync` 门控，插件在构造期经 `resolve_execution_mode` 一次解析；两插件保持基类默认钩子，路由后行为与被删内联代码逐字等价（`update_behaviors(sim_time, truth=..., inst=..., sync_to_kernel=False)` 含关键字形式不变，新同步门对这两模式恒为假）。两模式刻意只占 execution 三决策点中的两个：`finalize_post_step_truth` 不路由，登记为审慎决定而非疏漏——cooperative 构造器对 air-combat-hybrid 动作模式 fail-fast（基线既有 ValueError、错误文案点名缺失的 gate/finalization 契约），leader 组自身步进路径零 finalizer/air-combat 引用（其内层 `WorldBatchVecEnv` 的 `StandardExecutionPlugin` 在自身路径承担 hybrid 终结），为恒 no-op 的每 slot 调用买单只会白增热路径开销；一条源码钉扎测试断言 `step_wait` 不含 finalize 调用，未来解锁必须显式改测试。行为等价三层证明：精确参数元组的 mock 钉扎（非宽松 assert-called）、调用点源码结构钉扎、两插件钩子对基类的 `is` 同一性钉扎（未来任何覆写即红、迫使显式决策并补 dis 覆盖）。`core.py` 改动为纯 docstring（+24/−18），两插件描述与新事实一致。载荷：4 个文件、新增 179 行、删除 32 行；core 契约测试 84→99（14 个函数/15 个测试项）。 | GPT 评审第一轮：approve、一处非阻塞计数口径更正（已在本登记施加）。评审者逐行核验三个生产 diff（参数逐字等价含 `sync_to_kernel=False`；同步门恒假；leader `sync_command_chain(target_indices)` 保留），确认 cooperative 的 hybrid 拒绝为基线既有并判定不迁移裁量正确（构造期 fail-fast 即防护、每 slot no-op 调用纯属开销），核验 mock 测试钉住精确参数元组，确认唯一的 loader 实例属性 patch 接缝经插件属性查找仍然有效，复跑全部套件（core 99 绿含 I32 的 84；`world_batch`+`runtime/facade` 250 passed、同组 5 处 flecs 红——把实现者"240→255"的通过+失败总数口径改述为通过数口径 235→250；multi_agent 加 leader 87 绿；wp24 守护 3/3），并自跑实现者漏报的 manifest smoke（`446 passed, 45 subtests`）。`_shared_ops.py` 未动；十个 `project_world_*` 引用点不受影响；写集与 I41 零交集。落地树 maintained smoke 见下文。 |
| I41 include 方向 ratchet 收敛：六项评估与 Missile-EKF 迁移 | `accepted` | `f80a36c5`，分支 `codex/redundancy-consolidation` | 统一架构计划 T3 第二切片。I38 的六条 ratchet include 方向违规全部完成消费者/绑定面普查并给出书面裁决。收敛一条：(a) `components/weapon_common.h` 的 `Missile` 按值持有 `SeekerEkfState`/`SeekerEkfParams`、全仓恰四个触点且零 Python 绑定，两结构体逐字节迁入新 components 自有叶子 `src/components/combat/common/missile_seeker_state.h`（29 行）；`kalman_seeker.h` 反向 include（models→components 策略允许），`weapon_common.h` 换用新叶子，`default_guidance_model.cpp` 补上迁移切断的传递链原先提供的直接 include。五条缓办并升级为"已评估设计缺口"归因：三条 `gpu::` 边与 `ExecutionEpisodeController` 批量所有权边均为 WP4 热路径/GPU 集成缝上的多点位功能耦合（指向 T4 精确运行时对齐）；(f)——`WorldExecutionEpisodeStepRequest` 借用的 `StepEvaluationBatchConfig`/`EnvState`——普查最深：`bindings_episode.cpp` 57 次 `def_rw` 加 `EnvState` 按值嵌套十个 mission 聚合类型，原样迁移只会反转违规、contracts 镜像则要复制嵌套类型图，定性为 T1 DTO 收尾量级迁移而非 T3 机械搬移。允许清单净 6→5、存留五条 reason 升级（`allowlist_version` v2）；T6 台账新增第 7 节完整评估矩阵。本迭代聚焦回归还暴露 `test_ground_runtime_lifecycle_bridge.py` 一处既有红，经评审谱系甄别为 I34 下沉的姊妹守护缺口（48c86c4b 绿、自 c2952d61 起红；I39 修复只覆盖 wp24 守护文件）——本次落地登记入台账第 6 节、由 I42 修复。载荷：7 个文件、新增 441 行、删除 41 行（6 个修改加 29 行新叶子；零 CMake 改动）。 | GPT 评审第一轮：approve、零 blocker、三条非阻塞更正（均已在落地时施加）。评审者验证迁移结构体原始字节逐字节相等（含注释与 banner），独立复扫触点普查（恰为点名的四个文件、无第五个消费者；`default_guidance_model.cpp` 传递链修复正当；models→components 在 I38 策略与 components README 叶子定位内），重放 ratchet 的陈旧指纹检测（旧清单对当前树恰产出一条 stale 零 unexpected——门禁确实检测到修复本身），复算 (f) 普查（57 `def_rw`=15+42；十个嵌套类型逐一核对）并认同缓办裁决（一处措辞注记：(b) 边为三处调用点加一处类型构造点、非"四处调用点"），复现 kalman doctest（3 用例、17,423 断言、零失败）、ctest 8/8、门禁 7/7，并把六处聚焦回归红与台账逐一对账。更正：台账 7.4 节零绑定表述与 7.3 节自引的 (f) 绑定矛盾、已在落地时改写（(b)-(f) 类型在别处确有绑定但五条边本轮零触碰；仅 (a) 类型从未绑定）；新文件为 29 行非 24（合并载荷 +441/−41）；台账编辑触发的双语注册表 smoke 预期标记由本次落地的 registry 刷新解决。落地树 maintained smoke 见下文。 |
| I42 ground-bridge 命令链守护扫描集适配 | `accepted` | `7c5c8ea1`，分支 `codex/redundancy-consolidation` | 统一架构计划 T6，收敛 I41 落地时登记于台账第 6 节的姊妹守护缺口（I34 归因：守护断言 vec_env/cooperative 直接从 `bridge.py` 导入 `build_kernel_mission_command`，而 I34 下沉已把该调用移入 `_shared_ops.py`；I39 修复只覆盖了 wp24 守护文件）。`test_batch_envs_use_tasking_bridge_for_command_chain_sync` 现把 `_shared_ops.py` 源文本拼到每个逐文件扫描上（`read_text() + "\n" + shared_ops_text`，与 I39 同款本地拼接、单次读取、`NOTE(I42)` 注释按下沉事实改述表框），原有九条 `assertIn`/`assertNotIn` 检查原样执行——五个正向 token 全部存在于下沉调用链、四个禁用 legacy token 全部缺席，负向循环现同样覆盖物理执行写调用的模块（严格增强）。树外负向自证：仅拦截 `_shared_ops.py` 读取、内存副本改名 `build_kernel_mission_command`，守护如期红、工作树文件字节未动。台账新增 6.2 小节记录修复与复核证据。载荷：3 个文件、新增 212 行、删除 1 行（守护测试 +23/−1 加双语台账小节）。 | GPT 评审第一轮：approve、一处非阻塞计数更正（已在落地时施加：台账在双语共四处把断言写成"八条"，实为九条——五正向加四负向，token 在场性表述亦已精确改写）。评审者逐断言核验拼接 diff（零放松；−1 行即被替换的 `text =` 赋值行），实跑 Python 验证最微妙的正确性点（四对 maintained/legacy 名均非子串关系、全部 False），以不同破坏形式独立复现负向演练（改名 maintained setter；守护红、干净回撤、文件哈希一致），在隔离基线上确认目标文件 `4 passed`（基线唯一红恰为该节点），复跑 mission 目录（`90 passed, 8 subtests`）与 smoke（`445 passed, 1 failed, 45 subtests`——唯一红即本台账对的注册表标记，落地方刷新惯例由 I39/I40/I41 三个落地提交各自携带注册表文件实证），并明确区分本例与 I39 blocking 的不同（本轮实现者如实申报编辑前后两组数字、无失实声明）。落地树 maintained smoke 见下文。 |
| I43 T4 第一切片：逐步 builder 双所有权普查与 exact-runtime 计划重冻结 | `accepted` | `089fa4ec`，分支 `codex/redundancy-consolidation` | 统一架构计划 T4 第一切片；纯文档（经评审驱动的措辞精化后，两份 exact-runtime 计划文档合计 +257/−11）。对 `python/**`/`gym_envs/**` 手写逐步 DTO 构造与已落地 C++ 路径的全仓普查发现：执行步进热路径为三层并存、各有真实生产触发开关或专属回归消费者——(1) `compute_full_step` 为真正的默认编排器，逐项消费一次聚合 `compute_execution_episode_runtime` 调用的子产品（真正的逐项编译调用存活于缓存未命中与回退通道——措辞已按评审在落地时精化）；(2) 选配的 `execution_step_batch_prepare` 批量层；(3) 选配的 `execution_episode_controller_mainline`/`shadow_compare` 切换层，默认关闭且功能不完整（mainline 下 post-launch 评估被显式禁用）。本提交点零 (A) 类退役——普查检验的两个退役假设均被具体证据推翻并如实记录。精确裁定一对更窄配对：`prepare_step_evaluation` 的 frame/episode `elif` 分发在生产不可达（两个使能谓词共享同一门控与同一无条件编译的绑定单元），但有专属 monkeypatch 测试练习且底层另有独立 C++ 契约消费者，故 held 而非退役。计划文档已重冻结（Status 行、WP1/WP2/WP4 落地事实注记、新增普查节含明确的退役触发条件：mainline 补齐 post-launch 评估与全部 flight-shaping 选项并转为默认）。 | GPT 评审第一轮：approve、零 blocker、两条非阻塞措辞项（层 1 表述已在落地时从"逐项编译调用为真实默认"精化为"逐项消费聚合编译产品"——默认路径实为一次聚合调用；评审者对每条普查声明逐一读码核验：三层的开关与默认值、`_air_combat_post_launch_mixin.py:51-52` 的 mainline 禁用、共享门控不可达论证含 `bindings_episode.cpp` 零条件编译、WP1/WP2/WP4 事实注记）与一条交接警示：评审收尾窗口该工作树被并发写入四个非 I43 代码文件（纯删除 −172 行）；评审者改以 HEAD blob 复核全部普查事实不受影响，落地只提取了两份文档文件。验证：聚焦回归 `491 passed, 1 skipped, 254 subtests, 5 failed`（四处 wp22 红加懒加载缺口、均为台账已知；ground-bridge 红自 I42 起已绿）；ctest 8/8；maintained smoke `446 passed, 45 subtests` 不变（exact-runtime 计划配对不在强制双语 registry 范围，已对照 `STRICT_PLAN_SUBTREES` 核实）；ruff 与 `git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I44 T5 第二切片：opt-in 报告信封与第二批 argparse 收编 | `accepted` | `8bd21d86`，分支 `codex/redundancy-consolidation` | 统一架构计划 T5，在 I30 的实验面 owner 与 I20 的共享参数组先例上完成该轨道剩余两项交付。argparse 批次：对 `tools/**` 与运维入口全仓普查后落地三组——(1) I20 三件套（`add_probe_run_args`/`add_model_load_args`/`add_json_out_arg`）扩展到九个此前延后的部分匹配调用点、含 `evaluate.py`（其 `--algo` 枚举经新增的向后兼容 `choices=` 透传）；(2) 新建 `add_kces_before_report_args` 组覆盖四个 KCES before-report 消费者；(3) 在 `manifest_integrity.py` 新建 `add_retained_gate_output_args` 组覆盖三个独立评审 retained-gate CLI。五个已普查家族缓议并各附理由（约 26-30 文件的证据包家族、kill-chain 字段组合不齐、`--collector_action` 实质分歧、`--fire_range_m` 时间预算、`--repo_root` 涉 freshness 生成器）。首次对双别名 dest 使用 `required=True` 时发现并修复 I20 潜伏缺陷：`add_dual_option` 在同一 dest 上注册两个各自 required 的 action、致 required-dual 事实不可用（单传任一写法均被拒）；现升级为 required 互斥组——任一写法满足必填、`--help` 逐字节不变，docstring 记录了两处语义变化（都不传时的错误文本；同时传两种写法现报互斥错误、而旧的不可达路径会让后值生效——该句按评审于落地时补记）。报告信封：新建 `python/experiment/report_envelope.py`（纯标准库、零 bootstrap 副作用），在原样 payload 外包 `envelope_schema_version`/`tool_id`/`generated_at`/`git_rev`（best-effort）/`experiment_ref`（不透明、刻意不对 `ExperimentRegistry` 解析——只做词汇、不越入 T10 证据面）；`--report-envelope` 默认关闭且关闭路径返回同一对象，由 `naval_station_policy_eval.py` 与 `calibration_admission_audit.py` 接入示范（后者补两行 I16 惯例 sys.path 兜底）。载荷：49 个文件、新增 2,331 行、删除 160 行（20 修改 +299/−160、7 个新增实质文件 1,197 非空行、22 份 `--help` 逐字节基线）。新测试刻意不晋升进受管控 smoke 清单，符合该清单显式白名单意图与 I30 先例。 | GPT 评审第一轮：approve、零 blocker、两条 P3 措辞项（均已落地施加：上述 docstring 补记；本登记将独立评审子集数字标注为实现者口径——评审者无法重构其精确选择器、但成员测试逐一验证全绿）与两项台账补录（均已落地施加：`platform_spawn` 的 spdlog 采集错误与顺序敏感的 `source_evidence_governance` 既有红，二者均在写集无从致因的前提下复现）。评审者在修复前 helper 上回放 I20 缺陷（required-dual 事实不可用、故零兼容面损失），实测验证 required 互斥组的 `--help` 逐字节不变，从 HEAD 源码重建 `evaluate.py` 迁移前 parser 并逐字节比对 `format_help()`（行为为严格超集：新增 `--train-config` 别名），抽查组内 help 文本覆盖（date-stamp 示例、stdout 措辞），独立复跑信封负向测试与裸导入探针（`sys.modules` 无 `ef_py`/`gymnasium`/`stable_baselines3`/`torch`），认同 smoke 不晋升裁量，并复算全部数字（112 新测试=16+23+33+6+26+8；`tests/tools`+`tests/experiment` 322 passed/4 skipped；`tests/eval` 14/2/18；smoke `446 passed, 45 subtests` 不变；ruff 与 `git diff --check` 干净）。实现者口径、评审未独立重构的子集：独立评审/retained-write 选集 83 passed/4 skipped。落地树 maintained smoke 见下文。 |
| I48 T10 第一切片：证据与重放脊柱普查 | `accepted` | `684a5b2d`，分支 `codex/redundancy-consolidation` | 统一架构计划 T10 开局；纯文档，两份新双语 reference（`docs/plan/unified_architecture_program/t10_evidence_spine_census_20260721.md`/`.zh.md`，EN 207 + ZH 177 = 384 行）。对本基线五类证据面——trace ids、packet ancestry（`InformationStateSource`）、snapshot versions、replay gates、worldline/counterfactual——逐面登记词汇、生产者、消费者、测试钉扎与"按构造可重放"差距，另含八条叠加式词汇对齐清单（VA-1..VA-8）、带依赖标注的七步后续切片顺序与 additive-only 红线（既有证据字段被成员序 ABI、JSON 别名、保留产物哈希与测试钉扎；全部对齐以新字段/新生产者落地）。经核实的关键发现：kernel 的 trace 分配器仅在单个 event-store epoch 内单调（`clear()`/时钟回退即重置），维护 adapter 以占位 `trace_ids=[1]` 打标、导出时 `parent_trace_id` 硬编码为零；packet `snapshot_version` 是按导出 `index+1` 的序号（每次导出重置；counterfactual snapshot 固定常量 1——不存在 run 全局单调计数，故已插入单调生产者前置切片）；`InformationStateSource` 以六种字段名旅行、谱系词汇被实现两遍（C++ 契约类型加 Python `generation_request.py`）；裸 counterfactual facade 拥有两个合成 `ReplayEnvelope` 生产者而维护 adapter/live-run 路径没有，且它自铸多类 id（`worldline:runtime:*`、`worldline:baseline`/`branch`、`comparison_id`、snapshot 派生 `replay:facade:*`），仅部分请求字段由调用方提供；恢复分三层（WP15 `WorldlineBranchMetadata` 仅元数据；`RuntimeFacade` 写回 host-owned kinematics；resident/GPU/full-clone 拒绝）。未添加可选只读测试（各面已有六个入 smoke 的钉扎；钉住占位值恰会固化 T10 要替换的东西）。 | GPT 评审第一轮：needs-repair、四项 P1 事实错误（普查曾把按导出重置的序号称为真实单调版本、把 `ReplayEnvelope` 称为仅测试 fixture 生产、把全部 worldline id 称为调用方手写、把 restore 称为仅元数据）另加多处修订（trace epoch 限定、provenance 计数五改六、VA-2/5/6/8 精化、切片顺序倒置、钉扎口径过强、行数更正）。修复轮在基线上逐指针亲自读码核实后改述；修复复核（另开新评审）随后逐项对源码解除（`runtime_facade_packet.cpp` 重置点、两个合成生产者构造点及其调用点、id 自铸行、kinematics 写回与拒绝分支、分配器 epoch 重置），复现 smoke `445 passed, 1 failed, 45 subtests`（唯一红即本文档对的预期双语注册表标记，由本次落地的 registry 刷新解决），复跑链接审计（164 文档、2,652 链接、零问题），独立复算两个规范化注册表哈希完全一致，最终 approve、零 blocking、零 non-blocking。落地树 maintained smoke 见下文。 |
| I45 T8 第一切片：G4 信息状态层声明与真值泄漏清单 | `accepted` | `604a01a1`，分支 `codex/redundancy-consolidation` | 统一架构计划 T8 第一切片；零运行时行为变更（声明为纯模块级元数据加静态门禁）。新增仅依赖标准库的设施 `python/architecture/information_layer.py`（依赖方向 `gym_envs -> python.architecture <- python.rl`），逐字发布 I32 阶段契约白名单的六层/P0-P10 词汇、含八个已声明消费者的 G5 注册表与共享校验器；八个维护面观测/奖励模块挂 `INFORMATION_LAYER_CONSUMED`/`INFORMATION_LAYER_PRODUCED`/`SEMANTIC_STAGE` 常量（mission_observation、air_combat、naval、safety、shaping_inputs、objectives、仍活跃的 `universal_env_parts/observations.py` 组装器、`navigation_runtime/waypoint_rewards.py`）。门禁 `tests/architecture/information_state/test_g4_layer_declarations.py`（14 测试）校验存在性与权威词汇并对 I32 双向交叉钉扎，白名单与 `core.py` 均以 AST 读取故无需 `ef_py`/构建即可运行，仅接受元组形声明（列表形钉红），并证明可承载。双语 reference `t8_g4_truth_leak_inventory.md`/`.zh.md` 登记 C1-C19 消费者普查与 TL1-TL20 泄漏清单（15 泄漏/4 豁免/1 诊断；本切片已声明 11 条、带理由推迟 4 条——含产物同时馈入奖励输入与自动驾驶目标的共享航路引导辅助）。载荷：13 个文件、+803（8 个 tracked 模块 +98；5 个新文件 705 行）。 | GPT 评审第一轮：needs-repair、两项 P1 普查缺口——首轮 C16 把仍活跃的 `build_universal_observation`（`cooperative_world_batch_vec_env.py:627` 与 `multi_agent_runtime.py:190` 调用、读 `truth.x/y/contacts/rwr_warnings`）误判死路径，且漏掉直接航点奖励输入消费者 `build_waypoint_step_state`——另四项非阻塞（列表形声明可滑过 AST 提取器；门禁 import `world_batch` 包连带加载 `ef_py`、与其自述矛盾；两处计数错误）。修复轮把 C16 拆分为死构造器与活跃组装器（现 C17/TL18 挂声明）、声明航点消费者（C18/TL19）、把 `guidance.py` 裁为待裁定（C19/TL20）、提取器收紧为仅元组并加钉死测试、门禁改以 AST 读 `core.py` 并附屏蔽 `ef_py` 自证、更正两处计数。修复复核（另开新评审）：approve、零发现——声明对实际读取逐一验证，元组/列表探针与无 `ef_py` 运行独立复现（`14 passed` 且 `sys.modules` 无 `ef_py`），普查复扫无第三个漏网消费者，全部计数复算，相邻套件 `110 passed, 1 skipped, 5 subtests`，smoke `445 passed, 1 failed, 45 subtests`（唯一红即本配对的预期注册表标记、由本次落地刷新解决），五处 policy_execution/runtime_facade 红复确认为台账既有 wp12/wp22 节点。落地树 maintained smoke 见下文。 |
| I46 T3 第三切片：`ef_core` 物理 link 单元拆分第一单元（`ef_content`） | `accepted` | `595a7bba`，分支 `codex/redundancy-consolidation` | 统一架构计划 T3 第三切片，以最干净的叶子层开启物理拆分。`content/` 共 18 条工程 include 出边（16 条到 header-only `components/**`、2 条 content 内部、零上层；`core/interfaces/` 仅现于其入边侧），唯一编译单元 `unit_definition_loader.cpp`（约 1.9k 行）无动态初始化/自注册/constructor 属性——唯一文件级状态是匿名命名空间两个 `constexpr const char*` 常量；公有符号 `load_unit_definitions_json` 由 `simulation_kernel.cpp` 经 `default_unit_factory.h` 的显式未定义引用拉入。该层现为独立 `ef_content` STATIC 静态库、由 `ef_core` PUBLIC 单向链接，消费者零改动（复用同一 `src/` 公有 include 目录），编译环境逐字节一致（以该 TU 的实际编译命令对照拆分前 CMake 的临时重 configure 验证）。链接序在全部下游链接行核验（`libef_core.a` 在 `libef_content.a` 前，MinGW 单遍安全；归档成员确认无双编译）。smoke 成员文件 `test_cmake_target_readiness.py` 内的 CMake 文本隔离门禁钉住拆分：content 源不得进 `ef_core`（变量或字面路径形态、带 `./`/`${CMAKE_*_SOURCE_DIR}` 归一化的路径段匹配）、`ef_content` 全部 `target_link_libraries` 调用任意 scope 无回边、其源集（add_library 加全部后续 `target_sources`）保持 `${EF_CONTENT_SOURCES}` 组、前向边必须存在——解析器剥注释、token 化、`${VAR}` 传递展开，并对双引号串与 CMake bracket 参数联合掩码（引号或 bracket 内的伪命令既不误报也不满足断言）；十一个负向用例钉住修复各轮封堵的盲区。新 CMake 块内一行注释按实测出边事实修正。载荷：2 个文件、+678（CMakeLists.txt +36；门禁文件 +642 含 13 个新测试，五个既有测试逐字节未动）。 | 四轮评审。第一轮（本体达 approve 质量、一项 blocking）：拆分本体端到端验证——普查、符号链、编译环境等价、归档成员、10/10 链接行序、ctest 8/8、跨构建 parity、聚焦回归仅台账红、smoke——但原固定排版文本门禁存在可复现漏报/误报；同轮裁定 smoke +1 可接受（向既有 smoke 成员文件加断言、有别于晋升新文件）。第二至四轮迭代加固门禁、每轮由新开评审以对抗性变体独立复核：二轮封堵字面路径混入/第二条回边调用/注释与换行敏感（before/after 探针逐一实证 legacy 盲区）；三轮封堵字符串内伪命令/等价路径前缀/`target_sources` 聚合；四轮封堵 CMake bracket 参数伪命令（`cmake -P` 实证合法性）并在代码内注记单引号非边界。终态零发现通过：门禁文件 18 passed、对抗变体（`[==[` 内嵌 `]]`、未闭合 bracket、bracket 内引号、字符串内 `[[`）全部正确，源树 smoke `459 passed, 45 subtests`，ruff 与 `git diff --check` 干净，漂移隔离 stash 未动。一项如实披露的环境副作用：二轮修复者为跑 smoke 向全局 Python 安装 gymnasium/pytest-subtests（不影响仓库，已核验）。落地树 maintained smoke 见下文。 |
| I47 T9 第一切片：权限模型普查、注册式 Agency 词汇与 ratchet 门禁 | `accepted` | `9a054c0a`，分支 `codex/redundancy-consolidation` | 统一架构计划 T9 第一切片，零 C2 行为改动（五个全新增文件、2,433 行；零既有文件触碰）。维护 tasking 权限面普查为 14 个门禁散点（A1-A15 减去一个按统一口径排除的再导出）加两个 runtime 面交叉引用与范围外的策略网络面，按 role/scope/delegation/arbitration/doctrine 与评审中引入的 `gating` 维度归类（激活门与开火资格门裁定使能/禁用、非 producer 竞争）。`python/tasking_contracts/agency_registry.py` 为单一声明式词汇 owner（纯标准库、冻结、不导入 ef_py/python.rl/gym_envs、零生产消费者）：SCAL 五元 `AgentRole` schema（九角色全部携带完整字段）、编译权威镜像（`AuthorityScope`、`CommandRelationship`、`CoordinationMode`、全部六项 `NavalWarfareRole`、含 `mission_command` 的三个动作接口 scope）、委派载体、含有据可查的 `human > policy > scripted > diagnostic` 来源优先序（架构设计文档约 450 行）的合并策略/仲裁词汇、开火资格门与 `DoctrineFamily` 词汇占位。ratchet 门禁（`test_authority_registry_gate.py`，21 测试，加指纹 fixture）以词边界匹配钉住逐文件 token 计数、整目录扫描且经 AST/tokenize 剥离注释/docstring/再导出壳（同时仍捕获 prose 民俗）、经免注释欺骗的提取器（C++ 反斜杠-换行拼接先于注释识别、引号感知剥离）对照编译头文件、并对评审重裁站点钉死精确类别集。调用点收敛到词汇留待带领域证据评审的后续 T9 切片。 | 四轮评审、每轮均为新开独立评审。第一轮：needs-repair——镜像缺项（缺 `mission_command`、海军角色未声明、角色缺 schema 字段、无权威对照门禁）、探测词扭曲归类、四类可复现 ratchet 盲区。第二轮修复后，二轮复核确认前项但发现提取器可被注释欺骗、同义词盲区仅登记未修、A14 的 arbitration 归类无源码依据。第三轮修复（状态机注释剥离器、词边界同义词 token 与指纹重生、A14 依精确读码改归 `gating`——开火掩码是 `mission_observation.py:281-300` 的 holder 盲资格合取，A13 因 holder-id 选择保持 arbitration——加重翻即破的精确类别钉死）；三轮复核全部解除但抓到行注释续行缺口，四轮修复以"反斜杠-换行拼接先于注释识别"的编译器顺序语义封堵（三个真实头文件零拼接、提取字节级不变），对抗变体（尾随空格非拼接、转义反斜杠、block 注释拼接）逐一验证与编译器一致。四轮复核：P1 解除、变体全对、agency 21 passed、中立层 20 passed、smoke `445 passed, 1 failed, 45 subtests`（唯一红即本普查配对的预期注册表标记、由本次落地刷新解决），一处 P3（census JSON 残留被取代的 406-407 投影行号）按复核者精确处方在落地时施加并复跑门禁绿。落地树 maintained smoke 见下文。 |
| I49 T11 第一切片：内容流水线普查 | `accepted` | `1d25c4d1`，分支 `codex/redundancy-consolidation` | 统一架构计划 T11 开局；纯文档，两份新双语 reference（`docs/plan/unified_architecture_program/t11_content_pipeline_census_20260721.md`/`.zh.md`，EN 251 + ZH 195 = 446 行）。对照分阶段 `P0 ContentCompile` 模型的内容加载四面普查：(i) 单位定义加载器——1,881 行、唯一手写映射 `parse_unit_json` 782 行、430 处 `.value`、语义顶层键面 106（直接访问 54 加经接收整 entry 的导弹调参 helper 读取的 52）、五族 codec escape hatch 逐行钉定（fuze/fuse 双拼、引擎扁平/嵌套双路径、四种 sensor 变体、导弹调参三源合并、依赖字符串/对象多态），产出 `UnitDefinition`（58 直接成员）与 `MissileTuningDefinition`（56 字段），由 `DefaultUnitFactory::spawn`（619-1439、821 行、能力门禁后手工物化）消费，磁盘面为 27 个定义文件（11 单位平台、12 模块、3 武器、1 设施；29 个 JSON 减两个仅按直接父目录比较排除的证据描述符）；(ii) 场景链——已是最接近 P0 的分阶段流水线（parse、validate、merge/resolve、ingest、复校验、类型化 `CompiledScenario`、经 `spawn_unit` 物化），与面 (i) 仅经 `type_name` 字符串相遇（差距 G-A）；(iii) T5 实验矩阵，裁定在 T11 范围外；(iv) 校验/默认/版本化——单位解析仅结构性且存在未捕获的 `std::stoi`/`get<std::string>` 抛出路径（外层仅捕文件系统错误）、场景侧 `TypeError`/`ValueError` 分裂、无顶层内容 schema 版本。差距矩阵 G-A..G-E（核心为 G-B：手写 parse 供给已 schema 化的输出契约）、七步后续切片顺序、红线（内容 JSON 冻结、ABI、escape hatch 保留、以及点名 `default_loadout`（`std::unordered_map<int, std::string>`）须在表驱动化前按 I31 尖括号逗号先例显式裁定）。未做可选收敛。 | GPT 评审第一轮：needs-repair——评审确认普查大部（行数、映射范围、`.value` 计数、escape hatch、G-A 单通道、G-B 不对称、P0 子阶段"分析透镜"标注），但发现四项会误导后续切片的 P1 事实（加载面漏 16 个模块/武器/设施文件；键数漏 helper 的 52；两个 DTO 成员数皆错；红线漏 I31 逗号阻塞）加三项 P2（错误形态不全、spawn 范围差闭合括号、排除措辞过强）。修复轮逐项读码核实后修正全部七项（27 文件按类枚举并以 `f16c_block50.json` 交叉引用实证；两键集逐元素比对不相交、`aim_120c.json` 顶层实存 `max_flight_time_s`；成员机械重数；引 I31 台账先例）。修复复核（另开新评审）独立复算全部数字（29−2=27 含精确模块分布、54+52 零交集、58/56、619-1439/821），对照 I31 held 先例核验 `default_loadout` 类型，复现 smoke `458 passed, 1 failed, 45 subtests`（唯一红即本配对的预期注册表标记、由本次落地刷新解决），独立复算两个规范化注册表哈希完全一致，最终 approve、零发现。落地树 maintained smoke 见下文。 |
| I50 T8 第二切片：声明观察视图收敛 | `accepted` | `26e8b2b6`，分支 `codex/redundancy-consolidation` | 统一架构计划 T8 第二切片，即 I45 清单 §5 点名的收敛动作。新建 stdlib-only 视图 owner `gym_envs/observation_view.py`（221 行、16 个带层标签的读取面：own-ship 字段/属性读取、C1 变体与 naval 变体的目标航迹查找、own 导弹余量、support 单元共享战术图读取、交战证据、单位存活/血量、contacts、RWR 告警、以及显式诊断的 `debug_*` 面），按 G2 下沉至 `gym_envs` 父包公共层——`scenario_loader` 与 `universal_env_parts` 均向下消费，后者对前者保持零横向导入。八个已声明消费者（C1、C4-C8、C17、C18）迁移到经视图读取，结构性收敛 11 处已声明真值泄漏（双语清单中 TL1/2/4/5/7/8/10/11/12/18/19 翻转为"经声明视图收敛"；§4 分布现为 11 收敛 / 4 延期泄漏 / 4 豁免 / 1 诊断；新增 §6 切片记录含 §6.5 修复记录）。`python/architecture/information_layer.py` 新增视图 owner 与已收敛消费者注册元组；新增 AST 真值直读禁令门（`tests/architecture/information_state/test_g4_truth_read_ban.py`，196 行、12 个测试）：对已迁移消费者禁止裸 `truth.<attr>`/`getattr(truth, ...)` 读取，配内存注入的 load-bearing 负测试、行级 `g4-diagnostic-truth-read` 白名单、视图 owner 排除证明与诚实的检测边界注记。C17 的编译内核整体透传与延期的 C11-C14/C19 路径按设计不动。13 文件，+837/-200。 | GPT 评审第一轮：needs-repair——一项实证 P1 行为漂移（共享目标航迹面把 C5 naval 原守卫 `target_id <= 0` 悄然规范化为 `int(target_id) <= 0`；评审隔离探针显示 `target_id=0.5` 配 id-0 航迹时基线 `0` vs 改动 `None`，字符串 id 时基线 `TypeError` vs 可匹配）加一项 P2 分层违例（C17 迁移引入 `universal_env_parts -> scenario_loader` 首次横向导入，违反 G2；视图 owner 原置于 `scenario_loader` 内）。修复轮：新增独立 `naval_target_track` 面逐 token 复刻 naval 原实现（C5 委派它；C1 保留自身变体——两个基线守卫确实不同），视图 owner 下沉 `gym_envs/observation_view.py`，导入、注册表路径、门文案、双语文档引用全部随动且旧路径零残留。修复复核（另开新评审）零发现通过：枚举 16 个视图面且无未引用面、4 个复刻面与基线原文 AST 全等、117 个消费者函数 AST 核对零偏差、以 `git show` 基线原文实证 C1/C5 守卫分列并在两个漂移场景下三路（基线/视图/消费者）探针一致、双树行为对照完全一致（目标选集 60 passed/15 subtests/4 failed——四个既有 NumPy `asarray(copy=)` 机器红；广集 255 passed/8 subtests/4 个缺 `stable_baselines3` 收集错误，可运行节点集合 SHA256 两树相等）、信息态门 26 passed 且负测试验证 load-bearing、smoke 458 passed/45 subtests/1 failed 且唯一红为预期双语哈希落地标记（由本次落地的注册表刷新解决）。落地树 maintained smoke 见下文。 |
| I52 T11 第二切片：内容 escape-hatch schema 普查与草案 | `accepted` | `1610c9bf`，分支 `codex/redundancy-consolidation` | 统一架构计划 T11 第二切片，按 I49 普查后续切片顺序第 2 步：文档 + schema 草案、零 parse 变更。三个新文件：双语普查（`docs/plan/unified_architecture_program/t11_content_schema_survey_20260721.md`/`.zh.md`，规范化 407/338 行）与刻意未接线的机器可读草案（`tools/maintenance/dto_schema/drafts/content_unit_schema_draft.json`，2,162 行），草案镜像 T1 `Field` 模型并做内容扩展（`forms_aliases`、`polymorphic`、哨兵图例）。普查逐键规格化全部 106 个语义顶层键（54 直接 + 52 经导弹调参 helper，零交集重证）：调用行约定下的读取点（多行语句取实际 `.value(...)`/`parse_vector(...)` 调用行，约定已成文于 §0）、目标 DTO 成员及行号、27 个定义文件的实测分布（[U11/M12/W3/F1]）、含专设 PRESET 类别的哨兵语义（sensor 30 km、sonar 25 km、engine/aero tuning 默认、landing_gear 字面量预设、flight_model 块内回退、embarked_air_ops 块默认）、以及补齐嵌套双形态后的 escape-hatch 族归属（fuze 半径三别名加 trigger_logic|trigger_mode、failure_modes 数组|对象加权重覆盖、damage 几何嵌套/扁平双路径）。另登记 7 个"存在但未读"顶层键（4 个下划线注释加 `ew_suite_ref`/`rcs_profile_ref`/`rcs`——loader 从不赋值、factory 侧解析空引用；裁定移交下一切片；106 总数按定义不变），并给出 X-macro 逗号阻塞全表：58+56 个 DTO 成员中恰一个（`UnitDefinition::default_loadout`，`std::unordered_map<int, std::string>`，`unit_definition.h:190`），建议 HELD、依 I31 先例——这正是切片 4 表驱动化所需的显式前置裁定输入。 | GPT 评审第一轮：needs-repair——骨架成立（键集、分布、未读键、唯一逗号阻塞、草案未接线均独立复算）但发现五组 P1：helper 前五键读取行号错一行、sensor 四形态误画成单一互斥链（`sensor_ref` 是独立 `if` 可与后续三路链叠加，DDG-51 文件双键并存实证）、`name` 误标必填（实际回退 `type_str`）、四个 codec preset 默认误归值初始化 `{}`、嵌套双形态 escape hatch 漏录。修复轮一修复全部五组并在全表复核中主动补上四处同类事实（landing_gear 预设、逐挂载 sensor/sonar 预设、字面量预设图例注记、flight_model 块内回退）。复核（同评审独立重算）确认上述修复但又抓出九个仍引多行赋值起始行的 helper 行号；修复轮二按新成文的调用行约定修正九处并自查另修 guidance 别名区段六处同型引用。最终确认复核按"字面量必须在被引行本行"的严格判据重跑全表提取：106 键三方（EN/ZH/JSON）一致、全部 310 处行号引用（198 读取点 + 106 目标声明 + 6 guidance 别名）零偏差、双语计数相等、generate --check 全部最新且生成树干净（草案未接线的结构性证据）、链接审计干净、smoke 458 passed/1 failed/45 subtests 且唯一红为预期的未登记配对标记（由本次落地解决）；approve。落地树 maintained smoke 见下文。 |
| I51 T10 第二切片：加法性证据词汇表 | `accepted` | `fae17eb8`，分支 `codex/redundancy-consolidation` | 统一架构计划 T10 第二切片，按 I48 普查后续切片顺序第 2 步：纯文档、零代码零字段变更。两份新双语 reference（`docs/plan/unified_architecture_program/t10_evidence_glossary_20260721.md`/`.zh.md`，370/313 行），构成五个证据面上的权威 id/版本词汇表：共 **69** 个字段（uint64 身份 13、string 身份 27、无类型 ancestry `*_ref` 9、状态切片版本 12、格式/schema 版本 8），每字段给出声明指针、生产者普查（谁赋值、何时重置、声明默认 vs 生产路径实际值、可由调用方构造 vs facade/kernel 生产）、消费者/测试钉扎与 VA-1..VA-8 映射。关键记录事实：交战事件 store 的单一可重置计数器为其记录的**每一个** kernel 事件族铸造 id（11 族 + effects 四元组共 12 个铸造点，lethality-chain id 落 `LethalityChainHeader.event_id`；被 `clear()` 与时钟回卷重置）；导出 barrier 实写值与声明默认不同（`kExportBarrierSequence = 1` vs 默认 0）；数字文本化嵌入字符串的跨越点按非穷举方式编目（worldline id、restore 快照、world-batch adapter 的 `obs:{world}:{entity}` 组装、packet-provenance 版本嵌入）；vulnerability schema 版本按真实字段名拆两行（EffectsResult/EffectsEvent 的 `vulnerability_evidence_schema_version` vs 上游 `evidence_schema_version`，附 descriptor→profile→adjustment→result 完整复制链）。可选的 dto_schema 元数据子任务经评估放弃并给出范围化理由（无语义合适的既有通道；专用属性属 model/生成器代码变更、超出本切片零代码红线）。核查中发现的 census 两处小误（ancestry 字段名前缀；`window_commit` 属 `barrier_detail`）记录于 §5，census 本体不动。 | GPT 评审第一轮：needs-repair——四项 P1（4 行注释头 `.inc` 误计导致声明行号系统性错位；"所有 uint64 身份共用 engagement 分配器"过宽；`barrier_sequence` 的 DTO 默认与导出实写混淆；多字段错标"仅调用方生产"漏 raw facade 生产者）加两项 P2（漏 7 个 census 明示字段；"无仅文档通道"论证过强）与一项 CRLF P3（裁定沿目录先例不改）。修复轮一：脚本整表重生成指针、收窄分配器结论、全表区分声明/实际值、补 facade 生产者、补 7 行（62→68）、改写 §6。修复复核（另开新评审）验证 68/68 行与 10 项点名事实，但扩展搜索又出三项 P1（分配器集合仍漏 9 个铸造族；"仅两种文本跨越"仍绝对；vulnerability schema 版本字段名错误）加中文页链英文架构文档一项 P2。修复轮二：列全 12 个铸造点并附所属函数证明、跨越改非穷举并补 adapter 与 provenance 嵌入、vulnerability 按真实字段名拆两行附完整复制链（68→69）、修两处中文链接。确认复核对照全文件计数普查核验 12 铸造点（15 处自增 + 1 处重置、无第 13 点；training-projection 族经证无 store 记录路径）、重跑全表复算（69/69 行、每语言 182 个可解析指针、集合相等）、在 I50 落地后的 HEAD 上重跑 smoke（458 passed/1 failed/45 subtests，唯一红为预期的未登记配对标记、由本次落地解决），最终 approve。落地树 maintained smoke 见下文。 |
| I53 T9 第二切片：机械权限词汇收敛 | `accepted` | `1a2e64a5`，分支 `codex/redundancy-consolidation` | 统一架构计划 T9 第二切片：I47 普查 §7 所推迟的调用点收敛中零语义风险的机械子集。对全部十四个已普查散布站点逐一裁定（新普查 §3.2 矩阵，EN/ZH）：A1-A14 狭义机械子集为**空**——每个站点要么是编译端 `ef_py` 枚举访问（registry 是该编译真源的镜像）、要么是 schema 层 DTO 字段名键（归 T1 不归 T9）、要么就是权限控制流/prose 民俗本身（推迟的语义材料）——附一次性等值证明（24/24：registry 镜像与实时编译枚举集合相等；每个站点字面量与其登记词汇逐字节相同，现按四分列呈现：枚举镜像 / 委派载体 / ROE 模式字段 / 角色 authors 字段）。评审自己的维护面猎找随后在 A 清单**之外**找到唯一真机械站点——`python/rl/runtime/agent_shim.py` 本地复刻 registry 的五项合并策略集合——修复轮完成收敛：`ALLOWED_MERGE_POLICIES` 现在**就是** registry 的 `MERGE_POLICIES` 元组（同一性非拷贝），五个 `MERGE_*` 命名常量保留字面量以维持可 grep 的调用点词汇，新增防漂移单测钉死同一性、元组型、顺序与每名字面值（smoke 计数 459→460，因该测试文件本就在 smoke 清单内）。ratchet fixture 与门经证明无需变动：agent_shim 在普查扫描根之外、合并策略字符串非探测词。A6 行沿途更正两次："leader precedence"（naval 无 `leader_intent`）改为"配置压过运行时"，复核再次证伪（loading.py 装载期把 `scenario_data["mission_command"]` 与 `loader.mission_cmd` 绑定为**同一 dict**、finalize 再同步），最终定稿为——权限/ROE 字段重读是同一映射上的幂等伪优先级，运行期重绑（如 multi-agent 的 clone-restore 路径）是否形成真实分离显式待裁定并写入前置条件列。矩阵结论三处统一：狭义 0/14 + 猎找收敛 1 处；语义收敛按站点前置条件继续推迟。4 文件，+262/-16。 | GPT 评审第一轮：needs-repair——猎找发现被漏判的 agent_shim 机械站点（推翻"子集为空"）、等值证明措辞对两个角色 authors 字段过度声称载体/条令覆盖、A6 所述仲裁来源与源码不符。修复轮一实施收敛（方案 B 加防漂移单测）、修正证明分列、改写 A6——新的"配置压过运行时"框架又被新开的复核评审对照 loading.py 同一 dict 绑定证伪（唯一剩余 P2）。修复轮二落定"幂等伪优先级"最终措辞并把运行期重绑问题停放为前置条件。确认复核逐行核验同一 dict 事实（loading.py:113-117/:242、naval_profile 重读回退）、确认 A4/A5 的真实 leader-intent 优先级原样保留、对照基线复验同一性/行为等值，最终 approve 并附一条 P3（fixture 的 A6 `semantic` 注释文本滞后于更正措辞；纯描述性、门不敏感——欠账至下次 fixture 刷新，在此登记以免遗失）。落地树 maintained smoke 见下文。 |
| I55 T11 第三切片：现有 API 后的分阶段单位编译 | `accepted` | `60e1212b`，分支 `codex/redundancy-consolidation` | 统一架构计划 T11 第三切片，按 I49 普查后续切片顺序第 3 步。单位定义加载路径重组为声明式 `content_compile` pass——`parse_pass`（历史加载遍历，逐字搬迁）→ `validate_pass`（新增，零行为）→ `resolve_pass`（显式 pass-through 声明）——`load_unit_definitions_json` 降为薄编排层，`spawn()` 物化不动。手写映射逐字节保留：`parse_unit_json` 与全部 16 个 helper 函数体零 diff；`load_file` 仅加可选原始条目捕获 sink（成功路径两处 push_back，错误路径不动）；搬迁后的遍历体仅有三类声明的归一化差异（局部量名、sink 实参、提交位置）；事务性提交语义（临时 vector、任何失败类别都不部分提交）不变。新 `validate_pass` 依 109 键硬编码白名单（I52 的 106 读取键 + 3 个 present-but-unread 引用键 + 下划线注释约定）产出结构化未知顶层键诊断，默认路径调用后丢弃——不抛、不拒、不改、不打日志；I52 草案 JSON 保持未接线（构建/运行时零引用）。`resolve_pass` 声明现状——引用解析仍延迟到 factory（7 个消费点注明），pass 只读计数延迟引用，`ew_suite_ref`/`rcs_profile_ref` 死边如实记录。新文件进 CMake 的 `ef_content`/`ef_test`；新增 7 用例/49 断言 C++ 套件覆盖白名单正反例、诊断字段、非对象输入、resolve 不变性与未知键不改变加载结果。5 文件，+516/-7。 | GPT 评审：approve、零发现。评审独立重证纯搬迁（16/16 helper 与 `parse_unit_json` 函数体提取比对全等；遍历体仅三类声明归一化后相等；`load_file` 移除恰两个捕获块后相等；对 malformed JSON/描述符/文件系统三类失败验证事务语义未漂移），独立复算白名单恰 109/109（与 I52 普查及草案零缺零多；27 个定义文件的全部 46 个实际顶层键全部识别），并运行独立双构建 parity：基线（EF-w2）与改动（EF-w4）两个 `ef_py` 各加载全部 27 定义并生成完整数据库，快照 SHA-256 完全一致（`c7720934...`），另以合成 fixture 跨两构建重证导弹三源合并、fuze/fuse 后写优先、sensor preset 与挂载质量。门：ctest 8/8、C++ 120 用例/18,802 断言、CMake readiness 18 passed、include 方向门 7 passed、smoke 459 passed/45 subtests、ruff 与 diff-check 干净；4 例 naval 失败为既有 NumPy 1.26 `asarray(copy=)` 机器红、基线原样复现。落地树 maintained smoke 见下文。 |
| I56 T8 第三切片：延期消费者裁定与声明 | `accepted` | `6c1eef8c`，分支 `codex/redundancy-consolidation` | 统一架构计划 T8 第三切片：五个延期的聚合器/leader/引导消费者（C11-C14、C19；TL14-TL16、TL20）逐一完成认知层裁定与 G4 声明。裁定矩阵：C11 `step_evaluation.py` 与 C12 `execution_runtime/mainline.py` 为阶段捆绑编排器——声明（消费 World Truth，阶段按 I32 契约定 `P10 ObservationExport`）不迁移；C14 `leader_tasking.py` 裁定为**维护式条令**（非仅诊断），阶段 P2 TaskingIntent / P3 CommandDelivery——经中立层声明合法、迁移仍禁止（会引入 `python.rl -> gym_envs` 反向边）；C19 `guidance.py` 跨指令下发与奖励支撑（P3/P4/P10），等待指令/引导读取 owner；C13 leader `build_observation` 在修复轮**完成迁移**至 `gym_envs.observation_view`（8 处 `getattr(truth, "x"/"y", 0.0)` 经 `own_ship_field` 逐 token 搬迁，整体真值透传不动），配新 parity harness `tests/leader/test_leader_observation_view_parity.py`——pin 字面量由 fae17eb8 基线函数在合成场景（含无 x/y 默认值触发场景）实跑产出，另有视图缝损坏红证。注册设施拆分为 `VIEW_CONVERGED...`（9，禁令门扫描）与 `DECLARED_DEFERRED...`（4，仅声明）两分区、维护并集 13；声明门参数化至 13 并新增分区测试钉不相交/并集/非空；禁令门新增可承载的"延期消费者保持排除且仍实际持有裸读"参数化。清单 TL15 翻转 converged（分布 12 收敛 / 3 声明待收敛 / 4 豁免 / 1 诊断），§5 按消费者重写前置条件，新增 §7 含 §7.5 修复记录。11 文件，+660/-82。 | GPT 评审第一轮：needs-repair——C13 的延期依据不实（无任何 leader 数值测试覆盖 `build_observation`；模块无 stable_baselines3 也可干净导入，所称 SB3 阻断不存在），且 C11/C12 的 `P9` 阶段声明与 I32 契约不闭合（core.py 全文零 P9 声明；观测/奖励组装闭合于 P10）。修复轮走评审证明可行的迁移路线（方向 A）落地 C13 并配上述 parity harness，C11/C12 改为 P10-only 并给出 core.py 行号依据，§7.3 重写删除不实 SB3 表述、§7.5 如实记录两项被推翻的依据。确认复核以归一化 AST 重写全等验证迁移（把 8 处视图调用改写回 getattr 后与 fae17eb8 基线 AST 完全一致）、独立复算 pin 字面量、另跑全新合成场景双跑对照（逐元素相等）、复验 9/4/13 注册分区与 37 测试门构成、确认构建快照零触碰，最终 approve。落地树 maintained smoke 见下文。 |
| I54 T10 第三切片：专用证据生产者 | `accepted` | `658fd22bc199f1d792a258201a06e4b188aac7ba`（`codex/redundancy-consolidation`） | 统一架构计划 T10 第三切片，按 I48 普查后续切片顺序第 3 步（"先建专用生产者"）。`RuntimeFacade` 新增四个加法性 run-global 生产者方法——`allocate_run_snapshot_version`/`peek_next_run_snapshot_version`（VA-2 单调快照版本生产者）与 `allocate_trace_id`/`peek_next_trace_id`（VA-8 专用 trace-id 分配器）——类尾追加两个私有 uint64 游标。"run" 裁定为单个 facade 实例的生命周期（对照 facade → WorldBatchRuntime → kernels → 每-world 事件 store 的所有权链调研）：两游标首值 1、后缀自增单调，刻意不被 export/step/reset_batch/episode 清理/resize 以及内核侧 `clear()`/时钟回卷重置——结构性保证（facade 游标位于内核之上），与可重置的 `next_engagement_event_id_` id 空间分离。自定义 move 语义转移全部 4 成员（run 身份随 facade 迁移、目的地无缝续接序列），源游标经 `std::exchange` 置失效哨兵 0，自 move 有守卫、noexcept、sizeof tripwire 防漏成员；游标失效后四方法一律 fail-fast（`throw_evidence_allocator_invalidated` 抛 `std::logic_error`，消息点名两条进入路径）——失效途径为 move 或耗尽，后者语义如实成文：铸出 `UINT64_MAX` 后回绕恰落 0 哨兵，该计数器永久坍缩进失效态而非回绕重复。零接线：任何既有导出路径都不调用新生产者（那是 slice 4）；既有字段、默认值与序列化输出 byte 级不变。追加四个 nanobind 绑定；新增 3 用例/25 断言的 ef_test move 套件与 445 行聚焦 pytest（9 测试：单调性、与内核事件 id 独立、重置免疫、带点名字段覆盖守卫的全递归归一化序列化不变量）。7 文件，+698/-3。 | GPT 评审第一轮：needs-repair——默认 move 会拷贝 uint64 游标（moved-from facade 可静默铸重复 id、move 赋值可回退目标）、溢出注释过度承诺"永不重复"、序列化不变量只比手写 3 字段指纹、绑定 numstat 口径有误。修复轮一交付自定义 move + 哨兵 fail-fast + tripwire、诚实溢出段、全递归不变量测试；新开复核逐项验证（逐成员 move 审计、8 处 did-NOT-throw 红证的逻辑审计、对照改动前基线的跨构建递归归一化导出 SHA 相等）后仍抓到一项 P2：耗尽注释声称"回绕重复"而实际回绕恰落 0 哨兵、永久 fail-fast。修复轮二把耗尽契约改写为"坍缩进失效态"、哨兵/helper 改名 invalidated 语义、异常消息点名双路径——断言零改动（无任何测试匹配消息文本）。确认复核验证注释与控制流一致（含 peek 行为）、全仓旧名零残留，并重跑全部门（ef_test 3/25、pytest 9、ctest 8/8、smoke 459/45 于 I53 前的工作树基线）；approve。落地树 maintained smoke 见下文。 |
| I58 T11 第四切片·第一束：导弹调参 parse 表驱动化 | `accepted` | `195ab9de61c7bd9a0723ecd8688ad2a5e507cfab`（`codex/redundancy-consolidation`） | 统一架构计划 T11 第四切片第一束，按 I49 普查后续切片顺序第 4 步（表驱动 loader，束级 fixture parity）。`parse_missile_tuning_json_fields` 的 52 个 JSON 读取键迁移到单一声明式字段源：新 X-macro `src/content/detail/missile_tuning_fields.inc`（48 个标量 `EF_MISSILE_TUNING_FIELD(type, name, default)` + 4 个 `EF_MISSILE_TUNING_VECTOR_FIELD` mach 数组，顺序严格镜像结构体声明序；default token 镜像结构体 initializer 供未来单源化、刻意不被展开消费），helper 的 52 条手写语句换成两个宏展开且逐键语义保持（`src.value(#name, tuning.name)` 缺键保持值；`parse_vector`）。选型 X-macro 而非 dto_schema 注册：生成器会对每个 schema 无条件产出语义错位的 `gym_envs/scenario_loader/_generated` Python builder 且 freshness 门硬编码注册表；`.inc` 采用与 dto_schema 字节兼容的宏形式备未来收编。结构体本体保持手写（ABI 不动）；4 个非 helper 成员（warhead/fuze profile 及其 has_* 旗标）留在 `parse_unit_json` 侧；三源合并顺序、guidance 别名与全部 escape hatch 留在调用侧不动。新增两个 C++ parity 用例（全 52 键逐字段；三源覆盖+种子+缺键 NaN 默认）与锚定 I52 survey 的防漂移 pytest 门。4 文件，+737/-61。 | GPT 评审第一轮：needs-repair——运行时行为与 52 键展开被完整重证（评审自跑真实预处理器展开与基线 52 条语句 token 全等：760 tokens/4,185 字符；实现者仓库外双库 dump 覆盖真实 27 定义加合成全键条目 SHA 一致；键集/顺序/类型族/宏组与 I52 survey 及基线读取形态 52/52 相等），但防漂移门有两处可实证绕过：锚在测试内硬编码表（同步篡改 `.inc`+硬编码表可通过且已偏离 survey）、手写残留只查首尾键（中间键注入可通过）。修复轮重写门：I52 survey §1.2 表在测试时从严解析为权威锚（标题缺失/行不连续/未知类型族均红），硬编码表降为三方交叉第三腿，残留检测升级为 helper 体全 52 键 `src.value`/`parse_vector` 模式扫描加带引号键字面量皮带（连 `contains`/`operator[]` 绕形也抓）；六个内存副本负测试。确认复核重演两个原绕过（均红）、复演自己构造的 `contains+operator[]` 形态（皮带抓住）、另试三个新绕过（标题层级装饰变更诚实为绿；伪造第 53 行被连续性守卫抓红；helper 体外注入诚实超出声明范围），并验证修复轮 C++ 面逐字节稳定；approve。门：content 14 passed、freshness+readiness 19 passed、ef_test 122 用例/18,867 断言、ctest 8/8、smoke 460/45、ruff 与 diff-check 干净。落地树 maintained smoke 见下文。 |
| I59 T10 第四切片：真证据值经 opt-in 路径接线 | `accepted` | `959504eb0d0788a28c1333ed22fd42c22b1cb50a`（`codex/redundancy-consolidation`） | 统一架构计划 T10 第四切片，按 I48 普查后续切片顺序第 4 步。census 点名的两处 adapter 占位——`python/rl/runtime/world_batch/adapter.py` 维护窗口路径的 `trace_ids = [1]` 与合成 `input_snapshot_version = "obs:{world}:{entity}"`——经显式 opt-in 接到 I54 的 run-global 生产者：keyword-only 构造开关 `use_facade_evidence_producers=False`（默认关）、对四个 I54 绑定的能力探测 `has_run_global_evidence_producers`、缺绑定时 opt-in 抛具名 RuntimeError fail-fast。默认路径经构造与证据双重证明 byte 级不变：默认分支从不触碰生产者（三窗后两 peek 均为 1——并在 producerless facade proxy 上负向证明：默认路径若调用生产者会当场爆炸）、同 seed 双 adapter 规范化 dump 逐字节相等、caller/None/空白三态与基线三元表达式逐 case 相等、全部 trace/replay/diagnostics 钉扎零改动全绿。opt-in 开启后每窗每生产者恰铸一次：trace_ids [1]→[2]→[3] 且与穿插的内核交战事件活动独立，`input_snapshot_version` 变为 `snapshot:{n}`（复用 restore-boundary 既有 `"snapshot:"+version` 嵌入约定），按文档声明覆盖合成 caller 串。排除项带证据裁定：`parent_trace_id` 属 C++ 侧切片 6（packet ancestry）；`observation_packet_ids` 非 census 点名且无值级钉扎；两个生产消费者留在默认路径。2 文件，+547/-8（adapter +110/-8 加 437 行 9 测试聚焦套件）。 | GPT 评审第一轮：needs-repair——一项 P2：fail-fast 契约无负向门（评审内存置空守卫后七测试仍全绿），实现行为本身验证正确。修复轮新增 producerless facade proxy 门：断言能力位 False、默认路径照常完成且 `[1]`、opt-in 抛具名 RuntimeError 并匹配消息片段；守卫移除演练证明门 load-bearing（恰该测试红，失败形态为守卫本应拦截的裸 AttributeError）。确认复核以 blob 指纹证明修复轮生产代码零改动（adapter blob 与首轮完全一致）、审计 proxy 与 hasattr 能力探测的关系（缓存按 facade id 键控、骗不过）、重演演练（恰 1 failed/8 passed），并沿用首轮验证（默认三态等价、内核穿插独立性探针、111 项钉扎、回归 271/4 errors、ctest 8/8、smoke 460/45）后 approve。落地树 maintained smoke 见下文。 |
| I57 T6 第二机器红清偿包 | `accepted` | `3fabf01a3ad49b03b266c30b04be4ce7e040f604`（`codex/redundancy-consolidation`） | 统一架构计划 T6 第二清偿包：残差台账 §5 的七条可治理机器红逐条复现、定根因、按性质处置（台账新增 §8 逐条记录；§5 状态行更新）。首要发现：三个"lineage 缺口"假设全部错误——wp22 接缝早已落地本分支（`python/tasking_contracts/bridge_views.py:88/177/241`，`bridge.py` 仅 re-export）、wp12 的 `run_window` API 与 coordinator 三 token 俱在（`runtime_facade.h:116`）、懒加载模块刻意暴露 `_ef_py()`；三组守卫在不减弱前提下完成适配（扫描集取 bridge.py+bridge_views.py 并集且负向 token 要求两文件均缺席；空格归一化仅限一处 `&` 风格签名；能力断言保留在 `_ef_py()` 路径），五条红转绿。一条真潜在缺陷在生产修复：`rights_output_policy.py` 的 PDF 文本探针——pdftotext 输出是合法 UTF-8（TP-20 的 0x93 是 en-dash 尾字节、并非最初所称 cp1252 引号；事实已更正并留痕），但 `text=True` 用 GBK 控制台码页解码、杀死 reader 线程、把 `None` 喂进 `re.sub`。修复改为字节捕获 + 严格 UTF-8 解码，解码失败返回专用 `pdf_text_probe_decode_error_fail_closed` 形态且零权利声明命中——此前评审证明过渡版 `errors="ignore"` 失败开放（伪造 `RE\xffLEASE` 字节流拼接出 `public_release` 误命中）；9 测试聚焦套件（6 新行为钉扎 + 3 回归守卫，口径如实重分类）以桩断言锁定二进制捕获模式并钉住伪造字节 fail-closed 场景。未治理的诊断顶层脚本红按 I28 先例上 strict xfail；GPU 测试改 build-gpu 存在性条件 skip；leader-phase-manager 契约红完成分类（契约脚手架落后于本谱系的跑道帧门控与 sync_to_kernel 约定），修复方向记录在案、spec 与 runner 不动。终态：本机零不可归因红——剩余全部为环境类（路径分隔符、校准漂移、flecs/spdlog 收集、已分类契约红）或预期的落地侧双语哈希标记。10 文件，+962/-23。 | GPT 评审第一轮：needs-repair——守卫适配、治理与台账全部成立，但 PDF 探针的 `errors="ignore"` 被伪造字节探针证明失败开放、cp1252 根因声明与事实不符（严格 UTF-8 能正常解码真实 payload）、伪桩未锁定二进制捕获模式。修复轮独立复核评审事实（两份真实 PDF 的 GBK 报错位置与 UTF-8 多字节序列吻合）、改严格解码加专用 fail-closed 形态、补伪造字节负测试、桩锁定 text/universal_newlines/encoding/errors 四类 kwargs、6+3 测试口径在两语台账更正并留痕。确认复核重演伪造场景加新变体（截断多字节尾 fail-closed；BOM 前缀合法 payload 正确识别；None stdout 安全折叠）、核验新状态形态的消费方契约（唯一生产消费者按 `statement_detected` 取值，新状态自然落入 fail-closed 分支）、复证守卫适配证据、确认三条环境红仍红且零触碰；approve。落地树 maintained smoke 见下文。 |
| I60 T8 第四切片：TL13 缝的 ObservationViewSpec facade 导出 | `accepted` | `cf172d8f2f4c3abe374fe4f31a8587d9b29ff62c`（`codex/redundancy-consolidation`） | 统一架构计划 T8 第四切片，清单 §5 第一项：TL13 缝的视图元数据成为运行时可查询的声明。`ObservationViewSpec` DTO（原为 checkpoint schema 兼容规格，dto_schema 管辖，gym_envs 零运行时消费者）经 T1 机制追加四个字段——`view_id`、`information_layer_produced`、`information_layer_consumed`、`semantic_stage`——freshness 门全绿重新生成，全部 DTO 钉扎测试 additive 更新（字段数 7→11、既有断言零放松；遗留的 `evaluate_observation_view_checkpoint_compatibility` 只读原字段、经验证不触新字段）。新增只读纯常量生产者 `RuntimeFacade::describe_maintained_observation_view() const`（facade 头声明、query 单元实现、nanobind 绑定），导出镜像 Python 单源的结构性事实：`view_id = "gym_envs.observation_view"`、产出 Agent Observation、消费 World Truth/Track State/Shared Tactical Picture、阶段 P10 ObservationExport，`required/optional_fields` 刻意留空——详细字段目录留在 Python 注册表以避免双源漂移，由新一致性门守护（`tests/architecture/information_state/test_g4_observation_view_export.py`，7 用例：C++ 导出==Python 注册表 parity、G4 权威词汇白名单、五路 load-bearing 漂移注入、静态零接线扫描证明符号恰在三个 C++ 文件且 gym_envs 零引用、TL13 缝不变、与 facade 状态无关的决定性常量）。零消费者迁移（下一切片）；TL13 行为 byte 级不变。该树构建缓存已腐坏（缺 CMakeCache），按 README 以 pinned 离线依赖重配后全量 90/90 重建。11 文件，+448/-4。 | GPT 评审：approve、零发现。评审独立裁定 DTO 归置问题（复用计划点名的 ObservationViewSpec 可接受：新字段描述同一规格的身份/层/阶段侧面、遗留兼容逻辑经证不读新字段、独立 DTO 会新增类型/schema/绑定面而当前无消费者；提示未来迁移切片需定义空字段清单是"未知"还是"确实为空"），复核 `.inc` 追加纪律（旧 7 字段逐字未动、新 4 严格追加、field_count 11），独立解包导出 spec 并与 Python 注册表逐字核对（parity 为空），确认漂移注入真实，复跑全部门（聚焦 85+5、广域 264 加 4 个环境性 stable_baselines3 收集错误、ctest 8/8、ef_test 125 用例/18,892 断言、smoke 460/45、generate --check 全部最新、ruff 与 diff-check 干净），并验证重建产物与源码同步（增量构建 no-work）。落地树 maintained smoke 见下文。 |
| I64 T9 fixture 注释刷新 | `accepted` | `0aa76a0091509f746cd3f6b1b8ae8dae871c27f3`（`codex/redundancy-consolidation`） | 清偿 I53 落地时登记的 P3 欠账：agency 普查 fixture（`tests/architecture/fixtures/agency_authority_census_20260721.json`）的 A6 `semantic` 注释仍是 I53 评审链已证伪的"leader-intent 压过 mission-command 开火授权优先级"旧表述。注释改写为已裁定事实——naval_profile 无 `leader_intent`；`build_kernel_mission_command` 从 `loader.mission_cmd` 填权限/ROE 字段后再从 `scenario_data["mission_command"]` 重读，常规装载路径二者绑定同一 dict（`loading.py:113-117`，`:242` 再同步），仲裁形态的重读是幂等伪优先级；运行期重绑是否分离仍待裁定——并保留更正痕迹。只动 `semantic` 字段；ratchet 判定面（tokens/token_counts/categories）零触碰。其余 13 条 fixture 注释对照普查 §3 逐行审计一致（A4/A5 的真实 leader-intent 优先级表述正确：air/ground 各 29 处 `leader_intent`、naval 零处）。可选的语义内容断言裁定不做（门刻意只断非空；14 条中 13 条不引用普查章节，任何引用要求都会误伤）。普查 EN/ZH §8 各补后续小节闭合台账。3 文件，+35/-1。 | GPT 轻量复核（窄域——事实已由 I53 两轮评审钉死）：approve、零发现——fixture diff 确认仅 semantic 且 ratchet 面未动、新 A6 文本对照普查 §3/§3.2 与 loading.py 同 dict 事实验证无越界声明、抽查 A4/A8/A13 三条注释与源码证据一致、EN/ZH §8 小节对齐、agency 门 21 passed、ruff 与 diff-check 干净。落地树 maintained smoke 见下文。 |
| I70 构建基础设施：nanobind MinGW 可见性回移与 cmo_env 共享解释器覆盖 | `accepted` | `31c4f03e933131eb52da3b9670c0b3ab5c824401`（`codex/redundancy-consolidation`） | 落地波次的构建解锁：钉定的 nanobind v1.9.2 在 Windows MinGW GCC 下无法编译（NB_EXPORT 展开为 `__declspec(dllexport)` 而 NB_NAMESPACE 对任何 `__GNUC__` 编译器附加 `visibility("hidden")`，GCC 拒绝该矛盾），本机任何 nanobind-static 重编译都会变红。通过 FetchContent PATCH_COMMAND 以幂等、fail-closed 的 `cmake -P` 脚本（`cmake/patches/nanobind_mingw_visibility.cmake`）精确回移上游 bcd9360 的 NB_NAMESPACE 守卫（隐藏可见性限定到非 Windows GCC；v2.0.0 首发）：重复运行检测到已应用守卫即无操作；钉定 tag 变更则 FATAL_ERROR 并给出可行动信息而非静默跳过；MSVC 与 Linux GCC 走不变代码路径。bcd9360 的三个 hunk 中仅回移 NB_NAMESPACE，范围理由经评审后如实记录（NB_EXPORT 在 v1.9.2 已是 post-image——钉定版本早于 PR #440 的回退；NB_EXPORT_SHARED 的 pre-image 仍在但刻意不回移——仅静态库构建下对非导出静态符号附加隐藏可见性，不可能产生该矛盾）。改升 v2.0.0 记录为当前不可行（`src/interfaces/python` 的 `nb::shape`/ndarray/lambda 签名 API 需先迁移）；declare 处记录 CMake<3.27 的 UPDATE_DISCONNECTED tag 升级警示。`tools/maintenance/cmo_env.ps1` 增加 `CMO_PYTHON`/`CMO_VENV` 覆盖，使单任务工作树可共享一个解释器（镜像既有 `CMO_BUILD_DIR` 惯例）；两者均未设置时行为不变，两处 venv 缺失报错现均给出覆盖提示，导出 CMO_PYTHON 的嵌套跨仓警示记录于覆盖处。3 文件，+99/-2。 | 独立评审第 1 轮：needs-repair——一项阻塞 P2、仅注释：补丁脚本的范围理由（"v1.9.2 已按预期拼写其余两个守卫"）对照上游 bcd9360 实际 diff 检验，对 NB_EXPORT_SHARED hunk 为假——其 pre-image 逐字存在于钉定的 v1.9.2 头文件；声明仅 NB_EXPORT 一半成立。注释所支撑的结论——修复 MinGW 构建只需 NB_NAMESPACE hunk——独立确认正确。其余多以执行验证：原守卫串与钉定哈希处的原始 v1.9.2 `nb_defs.h` 逐字节一致、替换串与 bcd9360 post-image 一致；脚本在便签副本上证明 LF 与 CRLF 输入下 apply-再运行均幂等（关键：本机全局 `core.autocrlf=true`、FetchContent 检出 100% CRLF、CMake Windows 文本模式 `file(READ)`/`file(WRITE)` 归一化行尾）；fail-closed 路径（守卫结构变化、头文件缺失）exit 1 且信息可行动；三个 CI workflow 均 ubuntu-only、守卫改动在该处为预处理器无操作；v2.0.0 迁移阻塞声明抽查属实（bindings_core.cpp:788 的 `nb::shape`、bindings_gpu.cpp:364/511/538 与 binding_utils.h:30/39 的 `nb::ndarray`）；cmo_env.ps1 的 CMO_PYTHON > CMO_VENV > 根 .venv 优先级合理、三源均有存在性校验。修复轮将范围注释改写为三 hunk 事实并补 UPDATE_DISCONNECTED 与 CMO_PYTHON 双角色警示。确认轮以文件直接 diff 证明修复仅注释（补丁脚本功能体逐字符一致，第 1 轮经验证据无需重跑直接沿用）、将每条新声明对照第 1 轮上游事实复核、并审计落地树（未暂存集恰为两个切片文件加单文件 `cmake/patches` 目录；已暂存 I61 内容不相交）。非阻塞残余记录：`cmo_env.sh` 未同步移植；覆盖失败时报错措辞。Approve。落地树 maintained smoke 见下文。 |
| I61 T11 第四切片第二包：直连 unit-definition 字段按解析阶段表驱动 | `accepted` | `e710d64f8fa0e6dc7ee9ba071f6f59c841ce1131`（`codex/redundancy-consolidation`） | 统一架构计划 T11 第四切片第二包，循 I58 missile-tuning 先例：`parse_unit_json` 的纯机械顶层标量直读迁到单源 X-macro 字段表（`src/content/detail/unit_definition_direct_fields.inc`）。被接受的设计为两阶段——`EF_UNIT_DIRECT_EARLY_FIELD`（mass_kg）与 `EF_UNIT_DIRECT_DATA_LINK_FIELD`（data_link_network_id）在原始解析位置原地展开——因 JSON 转换可抛异常，解析阶段位置在畸形多键输入上行为可观测（fail-first 顺序）；round-1 的单阶段设计及其"读取顺序不可观测"的头部声明在修复轮撤回，被取代的 round-1 暂存层在与存档的 i61-staged.patch 确认逐字节一致后于落地时弃置。专用 C++ doctest 用例钉住畸形键 fail-first 顺序；633 行 pytest 守卫（`tests/architecture/content/test_unit_definition_direct_fields_inc.py`）携带穷举的字面直读形状清单、默认值篡改与展开位点移动红测试，并注册进 `tests/smoke/ci_smoke_suite.json`（CI 唯一 pytest 入口；I58 的姊妹守卫早于该惯例、仍未注册——记为潜在缺口，本切片不扩大处理）。六个 presence/enablement 标志、`default_loadout` 与编解码逃生舱按队列红线保持 held。5 文件，+789/-2。 | GPT 评审第 2 轮（修复后）：approve。评审者独立重推导整个 diff 而非信任交接：新旧 loader 全量 diff 恰为两处原地单行替换，两个阶段宏各自 token 对 token 展开为被移除的 pre-I61 语句（`def.` 后参数替换、#name 字符串化、字面默认值被消费——与 I58 非消费默认 token 的对比记录准确），.inc 尾部 #undef 使第二处重定义干净。fail-first 顺序声明验证为真实可观测：parse_pass 仅捕获 fs::filesystem_error（有记录），nlohmann type_error.302 逃逸出 load_unit_definitions_json，两个 C++ 探针可判别真实移动（engine_ref 数值加字符串 network-id 仅当 data-link 读仍在 engine_ref 之后才产生 get<string> 消息；数组 network-id 加对象 max-reports 仅当 network-id 先于 clamp 读才产生 "but is array"；空消息会使 CHECK 失败，两探针均不可能空洞通过）。守卫篡改覆盖确认真实：展开位点钉以精确串计数加相邻语句位置顺序，移动演练对两块均变红；阶段交换触发 _CONVERGED_FIELDS 组钉；8 行严格形状清单与评审者对父版本 loader 的独立正则扫描一致；round-1 P2 修复（排除登记 = 调查表减 PINNED 裁定集）属实且其旁路重放演示了旧的同义反复；残留/引号字面带核对过文件（仅存的 "mass_kg" 字面在提取体之外的 parse_warhead_json_fields；"data_link_network_id" 字面归零）；门已注册进 ci_smoke_suite.json。调查表锚点（第 3、48 行，number->double / int->int，升序）、I58 先例对齐（同目录、include 惯例、parse_xmacro 读取器、三腿门形状）、以及 round-1 "顺序不可观测"语言零残留均获确认。零阻塞发现；三项非阻塞：C++ 顺序测试注释高估其探针（两个探针 JSON 均不含 mass_kg 或 has_data_link——该钉仅存于静态门；后续加畸形 mass_kg 探针或修注释）、守卫函数体提取器对 C++14 数字分隔符的假想盲点、I61 登记行不在本提交内（顺延至本次登记刷新）。落地树验证：ef_test 127 用例 / 18,902 断言，ctest 8/8，聚焦内容门 34 passed（新守卫 + I58 姊妹），ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I62 T4 第二切片：清退八个死亡 per-step-builder 接口 | `accepted` | `f7ca0f64ff9f975e0e678c5980bd3d1793f2e6e9`（`codex/redundancy-consolidation`） | 统一架构计划 T4 第二切片：清退已提交的 I43 普查（docs/plan/exact_runtime/cpp_exact_runtime_refactor_plan.md）点名的八个死亡私有 per-step-builder 接口——I43 评审当时将其观察为并行写入并刻意排除在那次 docs-only 落地之外——代码半件现携带自身评审落地。gym_envs/scenario_loader/core.py 的五个 ScenarioLoader 转发壳（_build_mission_nav_products、_compute_mission_observation_products、_build_step_info_runtime_inputs、_consume_compiled_episode_runtime、_build_waypoint_reward_inputs，及其不再使用的 *_impl 导入；模块级自由函数保持存活且有模块内调用者），以及三个被 C++ 批处理路径取代的方法：CooperativeWorldBatchVecEnv._build_slot_observation（被 compute_execution_observation_batch 取代）、_WorldBatchVecEnvObservationMixin._collect_observations、_execution_episode_controller_state_requires_reprime。纯删除加两处随之而死的导入移除；同时闭合 I43 计划文本已声称清退的文档-代码分歧。4 文件，-172 行。 | GPT 评审：approve、零阻塞。评审者在 4571d1e4 全树对八个符号逐一 git grep：代码/测试/工具/fixture 引用为零（仅文档命中）；在父提交对 tests/、tools/ 复核无先在测试引用（无测试被删亦无需删）；五个所有权自由函数（build_mission_nav_products 等）确认仍被定义、导出且有模块内调用者、未被本 diff 触碰；被移除的 build_universal_observation 导入在 gym_envs.universal_env(_parts) 仍有导出与存活调用者（移除仅局部）；_execution_episode_mixin.py 的 json 导入移除后无残留使用。普查清单与 diff 符号对符号一致（第八个符号为 _execution_episode_controller_state_requires_reprime），EN/ZH 普查对齐且本提交不触碰文档。三项非阻塞：存档的 WP4 笔记仍列 _collect_observations 为 compatibility_adapter 行（历史存档记录，无需行动）；任务简报所引 .github/codex/review-boundaries.md 在该提交不存在（评审纪律改取 docs/plan/repository_consolidation/README.md 与 pr-review 提示词）；纯删除切片未登记防重引入漂移门（与 I14 死接口移除先例一致）。落地树验证：聚焦冒烟成员门 80 passed（world_batch 运行时表面、批场景运行时、agent_shim、场景编译器），四文件 py_compile 干净，ruff 干净；broad world_batch 集的 4 个 stable_baselines3 收集错误为 I60 起记录的已知环境性缺失，本切片未改变。落地树 maintained smoke 见下文。 |
| I68 T9 切片：经注册表声明的 A3 授权默认名所有权 | `accepted` | `c10cceac76492ad35ad68b92dc902fe97b292d19`（`codex/redundancy-consolidation`） | 统一架构计划 T9 切片：A3 授权默认*名字*所有权。维护 tasking 归一化层的 command-relationship / authority-scope 默认名从 A3 叶子提供者的本地字符串字面量（`python/rl/profile/common_core_defaults.py`，原为 `getattr(ef_py.CommandRelationship, "TACON")` / `getattr(ef_py.AuthorityScope, "Tactical")`）迁移到注册表声明（`python/tasking_contracts/agency_registry.py`：`DEFAULT_COMMAND_RELATIONSHIP` / `DEFAULT_AUTHORITY_SCOPE`）；A3 将注册表所有的名字解析到编译 `ef_py` 枚举——值逐字节一致、C2 行为零变更、依赖方向合法 `python.rl -> python.tasking_contracts`（I53 agent_shim 先例）。这是较早更宽变体经评审后的收窄版：范围从"A2/A3"诚实缩至仅 A3 名字所有权（A2 委派给 A3，注册表措辞传递性准确），钉测中越权的 ast/inspect 机械被移除。3 测试等价钉（`tests/architecture/agency/test_authority_default_single_source.py`）在基线红、变更后绿（函数粒度）；普查 EN/ZH 增第 9 节分类痕迹，3.2 节 A3 行加交叉引用注记。落地侧修复严格按评审者指定执行：唯一 `pre_i66` 遗留函数名改名、"每条断言"docstring 改写为函数粒度、两个 agency 门（从未注册的 `test_authority_registry_gate.py` 与新钉）均注册进 `tests/smoke/ci_smoke_suite.json`。6 文件，+252/-12 及冒烟注册。 | 独立静态评审（I68 工作副本，5 个跟踪 diff + 未跟踪测试，头 0aa76a00）：approve、零阻塞。收窄验证诚实：代码改动限于 agency_registry.py（声明 DEFAULT_*、docstring 更新、排序 __all__）与 A3 common_core_defaults.py（导入并解析注册表名）；A2 未触碰，注册表"A2/A3"措辞因 A2 委派 A3（common_core_profile.py:400-405）而传递性准确。字节一致独立确认：同名同 getattr；core_tasking_enums.h 验证 TACON=3/Tactical=3 与未设哨兵 None/Unspecified=0；限定 grep 确认除枚举定义与 bindings_command.cpp 导出外无编译位点赋值这两个成员，authorize_maintained_* 前提修正成立。等价钉裁定为承重：每个测试函数引用基线不存在的符号（registry.DEFAULT_* / a3._DEFAULT_*），基线整模块红、默认名变更即触发值+位置钉。依赖方向普查合法（python.rl -> python.tasking_contracts，agent_shim 先例）；注册表仍 stdlib-only、无反向边。ratchet 不变性按门的实际扫描器语义独立重算（tokenize 去注释、ast 导入丢弃、大小写敏感词边界）：变更后 A3 代码面恰含一个 CommandRelationship 与一个 AuthorityScope，与未改 fixture 钉一致。EN/ZH 第 9 节与头部注记逐节等价；cluster 哈希以注册表自身归一化重算并与更新后 JSON 一致。六项非阻塞：测试函数名唯一 pre_i66 遗留（穷举）；docstring "每条断言"高估（红-基线在函数粒度成立）；is-同一性分叉保证在 CPython 字符串驻留下空洞（行为漂移仍由值钉捕获）；3.2 节 A3 行陈旧无第 9 节指针（双语同等陈旧、对齐保持）；新测试与既有 agency 门均未注册冒烟；EN 第 9 节 Tactical "仅出现于"措辞宽松。判定：approve——无假声明、无行为变更、无弱化守卫；遗留项移交落地。落地树验证：agency 门 24 passed（21 注册表门 + 3 新钉），ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I63 T8 第五切片：G4 登记清账与门网加固 | `accepted` | `6c81d3b9fda72eb153f1cc552a9e5437f4f4e80e`（`codex/redundancy-consolidation`） | 统一架构计划 T8 第五切片：G4 登记清账与门网加固；仅文档+测试，行为零变更。双语 T8 G4 真值泄漏清单对已落地的 I60 结构声明导出清账（TL13 行、第 4 节收尾注、第 5 节后续切片改绑到类型化 ObservationViewSpec 数据流迁移；新增第 8/9 节；头部刷新到 0aa76a00/2026-07-26）。test_g4_truth_read_ban.py 增正向视图使用门（+168 行），新建 test_g4_inventory_consistency.py（221 行）将维护的 T8 登记与代码注册表在代码→文档方向钉定。评审轮产出两项阻塞修复，均已应用并确认：清单<->代码门的 docstring 与清单 9.2 节不再高估双向锁步（门仅强制代码→文档——注册表新增/改名与未记录 face 变红；陈旧或伪造登记行明确记录为不强制），奖励逃生舱扫描去除 __init__.py 排除使登记的"任意文件"声明字面为真（reward_runtime/__init__.py 验证为纯再导出面）。8.1 节写集括注更正（MAINTAINED_INFORMATION_LAYER_VIEW_OWNERS 为 I50 新增、I60 复用而非新增），第 9 节标题日期对齐 07-26 刷新。两个 G4 门按队列规则 4 注册进 tests/smoke/ci_smoke_suite.json。5 文件（另冒烟清单），跟踪 +395/-27 加 221 行新门。记录残余：视图使用门的承重演练仅覆盖导入半边；'.observation_view' 后缀匹配宽容度。 | 独立评审第 1 轮：needs-repair——两项阻塞（清单<->代码门方向高估；奖励扫描 __init__.py 排除与登记"任意文件"声明相悖），另四项非阻塞。确认轮（只读，EF-w1-helpers @ 0aa76a00）验证全部修复增量：重写的 docstring 逐断言对照门代码——五个测试全部仅强制代码→文档（注册点分模块存在于盘 + 斜杠路径在两份登记 + __all__ 与公开定义一致 + face 词边界记录），无断言将文档行反向解析回代码，方向声明与不强制情形枚举（陈旧行、伪造行保持绿）双双属实；9.2 行 2 双语句级等价；grep 确认无"锁步"式双向措辞残留。_reward_consumer_sources 现 rglob 全部 *.py 且无 __init__.py 排除（注释引 9.2 全覆盖声明）；reward_runtime/__init__.py 全文通读——51 行纯再导出、无 truth 记号、在 _raw_truth_read_lines 下干净——两门文件在工作树 venv 34/34 通过。非阻塞修复验证：8.1 括注 EN+ZH 正确重归属（git log -S 确认 26e8b2b6/I50 引入，与修复 JSON 引证一致）且 I60 为复用非新增；第 9 节标题双语均为 (I63, 2026-07-26)、07-22 零残留。附带维护：clusters json diff 限定于该对哈希/last_verified 加 generated_at，哈希治理测试通过。范围完整性：git status 恰为五个声明文件（4 改 + 1 新增）、无 gym_envs/C++ 生产编辑、零提交零暂存。零新发现；修复正确、完整、双语等价、最小化。Approve。落地树验证：information_state 门 61 passed，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I67 T2 收口交付物：学习运行时一致性笔记（修正案候选 (e)） | `accepted` | `39db0555612315167c0e4598672ba6891da25934`（`codex/redundancy-consolidation`） | 统一架构计划 T2 收口交付物：回填基线 Learning face 的学习运行时架构笔记（修正案候选 (e)）。对维护的 Python 学习运行时（python/rl/runtime/world_batch/**、policy_algo、training、tasking、agent_shim）针对基线架构第 17 节三条 Learning-face 契约的只读一致性普查（`docs/plan/unified_architecture_program/t2_learning_runtime_note_20260726.md` + 撰写的中文伴随），钉定基线 0aa76a00、每个判定附 file:line 证据。评审轮的阻塞修复已落：第 2 节 Env-as-View 的 episode 条款不再高估——现以引证如实陈述默认路径所有权（两个执行 episode 控制器旗标在 vec_env.py:147-148 默认 False；默认分支经 _compute_loader_step_outcome 拥有 Python episode 步进；opt-in 主线分支经 facade 镜像），契约 1 判定携带相应限定。另按评审修复：注册消费者计数（十三，并列构成）、第 6 节常量名（kClockDomainAdvisoryOnly）、第 3 节 VA-2 精确化（run-global 生产者 I54 落地、I59 后 opt-in 消费）。仅文档；代码零变更。2 文件（另注册表登记）。 | 独立评审第 1 轮：needs-repair——一项阻塞（第 2 节无引证且高估的 episode 条款）加四项非阻塞准确性项。修复在工作树内应用；中文伴随在修复轮撰写。确认轮（只读，EF-w10-t2note @ 0aa76a00）：重写的第 2 节 episode 条款每条引证重读并确认准确（vec_env.py:147-148/675/723-739/821、_execution_episode_mixin.py:181-191、adapter.py:886-887），契约 1 限定与证据匹配；十三个消费者构成核实；kClockDomainAdvisoryOnly 与 VA-2 措辞核实；新中文伴随全文逐节审计——判定、引证、数字与英文一致，头部循 t10 词汇表对惯例。零新发现。Approve。落地树验证：文档链接审计 0 issues，双语 cluster 对已登记且哈希同步，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I65 T6 第三残余清偿包：六项红处置 | `accepted` | `646009587a42adad91e484c226d4059994205290`（`codex/redundancy-consolidation`） | 统一架构计划 T6 第三残余清偿包：I57 后遗留的六个可治理红各按已证根因分类处置。(1) test_guard_enforcement.py / test_clock_domain_enforcement.py / test_default_factory_spawn_plan_resolution.py 的 flecs/spdlog 收集错误改为以实际依赖 include 存在为谓词的条件跳过（局部 _optional_dependency_include 返回 None 取代模块级 AssertionError；requires_flecs 标记编译绑定测试、requires_platform_spawn_includes 标记四个；理由点名缺失的 _deps/<dep>-src 并指向台账第 5 节）——根因为构建快照完整性工件，完整树上永不触发。(2) 三个伤害模型红中两个裁定为测试侧断言缺陷并直接修复（retained_pack/manifest.json 的路径后缀比较改为组件归一化）而非按漂移治理。(3) 三个真正固有的二进制校准红（两个空战表面 + fragility 基准，经 surface_probe._sample_primary_event 证明为一个漂移透过三个测试观测）改为 xfail(strict=True)，恢复即以 XPASS(strict) 浮出。台账第 5/8/9 节双语更新，含两条明示的分类分歧记录（9.2）。评审轮两项阻塞更正已落地并确认：9.6 冒烟计数归因诚实重推导（458→459 的 +1 系 test_agent_shim.py 在 I53 的 1a2e64a5 从 21→22——伤害模型文件非冒烟成员、不可能移动计数；评审者自己的 71cdb12c 假说亦被证伪为已在 8.10 基线内），9.1/9.4 三处数字误计按文件重数（7 测试 / 12 文本-AST 守卫 / 23 中 12 为恢复守卫）。10 文件，约 +962 治理/测试行。记录残余：xfail 范围警示、模块级 fragility fixture 警示、XPASS 可见性仅全量运行。 | 独立评审第 1 轮：needs-repair——两项阻塞（9.6 假冒烟门归因；台账自身证据三处数字误计），另 drive→drift 拼写；条件跳过谓词、strict xfail 与两处直接断言修复在第 1 轮均验证可靠。确认轮（只读）独立重推导每个更正数字与新归因：grep '^def test_' 得 16/1/7（共 24）；跳过标记 7/1/4（12 编译绑定，两 flecs 文件中 9）；空战文件 13 定义、2 个 xfail(strict=True)，11 恒可收集通过、12+11=23 与 9.6 括注一致。冒烟归因：git diff fae17eb8 0aa76a00 -- ci_smoke_suite.json 为空；test_agent_shim.py 在两端点 21/22 定义、区间唯一触及提交 1a2e64a5、零 parametrize 变更；63 成员清单零伤害模型成员；另一变更整文件成员 test_dto_schema_freshness.py 保持 1→1，唯一变更的节点成员文件无测试定义变化——agent_shim +1 完整解释 458→459；71cdb12c 假说正确否决（merge-base 确认其为基线祖先）。EN/ZH 每处更正句级等价；被取代措辞双语负向 grep 归零。范围收敛验证：git status 恰 10 个已审文件、8 个测试文件仍与原审计状态一致、零提交零暂存零构建。一条非阻塞注记仅针对修复 JSON 的自述（成员文件变更少计）；台账文本本身精确。Approve。落地树验证：T6 相关架构面（guard_enforcement、clock_domain、platform_spawn、damage_model）289 passed / 1 xfailed（fragility 基准校准 xfail(strict) 按设计触发；两个空战校准 xfail 属本切片写集、由运行时套件运行），information_state 同批 61 passed，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I66 T11 第四切片第三包：aero-tuning 解析表驱动 | `accepted` | `47abd3c9964d908259f76f67fc73f743c8935f9c`（`codex/redundancy-consolidation`） | 统一架构计划 T11 第四切片第三包，循 I58/I61 先例：`parse_aero_tuning_json_fields` 的 44 个纯机械 JSON 键（37 标量 + 7 个 std::vector<double> 马赫表）迁到单源 X-macro 表 `src/content/detail/aero_tuning_fields.inc`，分两遍 include（标量在 parse_vector lambda 之前、向量在其后），展开与变更前语句顺序 token 一致。`enabled` 刻意保持手写：其字面 `true` 默认对 airframe.tuning/aero_tuning 预设-再-合并编解码逃生舱承重。锚点按设计偏离 I58 并有门钉定：字段表锚定 AeroTuning 结构声明（`src/components/domains/air/platform/flight_dynamics_tuning.h`）而非 I52 调查表。两个 C++ doctest 平价用例循 I61 模板；pytest 守卫（`tests/architecture/content/test_aero_tuning_fields_inc.py`，23 测试）具三腿门形状并按评审阻塞项注册进 `tests/smoke/ci_smoke_suite.json`。4 文件另冒烟清单。记录残余：I58 姊妹守卫缺冒烟注册为可立即注册的跟进；`enabled` 折叠精确性注记。 | 独立评审第 1 轮：needs-repair——一项阻塞（防漂移守卫未注册冒烟清单、CI 中永不运行），另有当时未落地的 I61 引证与登记行落地义务为非阻塞；包机械（两遍 token 一致、`enabled` 保持、44 键完整、NaN 哨兵默认保持、篡改覆盖）第 1 轮均验证可靠。确认轮（只读）验证全部修复增量：ci_smoke_suite.json 恰增一行、位置与已落地 I61 提交 e710d64f 的插入邻域逐字节一致（两个 content 守卫落地后相邻）、文件重新解析 64 路径、注册路径解析到真实文件、守卫在工作树 venv 下 23 passed 0.46s；两处引证修复对照事实核实（e710d64f 存在、标题与分支与新措辞一致、I61 的字面默认消费对比声明获 I61 .inc 头部文本确认）；增量收敛（修复仅动清单与两个未跟踪包文件内部；loader 与 compile-passes diff 为原评审接受的既有包）；无双语对触碰。残余照记录。零新发现。Approve。落地树验证：ef_test 129 用例 / 18,962 断言（两个新 aero 平价用例绿），content 守卫 57 passed（aero + 直连字段 + missile-tuning），ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I69 T10 第五切片：maintained run 的 ReplayEnvelope 生产者 | `accepted` | `9362a13621090c3fba9fd0a2d8cf612eabe8aa97`（`codex/redundancy-consolidation`） | 统一架构计划 T10 第五切片，依 I48 普查的后续切片顺序第 5 步。既有两处 `ReplayEnvelope` 组装（`replay_envelope_from_experiment_request`、`runtime_counterfactual_restore_boundary_for_snapshot`，均为文件局部且本切片未触碰）都从 request/snapshot 字段构造信封；新增的只读生产者 `RuntimeFacade::build_maintained_replay_envelope` 改为从 maintained run 的**真实**窗口产物组装——`snapshot_ref`/`facade_provenance_ref` 复制导出 observation packet 自身 run 产出的 provenance 字符串与结构体，`barrier_ref` 复制窗口真实的 `window_commit` 记录，`event_order_ref` 锚定 engagement packet 由 VA-8 铸出的 `trace_ids` 尾项并带上 packet 真实的 `producer_node_id`，`source_time_s` 回显窗口真实的 context 时间，`run_id`/`episode_id`/`deterministic_seed` 仍为调用方所有的 run 身份。新增 `MaintainedReplayEnvelopeResult` DTO（一整块叠加式新增，零删除，无成员重排），其 `admitted` 为失败即关闭：仅当九道命名闸门全部通过**且**组装出的信封通过 `validate_replay_envelope` 时为真；被拒结果不暴露任何半组装信封。关键的真实性绑定：窗口每个 `trace_id` 都必须由**本** facade 的 VA-8 分配器铸出，因此默认 maintained 路径的占位 `trace_ids = [1]` 面对未被触碰的分配器（peek == 1）可证不可准入——即便调用方绕过 adapter 守卫也无法构造合成证据信封。普查 VA-2 已作出裁定而非悬置：packet 内嵌的 `snapshot_version` 是每次导出即重置的按导出序号，经实测证明不唯一（同一 run 的三次真实导出全部得到 `global:1`），故新增可选入 `run_snapshot_version` 参数（默认 0 = 关闭），使用第三切片的 VA-2 run 全局单调分配器将 ref 叠加式限定为 `"global:{export_n}:run_snapshot:{run_global_n}"`，既有值的确切含义与位置作为前缀完整保留，并对分配器游标重新校验，使调用方臆造的身份无法进入。Adapter 缝 `RuntimeFacadeAdapter.build_maintained_replay_envelope` 要求 I59 的可选入开关并给出命名 `RuntimeError`，且从窗口自身真实的 executed-node 记录中回收 run 全局版本而非接受外部传入。零接线：maintained 运行时无任何处调用该生产者，它只 peek 分配器游标，既有序列化值逐字节不变。9 个文件，+1353/-1（6 用例/67 断言的 ef_test 契约套件覆盖游标可控输入，另加 21 测试的真实运行端到端 pytest 文件）。该真实运行 pytest 文件刻意不注册进 smoke 清单（`tests/smoke/ci_smoke_suite.json`）：它端到端驱动真实 maintained 运行（实测成本），且其契约面已由随 ctest 运行的 6 用例/67 断言 ef_test 套件钉扎——即队列规则 4 的实测成本加自动化所有者豁免理由。 | GPT 评审第 1 轮：needs-repair——两项阻断发现。(1) ef_test 套件指向一个并不存在的真实运行证明文件 `tests/runtime/engagement/test_maintained_replay_envelope.py`，同时自述“刻意合成”，而 Python adapter 缝零覆盖：一个唯一测试自我免责于真实运行场景的切片，并未证明其论点。(2) 与 I54、I59 不同，缺少迭代登记注册。修复补齐了缺失的 21 测试真实运行文件——每个信封都由 seed-123 交战场景经真实 maintained 窗口驱动、携带真实导出 packet 构建，每个期望值都从窗口产物读回而非写成字面量——覆盖已准入且通过验证器的信封、缝的可选入与缺失窗口契约、九道失败即关闭闸门的负向证明（每道通过仅扰动一个其余皆真实的窗口产物字段触发；一次去守卫演练废除两道分配器闸门后，恰好对应的三个测试转红）、VA-2 默认与限定值对比，以及未变更的默认路径。评审者关于“不存在按切片的普查文档子节惯例”的更正已被接受（I54 与 I59 均未修订普查），故本行加上集群登记刷新即为完整的登记面。经验证为可靠的部分保持原样：无 ABI 违规、生产者确实消费真实 run 状态、trace-id 准入闸门仍对默认合成路径失败即关闭。 确认第 3 轮（仅修复增量）：diff-stat 独立重数（numstat 跟踪 7 文件 +513/-1，未跟踪测试文件 269+571 行，合 9 文件 +1353/-1，逐文件一致），冒烟豁免理由句双语在场且等价，登记对 cluster 哈希对照编辑后字节核实；approve。 |
| I73 T2/T3 切片：经中立层运行时协议的类型化 ScenarioLoader.sim 接缝 | `accepted` | `cab55609365312329be0c4985c006cbd26bdea77`（`codex/redundancy-consolidation`） | 统一架构计划 T2/T3 切片（队列的类型化接缝行）：`ScenarioLoader.sim` 句柄成为声明的结构契约。在中立 tasking-contracts 层定义纯 stdlib 的 `typing.Protocol`（`ScenarioLoaderRuntime`，`python/tasking_contracts/runtime_contract.py`：11 必需 + 6 可选方法集），模块 docstring 携带完整维护访问普查（四条访问路线带 file:line：`gym_envs/scenario_loader/core.py:1010`/`:1019` 的 TL13 `self.sim` 读、`runtime_support.resolve_loader_runtime_sim`、`bridge_views` 鸭视图、leader 运行时再暴露）。12 测试一致性门（`tests/architecture/tasking_contracts/test_scenario_loader_runtime_contract.py`，已注册冒烟）证明 `_ScenarioLoaderRuntimeProxy` 实现该协议（类字典在场 + 签名 + isinstance + 可选方法缺省钉），钉定维护路径代理注入（全仓恰一处 `ScenarioLoader(` 构造于维护树，`adapter.py:686`），原始内核注入保持仅测试。行为零变更：三个触及的生产文件仅注解/注释/docstring 编辑。无新枢纽；无 `gym_envs -> python.rl` 边。5 文件，跟踪 +13/-2 加两个新文件。记录残余：`tools/` 诊断按 WP22 惯例在注入门扫描根之外；代理可选方法缺省为既有普查发现、由缺省门钉定。 | 独立评审（EF-w5-reds 工作副本 vs f7ca0f64）：approve、零阻塞。diff = 2 新文件（stdlib-only runtime_checkable 协议 + 12 测试门）加 3 个触及文件、生产内容仅注解/注释/docstring——逐行验证零行为变更。构建者声明独立复现无高估：67/73/85(+15 子测试)/31 绿重跑；9 个协作环境红确认既有（文件未触及；unittest.mock 导入缺陷在 f7ca0f64 已在）。四条普查访问路线在源码逐一抽验；一致性门以新进程变异证明承重（方法删除、签名漂移、可选增长均变红）。维护注入普查经全仓 grep 确认（唯一构造 adapter.py:686）。中立层依赖终端；未加 gym_envs->python.rl 边。降级开放问题如实界定为既有。非阻塞：core.py 普查行号出生即陈旧（本切片自身 +4 行；文本针不受影响）；tools/ 在注入门扫描根外（有记录）。无 C++ 触及、无文档触及。落地侧修复（按评审注记）：普查 docstring 两处 core.py 行号刷新为切片后位置（1010/1019）。落地树验证：tasking-contracts 门 + 场景编译器 60 passed，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I75 T5 切片：承重的实验矩阵加固测试 | `accepted` | `0954c45594a3d88aafcf85c60d80996568b1634a`（`codex/redundancy-consolidation`） | 统一架构计划 T5 切片（队列的矩阵加固行）。调查对落地树裁定了该行前提：I36（009aaa67）已在生成器侧落地全部三个 I30 残余的防护（json.dumps 键转义、递归 `_strict_json_equal`、新鲜度门的配对表断言），故按该行"仅在需要时"条款不做任何生成器、CLI、配置路径或矩阵文件变更。本切片闭合的缺口是第一代回归测试不完全承重（I36 的场景交换测试仅比较两个本地字典副本、在注册表或门回归下保持绿）。新 `tests/architecture/governance/test_experiment_matrix_hardening.py`（已注册冒烟）增加三个真负向注入测试——每残余一个——各自证明绕过 I36 防护时变红，另有生成的空战配置 24/24 字节一致钉。生产零变更；`generate.py --check` 前后字节一致。2 文件。记录残余：I36 的空洞交换测试刻意保留（写集纪律；它记录了自己捕不到的残余）；EXPECTED_EXPERIMENT_SCENARIOS 耦合走 importlib 文件路径加载（tests/architecture 无包 __init__）。 | 独立评审（EF-w11-t5matrix 工作副本 vs f7ca0f64）：approve、零阻塞。I36 已修前提经 git show 009aaa67 与现行生成器代码核实；三个负向测试逐一验证为注入式真回归形状（回退 I36 防护即红）而非现状断言；24/24 字节一致自行运行 generate --check 复现；diff 零 CLI/配置/矩阵变更；importlib 耦合与冒烟注册核查。落地树验证：加固 + 新鲜度门 17 passed，实验矩阵 generate --check 24/24 up-to-date，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I76 T8 切片：逐文件维护消费者分类器 | `accepted` | `8361a81891da7913065012be696458b732979ae7`（`codex/redundancy-consolidation`） | 统一架构计划 T8 切片（队列的消费者分类器行）：以逐文件维护消费者分类器闭合 I63 的观测面逃生舱，对 I63 文件完全增量。新 stdlib-only 注册表 `python/architecture/consumer_classification.py` 将扩展维护面（gym_envs/** + python/rl/**，reward_runtime 扫描的超集）上每个读原始真值的文件分类为 观测消费者 / 奖励消费者 / 指令-动作-装载读者 / 诊断 / 已声明视图所有者：1 视图所有者、4 个 G4 已注册延迟读者、2 个钉为 G4 声明待办的观测消费者（`_vec_env_support`、`observation_batching`）、5 个指令/动作/装载/行为读者。门（`tests/architecture/information_state/test_g4_consumer_classification.py`，13 测试，已注册冒烟）本地重实现 I63 扫描词汇并对已落地 I63 门做 AST 级对等钉，强制扫描<->注册表双向精确一致（注入未注册消费者即败；陈旧行即败），并经声明的 SEMANTIC_STAGE 捕捉结构可查的分类谎言。生产零变更；零耦合（门之外无人导入注册表）。3 文件。记录跟进：将 I63 登记的逃生舱行翻转为分类器闭合（触及 I63 文档）；两个待办观测消费者的 G4 声明。 | 独立评审（EF-w13-t8class 工作副本 vs c10cceac）：approve、零阻塞。注册表全文通读（词汇、注册、纯校验规则 1-7、stdlib-only）；扫描与已落地 I63 门（6c81d3b9 经 git show）AST 级一致（除 docstring）；6 项分类逐一对源码抽验（含两个待声明观测消费者与 leader_tasking 的 P2/P3 指令路径读）；world_batch 目录无 SEMANTIC_STAGE 声明故待办钉机制正确；_generated 零真值读；62 passed 复跑（57=13 新+44 既有，另 5 清单门）；grep 证零生产耦合；冒烟注册经清单门良构；I63 登记翻转正确延后并记录为开放问题。落地树验证：information_state 门 74 passed（13 新 + 61 既有含 I63），ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I77 T9 切片：权限表征边界裁定（no-mapping 判定） | `accepted` | `45ce6c358d6b290953d1ed7ed80f2417821b98df`（`codex/redundancy-consolidation`） | 统一架构计划 T9 切片（队列的表征裁定行）：I68 暴露的表征边界——梯队权限（`CommandRelationship`/`AuthorityScope`）对动作接口权限（`AgentRole`/`AgentAuthorityScope`）——以完整证据链裁定。判定：全路径 no-mapping，正向 5/5（A2/A4/A5/A6/A13）、反向 2/2（world-batch adapter、agent_shim）；0 mapped、0 ambiguous。承重事实：无编译判定逻辑读取梯队值（仅枚举定义、TaskOrderCore 默认与 pybind 导出）；动作接口标识符在 python/rl/tasking、python/rl/profile、gym_envs 下零出现（唯一披露例外：普查外标量 `naval_actions.py:24`）；adapter 仅从动作载荷类型导出 `AgentAuthorityScope.scope`，`authorize_maintained_*` 谓词从不读载荷字段；两个记录在案的同形词仅以拼写跨界。交付物：双语维护文档对 `t9_authority_representation_adjudication_20260726.md`/.zh.md，携带诚实的领域评审记录（待人类 C2 签核、三个具名问题），及 17 测试一致性门 `tests/architecture/agency/test_authority_representation_boundary.py`（正/反向代码面判别钉复用普查门扫描器、编译头不相交钉、篡改自检），已注册冒烟。C2 行为零变更：无生产文件触及。5 文件。 | 独立评审（EF-w12-t9rep @ c10cceac）：approve、零阻塞。写集验证为仅文档/测试；no-mapping 判定经阅读被引代码独立抽验（A2 枚举强制/默认/推断全梯队、A13 holder_id/agent_id 开火逻辑无 AgentAuthorityScope 读、反向 adapter 路径 scope 仅由载荷类型导出、policy_contracts.h:454-476 谓词从不读载荷字段）；全面负向 grep 复现（含披露的 naval_actions.py:24 例外与词边界 AgentAuthorityScope 排除）。门经独立内存篡改探针证明承重：正向字面注入、反向访问器注入、文件掏空均变红；散文提及不误报。测试声明精确复现（17/41/64 passed）。领域评审记录双语诚实待批；EN/ZH 结构等价、注册表哈希与重算 sha256 一致。四项非阻塞：两处中文表述折叠 task-order 与 mission-command 用语、第 1 节 rg 转写不精确（事实本身重验为真）、构建者报告 generated_at 声明未在 diff 中出现、agency_registry.py:604 的 CATEGORY_SCOPE 并列记录为披露的共声明而非映射。落地树验证：agency 门 41 passed（17 新 + 24 既有），ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I80 T4 切片：精确运行时覆盖前置与缺口矩阵 | `accepted` | `407eea2205d532f7ffb275b4562924f6c4db4b2f`（`codex/redundancy-consolidation`） | 统一架构计划 T4 切片（队列的覆盖前置行，仅测试，任何层退役的前置条件）：`tests/runtime/exact/test_execution_controller_option_parity.py` 参数化每个维护的 `flight_shaping_backend` 选项（auto/compiled/gpu_host）叉乘发射后评估开/关，以一个小型确定性 4 步场景（跨越 episode 边界：episode 中、终止、autoreset 后）同时驱动默认 Python 路径（`ScenarioLoader.compute_full_step`）与 opt-in `execution_episode_controller_mainline` 路径，比较观测（atol 1e-5）、奖励与分项（1e-6）、mission_status 与终止——容差钉定为既有主线契约自身数值。覆盖格：5 格平价绿。缺口矩阵（4 个 skip 格、理由入库、逐一引证在场守卫）：gpu_host x *（控制器主线在 WorldBatchVecEnv 构造时拒绝 gpu_host）与 * x 发射后评估开（`_air_combat_post_launch_assessment_should_run` 在主线下硬返回 False）。无默认翻转、无生产编辑；`auto` 解析钉使未来 GPU 感知的 auto 规则以红浮出。已注册冒烟（实测约 13-18s）。3 文件。缺口矩阵是队列所有权迁移门的显式输入：gpu_host 与发射后格必须在任何 Python 层退役前闭合或 held。 | 独立评审（EF-w15-t4cov @ c10cceac）：approve、零阻塞。套件自行运行复现（5 passed 4 skipped）；缺口矩阵 skip 理由逐一对照在场守卫核实（vec_env.py 主线 gpu_host 拒绝；_air_combat_post_launch_mixin 硬 False）；平价断言核实为双路径产品的真实比较（fixture 驱动两条路径）；容差核实为既有主线契约钉定值而非杜撰；零默认翻转、零生产编辑；冒烟注册与队列规则 4 一致；auto 解析钉核查。落地树验证：平价套件 5 passed / 4 缺口 skip 带矩阵理由（13.42s），ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I74 T11 第四切片第四包：ship/submarine 平台字段表驱动 | `accepted` | `1a456e295b3ec761666c1d7dfc4da119f016f9b3`（`codex/redundancy-consolidation`） | 统一架构计划 T11 切片（队列的 ship/submarine 包行，第四切片之第四包）：`parse_unit_json` 的 22 个 ShipPlatform 与 15 个 SubmarinePlatform 内部标量读迁到两个 X-macro 表（`src/content/detail/ship_platform_fields.inc`、`submarine_platform_fields.inc`），在各 if 块内的原始接缝处原地展开。两个 .inc 而非共享一个，由理由测试钉定：两族同名成员携带不同结构默认 token（max_accel_mps2 0.12 对 0.05、max_decel 0.18 对 0.08、max_turn_rate 2.0 对 1.5），共享表无法同时单源两者。适用 I58 非消费默认惯例（默认 token 镜像结构初始化器供审阅对照；每块前的 `{}` 重置保持语义不变），与 I61 的消费默认形成对照。全部 37 个展开的 token 一致性对被移除语句机器验证。对象在场标志、重置、六个 held has_* 标志、`default_loadout` 与编解码逃生舱零触碰。两个 C++ doctest 平价用例循 I61 模板（全字段 fixture 平价含缺失/部分对象默认平价；畸形 fail-first 探针钉族内与跨族顺序）；三腿 pytest 守卫锚定 ship_platform.h/submarine_platform.h 结构声明（键==成员，对内部键强于调查表），已注册冒烟。6 文件。 | 独立评审（EF-w7-t8cmd @ f7ca0f64）：approve、零阻塞。37 个展开的 token 一致性经花括号匹配提取与逐行宏展开独立重推导（ship 22/22、submarine 15/15，含双族同名异默认成员样本）；双 .inc 理由从结构初始化器核实且有测试钉定；I58 非消费默认惯例核实（与 I61 消费默认对照准确、重置使其语义保持）；守卫三腿完整（头文件锚运行时解析、展开位钉+移动演练、默认 token 篡改）；held 面零触碰（diff 恰为两个 if 块）；冒烟注册在 I61 条目之后；54+12+64 测试复跑绿。非阻塞：构建者报告中三个畸形探针 fixture 实为单对象根形式而非 units 包装（探针有效、仅报告句不准）；守卫按 I61 惯例钉精确缩进文本（未来重排需同步守卫）；构建者的后台全 architecture 扫未归（落地侧重跑）。落地树验证：ef_test 137 用例 / 19,103 断言（两个新平价用例绿），ctest 8/8，content 守卫 + 27 定义海军数据库平价 99 passed，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I72 T6 跟进：路径后缀匹配器工具与断言形态裁定 | `accepted` | `7e5372d119382ee429a9c1ce79fe7a6ef5bd4bc3`（`codex/redundancy-consolidation`） | 统一架构计划 T6 跟进（队列 I72 行，经 I65 重叠裁定改造）：该行 2026-07-23 起草于 I65 之前，原定修复两个 Windows 专属 `retained_pack/manifest.json` 后缀断言；I65（64600958）已先以完整 `Path` 相等落地该修复。改造裁定 I65 形式对该两处调用点严格更强（全路径相等钉住整个前缀、不留后缀可诱捕），故两调用点与落地树保持逐字节一致，本切片贡献可复用的加强件：`tests/architecture/damage_model/helpers.py` 的 `path_suffix_components(value, count)`（双分隔符约定切分，用于测试无法构造完整期望路径的后缀检查——docstring 记录优先完整 Path 相等的适用规则）与 15 测试封闭式 `test_path_suffix_components.py`（Windows/POSIX/混合分隔符正例与含跨界 `not_retained_pack/manifest.json` 陷阱的负例——先内联断言原生 endswith 确实被骗再断言匹配器拒绝）。T6 残余台账增第 10 节 EN+ZH 记录断言形态裁定并交叉引用已落地的 8.9/9.3 节。冒烟豁免循 I37/I57 封闭单元测试先例。4 文件，约 +130。 | 改造版独立评审（对落地头 1a456e29）：approve、零阻塞。两处 I65 调用点经 git hash-object 验证与 HEAD blob 一致；真实增量验证恰为三项（helpers hunk +23、15 测试模块、台账第 10 节对 +58/+48），其余工作树 hunk 均识别为内嵌 I65 重复内容并排除出迁移；裁定文本与落地 8.9/9.3 节事实一致；not_retained_pack 陷阱验证真实（原生 endswith 接受、匹配器拒绝）；规定环境下 17 passed；helper 验证纯函数无耦合；冒烟清单未动、循记录先例。非阻塞：count=0 切片弱点注记；台账验证块一处占位符；落地欠注册表哈希刷新。落地树验证：匹配器套件 15 passed，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I71 落地波次收口件：I72+ 依赖序迭代队列 | `accepted` | `29c6fa0af231906edac405bd2fab3941c8a96280`（`codex/redundancy-consolidation`） | 统一架构计划落地波次收口件：依赖序迭代队列以维护计划对 `iteration_queue_i72_plus_20260726.md`/.zh.md 落地，取代从未落地的 2026-07-23 草案（integrated-final-repo fc574f96）并完成波次重编号。内容：近期行 I72-I80（现已全部落地或在制）、依赖门控行 I81-I87、收尾行 I88-I90（T7 两轮 + 修复包）、调度规则、held 清单，以及记录完整旧->新映射的编号注记表（草案 I67-I85 -> I72-I90，草案候选号 I65/I66 -> 落地 I69/I68），保持草案历史可审计。重编号的独立评审零编号错误、三项阻塞文本修订，均已应用并复核：I72 行改写为其改造后的匹配器加强范围（原范围已被 I65 落地）、状态段在制声明更正为真实的七行在制态（I73 已落地）、编号注记表补全三行收尾映射以闭合编号复用歧义。修订后并入所有者委托处置（2026-07-27）：I82 激活门携带 I80 缺口矩阵处置（gpu_host 格 HELD；发射后格绑定红线、控制器移植为独立工作项），I86 门记录 I77 的 no-mapping 签核预期使其 held 收口。计划 README EN/ZH 在相关权威下增队列链接。4 文件 + 队列对 + 注册表。 | 独立评审（对照 fc574f96 草案、落地台账、T6 残余台账与在制工作树）：needs-repair——重编号本身穷举验证正确（每个行号、依赖、激活门、候选改写、收尾区间与 EN/ZH 行等价零编号错误；bundle-4 更正与落地波次叙述与台账一致），三项阻塞文本修订：I72 行的已被满足范围、陈旧在制声明、编号表不完整。三项均按指定原文应用（另加评审者建议的基线提交头与最后核验刷新）并经修订轮复核：无残余声明与落地台账矛盾、EN/ZH 表行数相等、编辑限定于指定行。落地树验证：文档链接审计 180 文档 / 2,776 链接 / 0 issues，双语 cluster 对已登记，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I91 T9 所有者委托签核记录与 I80 处置并入 | `accepted` | `bf60b8cbfeb9bf3f3c47bbc849c013b4575bb9be`（`codex/redundancy-consolidation`） | 所有者委托裁定记录（明示授权"允许代签"，2026-07-27）：I77 表征裁定文档对增第 5.1 节——带日期的签核小节，如实声明其性质（所有者委托的代理裁定，非独立人类 C2 专家评审），并记录三项判定，每项在落笔前对照被引代码、普查与仓库 C2/条令参考重新推导：(1) 无隐式梯队->动作接口映射——no-mapping 判定作为维护契约成立（AFDP 3-0.1 的显式委托框架；A3 将每个任务令默认为 TACON/Tactical 而无编译判定逻辑读梯队值，隐式蕴含将使几乎所有 agent 默认获得接口权限）；任何未来映射必须经专门领域证据切片以显式注册结构（G5）到来。(2) 所有权问题因 (1) 而空置；`agency_registry.py` 预指定为若引入时的声明所有者（:604 的 CATEGORY_SCOPE 并列披露为共声明而非映射）。(3) A13 身份毗邻为共享世界实体 id 空间的巧合，由门监控（未来交叉读必须命名判别标识符、已落地边界门即变红）。队列后果记录：T9 行为行（I86）按其行内逻辑预期 held 收口。四处中文措辞混同（task-order 对 mission-command）同轮修正——评审标记的两处加第 3 节新发现的两处。仅文档；2 文件。 | 裁定在记录前实质开展（每判定的证据清单列于节内）：受托代理阅读了 common_core_profile.py:191-259、空/地 profile 仲裁点、air_combat_event_action.py:167-169、adapter.py:433-446/:591-600、policy_contracts.h:418-476、agency_registry.py:604、普查相关节、docs/standards/services/air_force.md（AFDP 3-0.1）与 docs/forward/c2/，独立同意全部三项建议立场，并补充决定性结构论点：A3 无处不在的 TACON/Tactical 默认使隐式蕴含在条令上坍缩。诚实约束核验：记录点名委托性质、从未声称人类专家。编辑后链接审计 180 文档 / 2,776 链接 / 0 issues；EN/ZH 逐段等价确认；`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I78 T1/T10 切片：谱系词汇模式所有者与接缝采纳 | `accepted` | `cb3d673a6dcd69d2829f43d85e979da56bbe137e`（`codex/redundancy-consolidation`） | 统一架构计划 T1/T10 切片（队列的谱系词汇行，T10 普查 VA-6）：C++ `ScenarioGenerationEvidenceMetadataRef`（3 字段）与 `ScenarioGenerationRequestMetadata`（13 字段，ABI 成员序）的谱系词汇转为模式所有。两个新 dto_schema 模块生成双面——`src/runtime/contracts/detail/` 下的 C++ .inc X-macro 片段与 `gym_envs/scenario_loader/_generated/` 下的 Python 构建器——保持字段名、顺序、默认（含 `contract_version = kScenarioGenerationContractVersionRequestV1`）与序列化。`counterfactual_replay_contract_types.h` 的手写结构体接缝转换到生成 include（落地 I69 基线 + 恰两处结构体体交换；I69 的 MaintainedReplayEnvelopeResult 块逐字保留）。跨语言平价门（`test_scenario_generation_lineage_parity.py`，已注册冒烟）断言接缝采纳（include 旁或替代处的手写成员即红）加模式==.inc 字段相等，字节精确新鲜度由 `generate.py --check` 独立所有。held 判定记录于模式源：C++ 专属 `has_deterministic_seed` 在场标志、字段集确实不同的平行工件包装、Python 面 `__post_init__` 归一化。12 文件。 | 两轮评审。第 1 轮（模式半件，即已批准的 partial）：字段表逐字段对照手写结构验证（成员序即 ABI）、generate --check 绿（233+4 工件最新）、平价门证明对名/序漂移变红、held 判定记录未静默吸收、运行时导入方向零变更。第 2 轮（接缝增量）：approve、零阻塞——转换后头文件与 9362a136/7e5372d1 落地基线（除 CRLF）逐字节等价、仅恰两处结构体体交换，I69 块逐字；.inc 字段名/型/默认/序与被移除手写成员精确一致；'= ""'/'= {}' NSDMI 替换在 C++20 下观测等价（含 designated-initializer 消费点）；四路变异探针（scratchpad 副本、仓库未动）证门承重：include 旁手写成员、接缝回退、.inc 重排、默认漂移全部变红；门 19 passed 重跑、generate --check 干净含两新模式/构建器。非阻塞：'18 passed' 计数为不含新鲜度门的组合（记账而非实质高估）；接缝断言比目标更严（结构体花括号内注释也红——fail-closed 可接受）；宏无 #ifdef 守卫与既有 effects_event 先例一致。落地树验证：全量重建绿（头文件改动重编 15 TU），ef_test 19,103 断言，ctest 8/8，谱系/新鲜度/场景生成门 15 passed，generate --check 干净，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I79 T10 切片 6A：维护交战包谱系生产者 | `accepted` | `f9769a51f92201ca00efd3b4a6695244a191b7fa`（`codex/redundancy-consolidation`） | 统一架构计划 T10 切片 6A（队列的 packet-ancestry 行）：`RuntimeFacade::build_maintained_packet_ancestry` 经加法、opt-in 路径填充交战包谱系，精确镜像 I69 生产者结构（加法 DTO 块 `MaintainedEngagementPacketAncestry` + `MaintainedPacketAncestryResult` 零重排追加；头声明 + counterfactual 实现 + 内部常量 + nanobind + 以 I59 `use_facade_evidence_producers` opt-in 门控的 adapter 接缝）。四个 fail-closed 门：(1) 窗口的维护回放信封必须构建并通过校验（逐字复用 I69 生产者及其九门，`ancestry.replay_envelope_ref` 绑定到校验器接受的信封）；(2) `parent_trace_id` 必须由本 facade 的 VA-8 分配器铸造——外来 facade 父项以具名理由 fail-closed；(3) 父项必须严格先于每个窗口 trace 标签（无自指/前向链接）；(4) 至少一条导出诊断 trace 携带运行铸造的包标签（值不可分辨 id 空间的判别器）。填充返回窗口导出 DiagnosticsTrace 族的父链接副本——窗口产品不动、`parent_trace_id=0` 保持根默认、默认序列化面字节一致。类型化谱系引用采用 I78 词汇（`ref_id`/`evidence_kind`/`provenance_label`）于新的不相交 id 命名空间。冒烟豁免循 I59/I69 真实运行先例、记录于测试 docstring。7 文件。 | 独立评审（EF-w16 @ 8361a818，对落地头碰撞核查）：approve、零阻塞。ABI：DTO 块严格追加、既有成员零重排/改型（对 HEAD diff 验证）；默认路径字节一致：维护运行时无人调用生产者（grep 调用点）、实现验证为副本而非变异；四门 fail-closed 逻辑逐一核查（门 2 分配器准入镜像 I69 模式、门 4 标签集判别器成立）；pytest 3 通过 + 11 绑定门控 skip 诚实（谓词基于绑定在场、非无条件，合 T6 治理）；I78 类型化引用字段名与 EF-w14 模式模块逐字段一致；非阻塞：构建者措辞将 I78 称为已落地（当时未落地、现已落地——实质无分歧）、门 2 的 [1, cursor) 界与 I69 同型（普查 VA-8 固有）由门 4 独立补强。落地树验证（重建 pyd 后）：ancestry + 信封 + trace 门 + counterfactual 套件 52 passed / 1 环境性 skip（11 个绑定门控 skip 构建后全部转绿），ef_test 19,103 断言，ctest 8/8，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I81 T1/T3 切片：I41(f) 合同边界边以 held with evidence 收口 | `accepted` | `e17f6c532c97b9dd67cb49d4334cdd2b71adafc5`（`codex/redundancy-consolidation`） | 统一架构计划 T1/T3 切片（队列的合同边界行）：I41(f) 边——`WorldExecutionEpisodeStepRequest` 向任务评估借用 `StepEvaluationBatchConfig`/`StepEvaluationBatchEnvState`——按该行自身的登记分支以 HELD WITH EVIDENCE 收口。T1 模式所有权路线经一手评估、三条源码验证论据下被排除：(1) 被借类型非叶闭合（`StepEvaluationBatchEnvState` 按值内嵌十个任务所有聚合，`ExecutionEpisodeState` 内嵌 core/geometry 的 `SpatialRouteWaypoint`，contracts 位置的定义将需要 contracts->core_geometry 边——超出 I41 普查的新违例类；contracts 策略目标集仅 {components}）；(2) include 方向扫描器把 `.inc` 文本包含计为边，模式生成的字段表包含任务所有 .inc 会在新指纹处重造违例——字节等价字段表生成可行、字节等价包含图闭包不可行；(3) 迁移唯一叶闭合片段（`StepEvaluationBatchConfig`）无法收缩 allowlist——env_state 仍强制该包含，纯折腾且暴露 ABI。冻结工件：包含点带日期裁定注释（仅注释头编辑，include 移至 34 行）、allowlist 条目重裁定至 v3 并带精确机器钉定退出条件（T1 DTO 族完成迁移 + 57 绑定平价）、冒烟注册门中的新 held 边钉测试（条目唯一性、裁定标记、15+42=57 def_rw 拆分）、T6 台账 7.5 节 EN+ZH。allowlist 按设计保持 5 条；依赖未反转。6 文件。 | 独立评审：approve、零阻塞。三条排除论据逐一对源验证（十个任务所有按值聚合在 execution_episode_batch_prepare.h 确认；ExecutionEpisodeState 第 8 行包含 spatial_query_runtime.h 且第 19 行内嵌 SpatialRouteWaypoint 向量；策略目标集 {components} 于 cpp_include_graph.py:307 确认；SOURCE_SUFFIXES 含 .inc 于 :35 确认且 flight_shaping_shared_fields.inc 双展开确认；叶闭合片段论证成立）。held 边钉测试承重：指纹为 file:line:include 且有未覆盖违例与陈旧条目双分支，8 通过自身即证负向演练两个方向。def_rw 普查独立重数 15+42=57。头编辑纯注释 18 行零代码 token。v3 退出条件精确且被测试机器钉定。台账 7.5 EN/ZH 对齐。工作树侧冒烟 642/4/45 独立复现。非阻塞：落地增量三处（台账 §10 与 §7.5 区域不同、三方合并干净；台账对哈希落地侧重刷；冒烟总数落地口径不同）、全仓 ruff 落地侧确认、v3 命名与 next_gate 措辞小注。落地树验证：include 方向门 8 passed，generate --check 干净，ruff 干净，`git diff --check` 干净；台账对哈希选择性刷新已在落地侧重跑。落地树 maintained smoke 见下文。 |
| I92 T8 跟进：待办消费者 G4 声明与登记定稿 | `accepted` | `f546fd74b1afc3888089edfba29a1f697d590469`（`codex/redundancy-consolidation`） | 统一架构计划 T8 跟进，闭合 I76 记录的两个开放问题。(1) 两个 G4 声明待办观测消费者（`python/rl/runtime/world_batch/_vec_env_support.py`、`observation_batching.py`）现携带模块级 G4 声明（CONSUMED = World Truth、PRODUCED = Agent Observation、SEMANTIC_STAGE = P10 ObservationExport，镜像 I45/I56 延迟读者风格）并注册于 `DECLARED_DEFERRED_INFORMATION_LAYER_CONSUMERS`；I76 分类器的 `G4_DECLARATION_PENDING_CONSUMERS` 钉清空、两文件迁至定稿分类。仅声明——两消费者行为零变更（常量与注释）。(2) I63 T8 登记的观测面逃生舱行翻转为分类器闭合，引用已落地 I76 门为闭合证据，附带日期跟进小节 EN+ZH。7 文件。 | 独立评审：approve、零阻塞。两消费者文件逐行 diff 验证仅声明（常量/注释、零行为）；注册表迁移与落地 I76 形态一致（blob 谱系核查）；I63 清单翻转准确引用落地 I76 门、EN/ZH 等价；information_state 79 passed 复现（74→79：+2 声明门参数化 +2 延迟原始读参数化 +1 定稿钉）。落地树验证：information_state 门 79 passed（翻转钉全绿），ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I93 T5 切片：协同飞行整形矩阵单源化 | `accepted` | `bf9f5584a115b9419ce23e30a58964c2e6c9c4f0`（`codex/redundancy-consolidation`） | 统一架构计划 T5 切片：协同飞行整形配置矩阵转为实验推导。全部 12 个活动根文件（9 个 cooperative_* + 3 个 p4b_*）现经 I30 机械从类型化 Experiment 定义（`python/experiment/cooperative_flight_matrix.py`）逐字节推导，采用两个配置基（组合不能删键、p4b 车道缺协同块）加字面覆盖拼写（尾零浮点与普通十进制学习率）；共享投影逻辑抽取到 `python/experiment/matrix_projection.py` 并被空战矩阵复用（实验类型语义无分叉）。生成器增 opt-in `--matrix cooperative_flight` 车道；CLI/配置路径零变更；examples/config 零编辑（生成与入库文件字节一致）。新鲜度门（`test_cooperative_flight_matrix_freshness.py`，已注册冒烟）证明 12/12 字节一致、手工编辑即红。6 文件。 | 独立评审：approve、零阻塞。12/12 字节一致自行以 --matrix cooperative_flight 运行核实且 examples/config 零 diff；双基 + 字面覆盖设计对照 I30 空战基核查无语义分叉（投影逻辑共享抽取）；新鲜度门对手工编辑变红验证；无 CLI/配置路径变更；冒烟覆盖核查；p4b 车道分类声明核实。落地树验证：两个生成器 --check 绿（空战 24/24 + 协同 12/12），矩阵门 19 passed，examples/config 零漂移，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I94 T11 第四切片第五包：engine-tuning 解析表驱动 | `accepted` | `719955dea25f17f66e605a50697bcb6b9dbb3355`（`codex/redundancy-consolidation`） | 统一架构计划 T11 第四切片第五包：`parse_unit_json` 的 16 个纯机械 EngineTuning double 读迁到单源 X-macro 表 `src/content/detail/engine_tuning_fields.inc`，在原始接缝处 token 一致展开。`enabled` 以两条验证过的理由保持手写：其字面 `true` 默认对 engine.tuning/engine_tuning 预设-再-合并编解码逃生舱承重，且顶层调用点按 `tuning.enabled` 分支决定重播种。与 aero/missile 先例的记录偏差：单宏、单遍 include（EngineTuning 无向量成员）；守卫中的纯标量类型集演练在未来出现向量成员时强制重裁定。三个 C++ doctest 用例循 I61/I66 模板（16 全哨兵平价 + enabled 字面默认；稀疏预设种子存活；经不同 nlohmann type_error 消息的畸形 fail-first 探针），三腿 pytest 守卫（声明锚 == .inc == 钉定表、双向默认平价、单遍钉、残留 + 引号字面带、12 负向演练），已注册冒烟。5 文件。 | 独立评审：approve、零阻塞。16 展开 token 一致对 git diff HEAD 重推导（6 样本 + 全名/序核对）；enabled 保持的两条理由在 loader :250/:770/:779-784 逐一验证；单遍偏差经结构验证（无向量成员）并被类型集演练钉定；守卫三腿完整、enabled 移除演练正确以签名匹配限定于 engine 助手体（避开 aero 助手同形行）；doctest 探针以不同 type_error 消息钉顺序、loader 无 try/catch 异常可达；冒烟注册邻接 aero 条目；22+79+5 测试独立复现。非阻塞：构建者计数 11 实为 12 个负向演练（少报）；两个移除演练形状较弱（正向门实际承重）。落地树验证：ef_test 19,137 断言（三个新 engine 用例绿），ctest 8/8，content 守卫 99 passed，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I85 T11 切片：typed_platform_request 背后的潜艇能力束试点 | `accepted` | `db0e6bd13aa4ef175e5d0967ba96483842366a89`（`codex/redundancy-consolidation`） | 统一架构计划 T11 切片（队列的能力束试点行）：内容能力束成为有界潜艇平台族在 `typed_platform_request` 背后的真源，`spawn_unit` 保持参照路径。新 opt-in 内容面 `python/content/capability_bundles/`（stdlib-only、单向朝运行时）：带版本的模式 `t11.content_capability_bundle.v1`（fail-closed 诊断恒携版本令牌）；G5 注册插座 `CapabilityBundleFamilyRegistry`（初始为空、重复注册即败、导入 `submarine.py` 即 opt-in）；产生维护 `typed_platform_request` 语义的潜艇展开器；以及把数据类树转为 ef_py DTO 的绑定适配器（`ef_py` 以参数传入——内容包永不导入绑定）。真源文档 `data/submarine/kilo_class_mvp.bundle.json` 声明精确能力构成。两级平价证据：对工厂能力束模板事实的有界纯 JSON 再推导（逐字段含证据序），以及实体级 fixture 在同种子单 facade 批中经参照 `spawn_unit` 路径（世界 0）与内容展开类型化请求（世界 1）生成 `Kilo_Class_MVP` 并比较全公开观测面。回滚壳：漂移门钉定默认路径对新机械零引用；`examples/config/**` 零编辑。13 文件。 | 独立评审：approve、零阻塞。回滚壳/opt-in 自行运行守卫并 grep 生产面零 python/content 导入；实体级平价 fixture 验证真实驱动双路径（spawn_from_request 世界 0 对类型化请求世界 1、同种子）并运行通过；束 JSON 对 DefaultUnitFactory::build_platform_capability_bundle_template 逐字段核实含证据序；fail-closed 诊断恒携版本令牌、注册表空启动且重复注册即败；examples/config 零编辑经 git status 确认；stdlib-only 与单向导入纪律核查（ef_py 仅经适配器参数）；三个测试文件冒烟注册且运行成本适度；G5 一致性成立（第二个族经注册接入、无需编辑共享代码）。非阻塞：回滚门扫描根未含 scripts/game 等（记录）；有界镜像保真边缘（构造性文档情形）；PATH ruff 版本注记。落地树验证：能力束套件 16 passed（模式、实体级平价、回滚守卫），examples/config 零漂移，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I95 T3 第四切片：ef_facade 物理链接单元拆分 | `accepted` | `9ab17d455ea3cf7f3e3d8fb606a8d2a8f672c999`（`codex/redundancy-consolidation`） | 统一架构计划 T3 第四切片：ef_core 的第二次物理链接单元拆分，循 I46 ef_content 先例但依赖方向反转（facade 位于 core 之上）。裁定记录：计划点名的剩余单元中，facade 是当下唯一无环可拆项——engine 与 mission 经 I41 棘轮的 `world_batch_runtime.h -> execution_episode_controller.h` 边符号成环（engine 按值持有 mission 控制器；顺延至 T4 批包装设计），engine 另携三条棘轮 engine->gpu 边。实现：`${EF_RUNTIME_FACADE_SOURCES}` 移出 `ef_core`；新 `add_library(ef_facade STATIC ...)` PUBLIC 链接 ef_core 及其第三方集（字节一致编译环境）；ef_core 不链接 ef_facade（回边即声明环——G2）；两个 facade 消费者重指向（`ef_py` 链接 `ef_facade ef_core` 保持 MinGW 单遍回引序；`ef_test` 同理）；ef_app 与 gpu 目标验证无 facade。零源文件移动；include 拼写不变；include 方向 allowlist 未动（facade 零入边性质已有门强制）。门：冒烟注册的 `test_cmake_target_readiness.py` 增十一个测试（经 I46 解析器的五个方向反转 CMake 不变量、金样 fixture、六个负向变异、两个隔离扫描：facade 文件仅可经声明接缝头 `runtime_facade_internal.h` 包含 core/engine 内部，且该接缝头不得离开 `src/runtime/facade`）。src/README 对中陈旧的单目标段等价更新。4 文件。 | 独立评审：approve、零阻塞。裁定核实（world_batch_runtime.h:12 包含并按值持有控制器向量；allowlist 恰 5 条含该边与三条 engine->gpu；facade 零入边经全仓 grep 与既有门确认）；CMake 变更逐项核查（源组移出、STATIC 新库、第三方集一致、无回边、消费者重指向、MinGW 链接序推理评估）；十一个新测试承重（五不变量 + 金样 + 六变异 + 两扫描、注入违例自检）；接缝头唯一性经 grep 验证；README 对等价更新。非阻塞：报告'13 门'计数应为 11 个新测试（实质无缺）；构建者全 architecture 扫未归（落地补跑）；FACADE_PERMITTED_CALLERS 与链接集的宽容差异记录。落地树验证：拆分下 ef_core/ef_facade/ef_py/ef_test/ef_app 全量重配置+重构建绿，ef_test 19,137 断言，ctest 8/8，构建系统 + include 方向 + runtime_facade 门模块 108 passed，运行时冒烟成员 39 passed，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |
| I86 T9 行为切片：按 no-mapping 判定以 held 收口 | `held` | 仅收口记录；无提交（判定链：I77 `45ce6c35`、I91 `bf60b8cb`） | 队列行 I86（首个行为性 Agency 切片）按其行内逻辑以 HELD 收口：该行激活门要求 I77 批准的梯队->动作接口映射，而 I77 裁定——经 I91（`bf60b8cb`）落地的所有者委托签核确认——全路径 no-mapping（正向 5/5、反向 2/2；两套表征作为刻意正交维护）。按队列红线（"任何 T9 行为迭代不得始于名称相似；表征判定为强制前置"）与该行显式分支（"若 I77 返回 no-mapping，I86 改为 held 收口"），不存在行为工作项。重开条件：未来领域证据切片经 `agency_registry.py`（预指定声明所有者）引入显式注册映射时，以新编号重激活该行。零代码变更；本行即收口记录。 | 按规则收口：判定链为 I77（45ce6c35，独立评审 approve、no-mapping 判定经抽验）-> I91（bf60b8cb，所有者委托签核含三问记录与队列后果句）-> 本登记行。已落地边界门（`tests/architecture/agency/test_authority_representation_boundary.py`，17 测试，已注册冒烟）使 no-mapping 状态受机器强制，held 收口是被监控的而非口头的。除常设门外无需额外验证运行。 |
| I82 T3/T4 切片：覆盖格控制器默认解析、翻转因性能证据 held | `accepted` | `b68ae2d9`（`codex/redundancy-consolidation`） | 覆盖格解析以去武装状态落地：所有未设默认仍走 Python 路径，显式 True/False 语义不变；性能证据不足时不武装翻转。 | 独立评审 approve、零阻塞；平价 + world-batch 冒烟成员 50 passed / 4 缺口格 skip，ruff 与 `git diff --check` 干净。 |
| I84 T10 切片 7：维护世界线对比生产者 | `accepted` | `915996df`（`codex/redundancy-consolidation`） | 统一架构计划 T10 切片 7（队列的世界线对比行）：`RuntimeFacade::build_maintained_worldline_comparison` 将单一 facade 运行的两个维护窗口连接为 opt-in 证据级对比，逐侧消费 I69 回放信封与 I79 包谱系。加法 DTO 对（`MaintainedWorldlineComparison` + 结果包装，零重排追加）仅携带证据 id——不提升真值红线记录于 C++ 类型并由门强制：两侧信封须过切片 5 校验器（含 VA-8 本 facade 铸造准入）、两侧谱系须过切片 6A 门、确定性回放引用在场、同 facade/同 run 关联必需，`promoted_to_support` 恒假且 `truth_claim` 限于证据对比词汇。Python 接缝在 I59 `use_facade_evidence_producers` opt-in 之后；新的不相交 id 命名空间（`comparison:maintained:*`/`worldline:maintained:*`）；默认路径字节一致（零接线、只读生产者）。真实运行 pytest 循 I69/I79 惯例、逐门负向扰动；冒烟豁免循同先例、理由在 docstring。落地按预判以并集解决了与已并入太阳字段绑定区的碰撞（双注册、已格式化）。7 文件。 | 独立评审：approve、零阻塞。ABI 加法性对工作树基线与落地头双重验证（太阳字段区碰撞经 merge-tree 评估并在落地实测解决）；fail-closed 门含两侧信封准入与同 facade/同 run 强制核查；不提升真值核实（仅证据 id）；默认路径字节一致（grep 零调用点、只读实现）；pytest 跳过谓词基于绑定在场（非无条件）；I78 类型化引用字段逐一致。非阻塞：10 个绑定门控测试在波次头二进制上跳过（协议固有——落地构建后须转绿，已实证）、外 facade 负向测试的窗口数效率注记、报告路径小误。落地树验证（重建 pyd 后）：世界线 + 谱系 + 反事实套件 42 passed / 2 环境性 skip（绑定门控测试构建后全绿），ef_test 19,137 断言，ctest 8/8，ruff 干净，`git diff --check` 干净。落地树 maintained smoke 见下文。 |

| I83 T2 切片：`WorldBatchCore` 第一切片 | `accepted` | `b9ebbced`（`codex/redundancy-consolidation`） | 仅从已度量的共用 execution/observation seam 抽取 `WorldBatchCore`；single、leader、cooperative 调用者保留兼容路径，核心不含投机性 plugin 方法。 | 独立 agent 评审 PASS：提取范围仅限已度量接缝；136 个定向测试通过。 |
| I87 T8 切片：类型化 observation-view 试点 | `accepted` | `18abf5e7`（`codex/redundancy-consolidation`） | 一个有界 TL13 消费者族开始消费类型化 observation view，不再只把 `ObservationViewSpec` 当元数据：C3/C20 移除叶级 `truth.x/y` 读取；opaque whole truth object 仍进入 compiled kernel，TL13/proxy 不扩面。 | 独立评审证据记录为 68 个聚焦用例通过。落地验证：maintained smoke 752 passed / 4 skipped / 45 subtests；`ef_test` 143 cases / 19,147 assertions；文档登记 89/89 synced；链接审计 180 documents / 2,782 links / 0 issues。 |
| I96 PR-bot 修复：畸形 capability-bundle bool flag | `accepted` | `0a50ff8c`（`codex/redundancy-consolidation`） | PR-bot 修复，不是重新编号的 I72-I90 队列项：bool field 必须为真实 boolean，unsupported 结果必须携带非空 `unsupported_reason`。 | 落地前独立 agent 评审 PASS（候选 `37ab60a1`）：核对 bool 类型与 `unsupported_reason`；28 个聚焦测试、ruff 与 `git diff --check` 通过。 |
| I97 PR-bot 修复：T6 校准残差 xfail | `accepted` | `cbc72768`（`codex/redundancy-consolidation`） | PR-bot 修复，不是重新编号的 I72-I90 队列项：聚焦七项校准后的残差预期，并记录与 T6 §11 的关系。 | 评审证据有意保留限制：实际序列为 3 pass + 7 xfail，`runxfail` 输出 `FFFFFFF`。Windows binding 进程退出时挂起，pytest 未打印 summary；本行不声称套件完成或干净退出。 |
| I98 PR-bot 修复：opaque facade evidence identity | `accepted` | `b2cf3021`（`codex/redundancy-consolidation`） | PR-bot 修复，不是重新编号的 I72-I90 队列项：将 maintained-window evidence 绑定到 opaque facade identity，不改变原 I72-I90 队列映射。 | 两轮修复后的独立 agent 评审 PASS：先前的 worldline error masking 与 private-field aggregate 问题均已修复；最终候选为 143/143 C++ cases（19,147 assertions）及 engagement/bindings Python 91 passed / 2 expected skips，ruff、`git diff --check`、clang-format 均干净。landing integration smoke 覆盖该接缝。`727193b2` 是谱系 CI gate 的前置/核验修复，不是新迭代编号。 |

### 落地树验证记录（2026-07-26/27 落地波次）

| I89 T7 窄修复包：I88 残余平价与治理收口 | `accepted` | `a272fc04`（落地提交；已评审候选谱系：`bdf03553`、`b2cec611`、`86186e56`、`93f1214c`、`26a64f0a`） | I88 返回 findings 而非 clean。本窄包修复有效 C++/Python `sensor_refs` 分支不一致（空数组抑制 inline sensor；非数组落入 fallback；数组中的非字符串元素忽略；非空字符串数组产生 `sensor_refs`），刷新已接受 I87 的 T8 登记与 T9 证据指针，并为每个非代码幸存面登记 owner 与下一道门。不删除用户生成物/工作树，也不扩张 held 范围。 | 累计候选独立评审 PASS；落地精确采用已评审 blob。候选门禁：22 个聚焦测试、双语 90/90、182 文档/2802 链接/0 issues、ruff 与 diff check 干净。修复后门禁与最终审计由 I90 记录。 |
上表中写"落地树 maintained smoke 见下文"的行即指本记录。波次头 `9ab17d455ea3cf7f3e3d8fb606a8d2a8f672c999`（26 个迭代：I61-I63、I65-I81、I85、I91-I95 及 I70 构建修复，2026-07-26/27 落地）：maintained smoke 701 passed, 4 gap-matrix skips, 0 failed (exit 0)；ef_test 137 用例 / 19,137 断言；ctest 8/8；dto_schema 与实验矩阵生成器 --check 干净（空战 24/24、协同 12/12）；文档链接审计 0 issues；全仓 ruff 干净；`git diff --check` 干净。每个迭代均携带独立评审结论落地；needs-repair 结论按评审纪律在提交前修复并通过确认轮。

当前结算 head `18abf5e7` 包含谱系 CI gate 前置/核验修复 `727193b2`：maintained smoke 752 passed / 4 skipped / 45 subtests；`ef_test` 143 cases / 19,147 assertions；文档登记 89/89 synced；文档链接审计 180 documents / 2,782 links / 0 issues。

### I2 残余在 I3 的处置

- 仓库根 README 对继续直接审阅；维护链接审计已经覆盖它，为一个特例新增第二套
  registry 路径模型不具备收益。
- 独立 realism-authority 内容改为路由到既有 gradient realism、source admission 与
  lifecycle owner，不再重复规则。
- 六个旧分歧通过四个有证据的 baseline 刷新、删除 air-combat 复制的归档细节，
  以及补回 review 路由完成闭环。
- 旧 archive 规范化和 evidence 压缩仍属于 P7，没有混入 I3。

## 未满足额外门槛时不得删除的表面

以下内容不能仅因体积大、陈旧或重复就成为删除候选：

- public 或 compatibility API：必须先有内部及合理外部 caller 的迁移路径和弃用边界；
- 测试：必须由同等或更强行为断言替代，并保留相关 bug 历史；
- frozen config、canonical scenario、accepted evidence、source-rights 记录、
  第三方输入和 provenance manifest；
- maintained release 或 validation 路径消费的 generated artifact：必须先证明可在干净
  环境重建；
- 仅因历史陈旧或篇幅过长的 archive 记录；
- 未准入 tracked 范围的 ignored/private/local 工作区；
- 用户拥有的无关工作区变更；
- 没有 characterization test 的行为：必须先测量并决定意图边界。

删除任何 evidence 或 archive 包之前，必须扫描 consumer/reference，审查权利和
provenance，并证明更小的保留集合仍支撑同一有界 claim。

## 提交与审阅纪律

- 除非用户明确改变分支策略，所有迭代都在 `codex/redundancy-consolidation`。
- 一轮迭代对应一个完整提交。
- 独立 reviewer 不得修改被审实现。
- 审阅必须判断功能是否被消除、是否重复已有 owner，不能只看测试是否通过或代码是否缩短。
- 窄测试通过不证明全仓兼容。
- 若本轮发现实质不同的问题，记录为后续候选，不扩大当前 write set。
- 存在未解决 blocking finding、必需验证失败或无法解释的删除时不得提交。

## 停止条件

只有最终审计证明以下条件全部满足，精简计划才能宣告完成：

1. 维护入口均有唯一命名 owner，且没有已知失效内部导航；
2. active tree 中没有已确认的不可达 production body 或无 owner compatibility path；
3. 剩余重复 schema/helper 已有共享 owner，或有明确理由保持独立表示；
4. 剩余 archive/evidence 重复是 provenance、权利、可复现性或有界验收所必需；
5. 剩余候选已被分类为有意、受明确兼容决定 held，或其风险/成本确实高于收益；
6. focused test、maintained smoke/build gate、文档 gate 和最终独立审阅全部通过；
7. 最终残余报告明确记录了有意保留内容及理由。

最后一次实质整合后至少连续运行两轮残余审计。第二轮不得发现新的高置信、可安全
整合候选。没有文本重复不等于完成；还必须审计 caller、行为、文档和 evidence owner。

若进展需要外部兼容决定、不可获得的 source rights，或仓库内无法 characterization
的行为，应把候选标为 `held` 并写明缺失 authority。不得为了减少行数而删除。

## 相关权威

- [文档生命周期规范](../../standards/governance/document_lifecycle_policy.zh.md)
- [Agent 文档权威索引](../../agent/rules/document_authority_map.zh.md)
- [标准维护政策](../../standards/governance/standards_maintenance_policy.zh.md)
- [Subagent 使用规范](../../standards/governance/subagent_usage_policy.zh.md)
