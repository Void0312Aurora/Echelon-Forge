# Repository Consolidation Plan

Language:
- English canonical: `README.md`
- Chinese companion: [README.zh.md](README.zh.md)

Document kind: `plan`
Lifecycle: `maintained`
Canonical: `docs/plan/repository_consolidation/README.md`
Owner: `repository consolidation workline`
Last verified: `2026-07-18`

Status: active consolidation plan on branch `codex/redundancy-consolidation`.

## Objective

Reduce redundant code, duplicated authority, compatibility residue, oversized
navigation surfaces, and documentation drift across the repository. The work
may adjust architecture when the evidence supports it, but it must preserve
maintained behavior, explicit compatibility commitments, tests, provenance,
and bounded capability claims.

All consolidation iterations remain on the same branch. Each iteration produces
one independently reviewable commit and must be accepted before the next
iteration changes the same surface.

## Iteration Protocol

Every iteration follows this sequence:

1. **Analyze**
   - inspect the current worktree and callers;
   - identify the duplicated owner or obsolete path;
   - define the behavior and evidence that must survive;
   - freeze the write set and non-goals.
2. **Implement**
   - make the smallest coherent architectural change that moves toward one
     maintained owner;
   - migrate callers before removing compatibility or history;
   - do not mix unrelated cleanup into the iteration.
3. **Validate**
   - run focused tests for every touched behavior;
   - run structural, link, lint, build, or smoke gates appropriate to the risk;
   - record commands and outcomes before review.
4. **Independent review**
   - dispatch a separate subagent that did not author the change;
   - review the frozen diff for behavior loss, removed functionality, caller
     survival, compatibility, documentation authority, test adequacy, and any
     unnecessary parallel mechanism that duplicates an existing owner;
   - the reviewer is read-only and returns blocking findings, non-blocking
     findings, and a verdict.
5. **Repair and re-review**
   - fix blocking findings and rerun affected validation;
   - material fixes require a final independent re-check.
6. **Commit**
   - commit only after the final diff has no unresolved blocking review finding;
   - use one commit for the iteration and record its evidence in the iteration
     register below;
   - do not amend an earlier accepted iteration to hide later repairs.

No implementation change made after the final review may be included in the
commit without another review pass.

## Required Acceptance Evidence

An iteration is accepted only when its handoff identifies:

- exact write set and non-goals;
- caller or consumer inventory for removed/moved surfaces;
- focused validation commands and results;
- broader maintained smoke/build result when production behavior changed;
- line/file delta and remaining duplicate owner, if any;
- independent reviewer identity, reviewed revision/diff, findings, and verdict;
- explicit statement of preserved behavior and retained residuals;
- commit hash after commit creation.

Documentation-only iterations must additionally prove local links for the
touched entry surface, bilingual companion consistency where required, and
`git diff --check`.

## Candidate Priority

The list is a routing order, not pre-authorization. Every candidate requires a
fresh source and caller audit when its iteration begins.

| Priority | Candidate | Intended result | Main risk |
| --- | --- | --- | --- |
| P0 | Correctness defects exposed by duplicate representations | Fix incorrect behavior before mechanical deletion. | A compatibility-preserving patch can still change default semantics. |
| P1 | Unreachable implementations and exact helper duplication | Remove dead bodies and establish one runtime/test helper owner. | Hidden construction or import patterns may bypass normal callers. |
| P2 | Documentation lifecycle and consolidation governance | Establish executable classification, review, archive, and evidence rules. | A policy-only change can add another unused authority unless indexed and enforced. |
| P3 | Maintained navigation and compatibility shims | Repair entry links, migrate internal callers from shims, and leave bounded external compatibility facades. | Moving or deleting a shim before caller migration breaks users not visible in tests. |
| P4 | Repeated Python taxonomy, geometry, objective, C2/ROE, and bootstrap helpers | Create one semantic owner with scalar/tensor or runtime/compiler parity tests. | Similar-looking helpers may contain intentional semantic differences. |
| P5 | World-batch, step-evaluation, effects-event, and configuration schema duplication | Replace field-by-field synchronization with shared payloads or explicit adapters. | Public bindings, ABI shape, serialization, and report schemas may change. |
| P6 | Python package cycles and C++ target/layer boundaries | Enforce dependency direction and smaller build/runtime ownership units. | Broad import, linker, initialization-order, and performance regressions. |
| P7 | Documentation compaction, archive normalization, and evidence manifests | Make maintained navigation small; collapse duplicate archive layers only after provenance mapping. | Path moves can break tools, tests, historical references, and rights evidence. |
| P8 | Final repository-wide residual audit | Classify every surviving duplicate as intentional, held, or not cost-effective. | A scan can miss behavioral duplication or overstate textual similarity. |

## Iteration Register

| Iteration | Status | Commit / branch evidence | Scope and result | Validation / review evidence |
| --- | --- | --- | --- | --- |
| I1 Runtime and test infrastructure consolidation | `accepted` | `aaec45882173d57c679e3e7233a81980ee9d8fdc` on `codex/redundancy-consolidation` | Fixed sparse missile-tuning override semantics, removed the unreachable `UniversalEnv` body, centralized `ef_py` runtime bootstrap and suite-manifest parsing, and consolidated repeated test helpers. Net change: 1,413 insertions and 2,390 deletions. | Focused regressions and the maintained smoke suite passed (`361 passed`, `41 subtests passed` in the final iteration closeout). An independent review reported no unresolved blocking finding before commit. |
| I2 Documentation lifecycle and consolidation governance | `accepted` | `c844bd900856682f18d6dc72fcb442b95e75c18a` on `codex/redundancy-consolidation` | Added lifecycle and consolidation authority, one shared maintained-document scope, a strict link audit, selective bilingual-registry refresh, and link-safe repairs without a baseline allowlist. The strict registry now covers 76 pairs. Final diff: 57 files, 1,960 insertions, 427 deletions. | Default audit: 155 documents, 2,592 internal links, zero issues. Focused governance tests: `15 passed`; maintained smoke: `371 passed`, `41 subtests passed`; Ruff and `git diff --check` passed. Registry: 70 synced, 6 preserved legacy divergences, 1 legacy missing English companion. `iteration2_independent_review` found two archive-authority blockers; both were repaired. Final `bilingual_registry_audit` re-review left no unresolved blocker. |
| I3 Documentation de-duplication and bilingual residual closure | `accepted` | `d0dbf0d7ee68643baa30d41e66e3955407d3a3ba` on `codex/redundancy-consolidation` | Rejected a proposed root-README registry special case, compressed the duplicate realism-authority standard into a compatibility routing pair backed by existing owners, removed copied archive implementation detail from the air-combat index, restored the missing review route, and refreshed four verified legacy baselines. Final diff: 17 files, 184 insertions, 192 deletions (net -8). | Registry: 77/77 synced with zero missing peers or drift. Link audit: 156 documents, 2,590 links, zero issues. Focused governance: `25 passed`; maintained smoke: `372 passed`, `41 subtests passed`; Ruff and `git diff --check` passed. Independent review found authority and registry-gate blockers; both were repaired, and final re-review approved the candidate with zero blockers. |
| I4 Scenario compiler compatibility owner consolidation | `accepted` | `afe03257e26f9355013293fb0bace77cfeb4091b` on `codex/redundancy-consolidation` | Replaced the incomplete 64-name manual facade binding with canonical `__all__`-driven forwarding for all 86 exports, migrated 15 maintained callers to `python.scenario.compiler`, and kept tests/archive on the compatibility path. A structural gate prevents new maintained callers from using the facade. Final diff: 22 files, 77 insertions, 86 deletions (net -9). | Direct star-import parity: 86/86 exports with object identity. Focused scenario, world-batch, multi-agent, and architecture tests: `81 passed`; maintained smoke: `374 passed`, `41 subtests passed`; Ruff and `git diff --check` passed. Independent review found one parallel-scan blocker; after reusing the existing full-repository scanner with AST import checks, final re-review approved the candidate with zero blockers. |
| I5 MissionCommand shared-core owner consolidation | `accepted` | `9c999b81` on `codex/redundancy-consolidation` | Made `MissionCommandCore` the equality-capable shared owner, replaced the duplicate directive struct with an alias, collapsed projection and maintained-contract writeback to value copies, and made episode-state equivalence consume owner equality. This fixes four serialized fields that the handwritten comparison omitted while retaining the Python type name; complete and mixed umbrella/core equality are explicitly deleted so domain slices cannot be silently ignored. Final diff: 9 files, 65 insertions, 74 deletions (net -9). | Rebuilt `ef_core`, `ef_py`, and `ef_test`; `ef_test_all` passed. Focused architecture, binding, episode, world-batch, and domain regressions: `119 passed`, `1 skipped`, `6 subtests passed`; maintained smoke: `375 passed`, `45 subtests passed`; Ruff and `git diff --check` passed. Independent review first found inherited partial umbrella equality and missing positive test baselines, then found two mixed umbrella/core comparison directions. All findings were repaired with compile and runtime guards; final re-review approved the candidate with zero blockers. |
| I6 Execution runtime behavior-owner consolidation | `accepted` | `143ee4e9` on `codex/redundancy-consolidation` | Removed the duplicate `execution_frame_runtime.cpp`, moved the retained Frame compatibility symbols into the Episode-owned implementation, and made both APIs share one internal common-products path and one batch scheduler. Public Frame/Episode DTOs, Python names, nominal type separation, the 64-item parallel threshold, ordering, and exception propagation remain intact. Final diff: 13 files, 147 insertions, 151 deletions (net -4). | Rebuilt `ef_core`, `ef_py`, and `ef_test`; `ef_test_all` passed. Focused scalar/batch/fallback/architecture regressions: `46 passed`, `226 subtests passed`; maintained multi-agent/world-batch callers: `71 passed`, `1 skipped`, `5 subtests passed`; maintained smoke: `375 passed`, `45 subtests passed`. Registry: 77/77 synced; link audit: 156 documents, 2,592 links, zero issues; Ruff and `git diff --check` passed. Independent re-review approved the candidate with zero blockers. |
| I7 Mode-choice surface owner consolidation | `accepted` | `587df736` on `codex/redundancy-consolidation` | Made `python.mission_obs_taxonomy` (mission-observation modes) and `python.env_config` (action, execution-step-runtime, step-info, and flight-shaping modes) the single ordered owners for mode surfaces. Training/eval CLI choice lists and validation sets now derive from the owner tuples, removing the five hand-written literal copies in `python/training/cli.py`, `python/env_config.py`, `tools/eval/eval_utils.py`, and `tools/eval/sb3_eval_base.py`. Exported names, choice contents, and ordering are unchanged; new parity tests pin every derived surface to its owner. Final diff: 9 files, 111 insertions, 27 deletions. | Focused env-config/taxonomy, training-bootstrap, and evaluation-CLI regressions: `48 passed`, `2 skipped`, `20 subtests passed`; maintained smoke: `380 passed`, `45 subtests passed` (baseline 375 plus the five new parity guards). Ruff, `git diff --check`, bilingual registry audit (77 pairs), and maintained link audit passed. Independent review approved the candidate with zero blockers. |
| I8 Scalar helper and mode-literal owner consolidation | `accepted` | `aa5b537c` on `codex/redundancy-consolidation` | Established the dependency-free owners `python.angles` (signed/heading wraps, sign-preserving heading error, bearings, planar distance) and `python.coercion` (`coerce_nonnegative_int`), migrating about nineteen maintained angle-helper call sites and all five coercion copies onto them through thin aliases that keep every public name importable. Deliberate variants stay local and test-pinned: the 1e-9 zero-snap wraps, the degenerate-bearing fallback, and the plain-`%360` bearing form with a measured ~1e-13 degree bit-level divergence from the owner form. Remaining runtime mode literals (ScenarioLoader backend checks, world-batch and cooperative vec-env checks, scenario-loader normalizers, diagnostics benchmark CLIs) now derive from semantically matching tuples in `python.env_config`; batch observation and visual backends remain distinct owners from flight shaping despite their currently identical values. The previously reported `universal_env.py` residue does not exist at this baseline. `ModeChoiceSurfaceParityTests` gained an `ACTION_MODES` content pin and quote-agnostic negative guards. Final diff: 33 files, 560 insertions, 119 deletions (code and tests: 27 modified files +117/-115 plus three new files; remainder is this register update and the registry refresh). | New 25-case parity suite compares the owners bit-for-bit against embedded copies of the removed formulas (±180/360/negative/non-finite coverage) and joined the CI smoke manifest. Focused naval/world-batch/multi-agent/leader/scenario-compiler/navigation/eval regressions: `325 passed`, `3 skipped`, `51 subtests passed`. Maintained smoke: `406 passed`, `45 subtests passed` (baseline 380 plus 26 new guards). Ruff and `git diff --check` passed. Independent review reproduced numeric parity on a separate 14k-point bit-level grid with zero failures and approved the candidate with zero blockers. |
| I9 Training and evaluation entry-point consolidation | `accepted` | `c20d2366` on `codex/redundancy-consolidation` | Sank the implementation bulk of the root `train.py` (1,008 to 331 lines) into `python/training/`: lazy SB3/torch dependency loading (`deps.py`), safe action-bias initialization (`action_bias.py`), and vec-env selection/construction (`vec_env_factory.py`). `train.py` keeps a thin `main()`, compatibility re-exports (including the restored `apply_global_seed` that `tools/diagnostics/trace_training_nonfinite_source.py` imports), and the WP24 UniversalEnv retirement guard strings. Checkpoint loading is now owned by the bootstrap-neutral `python.rl.policy_checkpoint.load_sb3_policy`; `tools/eval/sb3_eval_base` keeps a compatibility re-export after its diagnostics-local fail-closed bootstrap. One deliberate behavior improvement is recorded: historical HMoE/Squashed checkpoints that previously failed to load through `evaluate.py` now load via the shared historical policy-class detection. Two end-to-end entry smoke tests pin the evaluate CLI and the `train.py --test_only` path, and a follow-up guard verifies that importing the shared loader does not inspect local-build configuration. Final diff: 14 files, 1251 insertions, 762 deletions (code and tests: 6 modified files +121/-758 plus five new files; remainder is this register update and the registry refresh). | Independent review verified behavior preservation by AST-normalized function-by-function comparison (action-bias values, all vec-env kwargs, `main()` orchestration and log text), confirmed `import train` loads no torch, and byte-compared all four `--help` outputs against the baseline. Focused training/policy/eval/architecture regressions: `251 passed`, `2 skipped` (one pre-existing GBK-console failure reproduced on the unpatched baseline). Maintained smoke on the landed tree: `406 passed`, `45 subtests passed`. Ruff and `git diff --check` passed. Independent review approved the candidate with zero blockers and adjudicated the two new entry smoke tests keep. |
| I10 Flight-shaping field-tax elimination | `accepted` | `c1a8b2f4` on `codex/redundancy-consolidation` | Hoisted the 89 config-static shaping fields shared by `FlightShapingRuntimeInputs` and `StepEvaluationBatchConfig` into one X-macro list (`src/core/mission/runtime/detail/flight_shaping_shared_fields.inc`); both struct field blocks, the batch-prepare field copy, and the FlightShaping `def_rw` bindings now expand from that single owner. Field types, names, defaults, ordering, and the 118-attribute Python input surface are unchanged; the two mission-dynamic `target_*` fields stay hand-written to preserve member order. Final diff: 8 files, 136 insertions, 373 deletions (code: 5 files, +128/-367, net -239; remainder is this register update and the registry refresh). | Rebuilt `ef_core`, `ef_py`, and `ef_test` in an isolated worktree; `ef_test_all` passed. Field audit: 118/118 and 112/112 identical (type/name/default/order); old-vs-new build parity probe produced byte-identical 546-line output. Focused execution/facade/world-batch/architecture regressions: `282 passed`, `1 skipped` (the pre-existing window-loop string-style and local winsock link failures reproduced unchanged on the unpatched baseline). Maintained smoke: `380 passed`, `45 subtests passed`. `git diff --check` passed. Independent review found one register-accounting blocker; after the register repair, the final re-check approved the candidate with zero blockers. |
| I11 Effects-event field-surface owner consolidation | `accepted` | `cc3bdf1a` on `codex/redundancy-consolidation` | Extracted the 135-field `EffectsEvent` inventory into the X-macro list `src/runtime/contracts/detail/effects_event_fields.inc` (48 event-only plus 87 `EffectsResult`-overlap entries) and expanded the contract struct, the `apply_effects_result_fields` projection, and the Python `def_rw` bindings from that single owner. The overlap count corrects the earlier 88-field estimate: `destroy_missile` is a result-only control flag that was never copied. `weapon_launch_adapter.h` (`EffectsEventSnapshot`, `make_effects_event`) was confirmed build-unreachable (text-only shape-test references) and is registered as a deletion candidate, untouched this iteration. The contract shape test now expands the include textually so every field assertion keeps failing on list removals. Final diff: 8 files, 197 insertions, 433 deletions (code and tests: 4 modified files +39/-429 plus the 150-line list; remainder is this register update and the registry refresh). | Independent review verified all 135 fields (type/name/default/order) and the 87/48 split item-by-item against the pre-change sources, re-ran 204 targeted engagement/bindings/air-combat/architecture tests and `ef_test` (113 cases, 18,753 assertions) green, and reproduced the cross-build parity probe (dir() sequence and per-field defaults identical to mainline); verdict approve with zero blockers. On the merged landing tree (I8+I9+I10+I11) the incremental rebuild passed `ef_test_all`, engagement/execution regressions returned `87 passed`, `226 subtests passed`, and maintained smoke returned `406 passed`, `45 subtests passed`. `git diff --check` passed. |
| I12 Runtime bootstrap import-surface migration | `accepted` | `a1c33d43` on `codex/redundancy-consolidation` | Migrated 167 maintained callers (116 tests, 42 tools, 8 python, 1 examples; 174 direct-import sites, the only import form at this baseline) from the compatibility shim `python.testing.runtime` to the canonical owner `python.runtime_bootstrap`. The shim is retained as an archive-only re-export facade with an updated docstring; the eleven `tools/archive/` callers stay on it per the I4 precedent. A new AST governance gate (`tests/architecture/governance/test_runtime_bootstrap_owner.py`, added to the smoke manifest) forbids maintained-surface imports of the shim. Landed onto the I13 head via three-way merge (adjacent import-block edits); byte-code cache noise was excluded from the transplant. Final diff: 173 files, 253 insertions, 183 deletions (code and tests: 169 modified files +181/-179 plus the gate test; remainder is this register update and the registry refresh). | `compileall` clean; both new governance gates pass on the landed tree. Maintained smoke on the landed tree: `410 passed`, `45 subtests passed` (409 baseline plus the new gate). Full-suite run in the stream worktree: `2095 passed`, `3 skipped`, with all thirteen known baseline-red items reproduced; the remaining failures are not import-routing regressions — the `ef_py` import errors were a transient race with a concurrent worktree rebuild, artifact-path and worktree-path parametrization failures are worktree-environment effects, and the `weapon_guidance_realism` suite fails identically (45 cases) on pre-migration baseline trees with matching builds, marking it machine-baseline-red outside the maintained smoke gate. Ruff and `git diff --check` passed. Per owner instruction no independent review agent was dispatched; the orchestrator verified the transplant and reran the landing gates. |
| I13 World-batch vec-env shim call-surface consolidation | `accepted` | `86bd0fcd` on `codex/redundancy-consolidation` | Migrated the maintained call surface (3 production `python/` modules, 5 `tools/diagnostics` scripts, 8 plain-import test files, and 3 monkeypatch-style `tests/world_batch` files) off `python.rl.runtime.world_batch_vec_env` onto the canonical `python.rl.runtime.world_batch.vec_env` and `_observation_mixin` owners, retargeting each monkeypatch site to its true per-symbol owner. `examples/viz/runtime/viz_session.py` (architecture-pinned), a subprocess string in `tests/gpu`, and the archive surfaces deliberately stay on the shim. With maintained callers at zero, the shim collapsed from a `types.ModuleType` subclass with a seven-entry mutable-forwarding table into a plain re-export shell; a new three-case AST gate (`tests/architecture/runtime_facade/test_world_batch_owner_imports.py`, added to the smoke manifest) forbids re-importing the shim from the maintained surface and pins the shim `__all__` to its actual re-exports. The config-key string `runtime.world_batch_vec_env=true` is untouched. Final diff: 25 files, 165 insertions, 63 deletions (code and tests: 21 modified files +50/-59 plus the 89-line gate test; remainder is this register update and the registry refresh). | Focused world-batch/multi-agent/facade/training/policy regressions: `291 passed`, `1 skipped`, `104 subtests passed`; all world-batch monkeypatch tests, including the three retargeted files, pass. The six remaining failures are this worktree's pre-existing local flecs link/artifact gaps, reproduced as baseline issues. Maintained smoke on the landed tree: `409 passed`, `45 subtests passed` (baseline 406 plus the three new gate tests). Ruff and `git diff --check` passed. Per owner instruction no independent review agent was dispatched; the orchestrator verified byte-identical transplantation and reran the landing gates. |
| I14 Build-unreachable dead-interface removal | `accepted` | `c04da9a8` on `codex/redundancy-consolidation` | Removed the build-unreachable `src/core/engine/weapon_launch_adapter.h` (503 lines, `EffectsEventSnapshot` plus `make_effects_event`) together with its five-case, 332-line text/migration shape test and the corresponding CI-smoke manifest entry. Full-tree reference and symbol audits found no production include, CMake, tool, Python, or contract-spec consumer; only text-level test reads. The two other orphan candidates stay `held` with concrete evidence: `information_transform_contracts.h` is compiled by nine architecture snippet probes and named by an active review document, and `same_window_edge_validation.h` is compiled by six snippet probes. Final diff: 6 files, 6 insertions, 840 deletions (code and tests: 3 files, -836; remainder is this register update and the registry refresh). | Rebuilt `ef_core`, `ef_py`, and `ef_test` in the stream worktree; `ef_test_all` passed. Maintained smoke on the landed tree: `405 passed`, `45 subtests passed` (410 baseline minus exactly the five deleted shape tests). The stream's broad regression (`663 passed`, `12 failed`, `5 errors`) reproduced only pre-existing machine-baseline items in untouched surfaces (path-separator matching, GBK/PDF probes, a fragility numeric baseline, stale allowlist and facade text-shape assertions), consistent with the machine-baseline inventory recorded in I12/I13; none touch the deleted surface. `git diff --check` passed. Per owner instruction no independent review agent was dispatched; the orchestrator verified the transplant, the suite-manifest merge, and the landing gates. |
| I15 Test-archive retirement and test-wall adjudication | `accepted` | `3342c435` on `codex/redundancy-consolidation` | Retired the whole `tests/archive` tree (52 files: historical raw-`UniversalEnv` regression specs, scripted-bridge specs, and legacy leader contracts) after a per-file reference scan found zero functional consumers — suite manifests, conftest, and runners carry no references, and the three tests that mention the path only use it as an exclusion pattern or synthetic tmp fixture. Coverage supersession by the maintained surfaces was checked per domain. The bilingual `tests/` and `tests/contracts/unit/training/` READMEs now record the retirement and point to git history instead of carrying broken links. Two adjudications are recorded as `held` with evidence: the weapon-guidance authority-table modules contain substantive test logic (kernel calls, fixtures, assertion chains) rather than pure data tables, and contract-spec parameterization is unsupported by the maintained runner, so neither was forced into this iteration. Final diff: 59 files, 10 insertions, 4,210 deletions. | The three gate/audit tests that mention the archive path pass on the landed tree (`11 passed`). Maintained smoke: `405 passed`, `45 subtests passed`, unchanged, confirming the archive carried no smoke contribution. `git diff --check` passed. Per owner instruction no independent review agent was dispatched; the orchestrator reran the reference scan and the landing gates. |
| I16 Tools scaffolding consolidation | `accepted` | `aa1ec513` on `codex/redundancy-consolidation` | Converged the hand-written `REPO_ROOT`/`sys.path` bootstrap blocks across diagnostics, geometry, and the seven maintenance evidence packages (~78 scripts) onto `python.runtime_bootstrap` (`ensure_repo_imports`/`repo_root`/`resolve_repo_path`), keeping the two-line direct-run path hint and the chdir semantics of scripts that rely on them. Lifted isomorphic scalar/IO helpers into `tools/diagnostics/common.py` (`finite_float`, `finite_float_or_none`, `mean_finite`, `native_stdout_to_stderr`, a parameterized `write_json_output`) and reused the chunked file/text hashing owner in `tools/maintenance/retained_artifacts/manifest_integrity`; every diagnostic capability, CLI surface, and output format is preserved. Semantically inequivalent variants stay local with comments (three `_mean` empty-input conventions, non-resolving path-display helpers, the weapon-probe schema `_finite_float`), and no `write_retained_*` output path was touched. Final diff: 87 files, 991 insertions, 1,923 deletions (code: 84 files +985/-1919; remainder is this register update and the registry refresh). | Focused regressions matched the pre-change failure sets exactly (`tests/tools` 7 failed/94 passed; fire-timing contracts 19 passed; process-probe 41 passed; damage_model 4 failed/247 passed/5 errors including the known GBK item). Maintained smoke: `405 passed`, `45 subtests passed`. Eight CLI `--help` smokes across the three regions passed. Ruff and `git diff --check` passed. Per owner instruction no independent review agent was dispatched; the orchestrator verified the transplant (excluding the stream's tracked-bytecode noise) and reran the landing gates. |
| I17 I8-I11 post-review remediation | `accepted` | `1cfca742` on `codex/redundancy-consolidation` | Repaired the three actionable findings from the post-acceptance I8-I11 review. SB3 checkpoint loading moved to the bootstrap-neutral `python.rl.policy_checkpoint` owner so the root `evaluate.py` keeps the installed-wheel fallback, while `tools/eval/sb3_eval_base.py` retains its diagnostics-local fail-closed bootstrap and compatibility re-export. Batch observation and visual backend choices now have distinct semantic owners and drive both runtime normalizers and the world-batch benchmark CLI instead of borrowing `FLIGHT_SHAPING_BACKENDS`. The I9 `train.py` baseline was corrected from 927 to 1,008 lines, and the malformed I10 `118/15` attribute claim was corrected to the 118-attribute input surface. Final diff: 11 files, 143 insertions, 60 deletions. | Focused env-config, evaluate, historical checkpoint, and training-entry regressions: `27 passed`, `1 skipped`. Eight changed Python files passed in-memory syntax compilation and Ruff with cache disabled. The maintained bilingual audit reports 77/77 pairs synced, and `git diff --check` passed. |
| I18 Diagnostics episode/env construction consolidation | `accepted` | `abed924c` on `codex/redundancy-consolidation` | Consolidated the shared reset/step/termination/buffer-dispatch/close shell of the three diagnostics collectors (`event_credit_head` offline/online, fire-timing `real_update`) into `tools/diagnostics/common.py`, keeping domain buffers in the callers and expressing online value bootstrap through an episode-end hook. Routed the canonical single-world construction paths of the weapon-employment process probe and the post-launch assessment benchmark through `build_single_world_batch_execution_runtime`, preserving every non-default setting and the injected-VecEnv compatibility seam. Multi-world/private-VecEnv benchmarks, semantically distinct rollout loops, and the kernel-direct probes stay local by design. This iteration trades a recorded net line increase for a single collection-shell owner: 9 files, 426 insertions, 200 deletions including the register and registry refresh (code: 6 files +420/-196); future collectors reuse the shell instead of copying the loop. | Pre/post focused regressions matched exactly (fire-timing 19; process-probe 41; event-timing 7; world-batch 113 passed, 1 skipped, 20 subtests). Real two-step offline/real collection reports were byte-equal before and after. Seven CLI `--help` smokes, Ruff, and `git diff --check` passed. Maintained smoke on the landed tree: `407 passed`, `45 subtests passed` (the I17 base added two env-config guards to the smoke file). Per owner instruction no independent review agent was dispatched; the orchestrator verified byte-identical transplantation and reran the landing gates. |
| I19 Retained-write single-owner convergence | `accepted` | `33501818` on `codex/redundancy-consolidation` | Introduced `write_and_hash_json()` in `tools/maintenance/retained_artifacts/manifest_integrity.py` and converged 23 of the 24 `write_retained_*` functions across the seven evidence packages into thin shells that delegate JSON serialization and SHA-256 hashing to the shared owner, preserving every function name, signature, return value, and byte-level output convention (including the Windows `\r\n` write-text behavior and the `_sha256_text` manifest-hash path in `candidate_artifacts`). The one residual, `release_governance/source_release_signoff`, keeps its local implementation because its three-pass manifest stabilization is semantically distinct. A new 260-line parity suite (`tests/tools/test_retained_write_parity.py`) embeds the removed implementations as references and proves byte equality per function and payload. Final diff: 28 files, 487 insertions, 164 deletions (code and tests: 24 modified files +205/-160 plus the parity suite; remainder is this register update and the registry refresh). | Parity suite: `69 passed`, `4 skipped` (the skips are Unicode cases where the removed implementations themselves crash under a GBK locale; the shared owner's explicit UTF-8 is a strict improvement). `tests/tools` failure set unchanged (7 failed, now 163 passed with the parity additions). The retained/manifest/sha256 subset of `tests/architecture/damage_model` is unchanged (2 failed, 38 passed, 1 error, all pre-existing local items). Maintained smoke on the landed tree: `407 passed`, `45 subtests passed`. Ruff and `git diff --check` passed. Per owner instruction no independent review agent was dispatched; the orchestrator verified byte-identical transplantation and reran the landing gates. |
| I21 Contract-spec extends inheritance and near-duplicate convergence | `accepted` | `3c86574a` on `codex/redundancy-consolidation` | Lifts the I15 `held` finding that contract-spec parameterization lacked runner support: `python/testing/contracts/common.py` now supports `extends` with local-first/repository-root-fallback path resolution, recursive reuse of the existing `_deep_merge`, cycle detection, and a four-level chain limit; the `extends` key never reaches the effective spec. The naval threat-ROE pair converges on `tests/contracts/_base/naval_screen_threat_roe_base.json` and the scripted-takeoff pair on `_base/scripted_takeoff_base.json`, with all four effective specs proven deep-equal to their pre-convergence baselines by an embedded golden test; base specs live outside runner collection globs (`collected_base_specs=0`). The wrappers cluster stays local (leaf similarity only 0.79-0.96). Final diff: 11 files, 409 insertions, 104 deletions (code and tests: 5 modified +63/-100 plus two base specs and the extends test; remainder is this register update and the registry refresh). | Effective-spec parity: 4/4 deep-equal. All four converged contracts and the 10-entry CI contract suite pass; runners/scenario regressions `73 passed`. Maintained smoke on the landed tree: `408 passed`, `45 subtests passed`. Ruff and `git diff --check` passed. Per owner instruction no independent review agent was dispatched; the orchestrator verified staged-blob-identical transplantation and reran the contract suite, the extends tests, and the landing smoke. |
| I20 Probe CLI shared argument groups | `accepted` | `2ca09145` on `codex/redundancy-consolidation` | Landed `add_probe_run_args`/`add_model_load_args`/`add_json_out_arg` builders in `tools/diagnostics/common.py` with underscore canonical dests and dual underscore/hyphen registration (aliases use `help=SUPPRESS` so every `--help` surface stays byte-identical). Migrated the seven full-match probe CLIs (weapon-employment process probe, event-credit offline/online, fire-timing real_update/chain_breakpoint/learnability_audit/window_position_sweep), removing 57 hand-written `add_argument` calls; partial-match scripts are deferred to a later batch. Like I18, this iteration invests net lines in a shared owner so future probes stop copying scaffold. Final diff: 11 files, 314 insertions, 68 deletions (code: 8 files +308/-64; remainder is this register update and the registry refresh). | All seven migrated scripts keep byte-identical `--help` output before and after; three dual-alias parse checks agree on dest values. Focused regressions: `60 passed` (fire-timing contracts 19, process-probe 41). Maintained smoke on the landed tree: `408 passed`, `45 subtests passed`. Ruff and `git diff --check` passed. Per owner instruction no independent review agent was dispatched; the orchestrator verified staged-blob-identical transplantation and reran the landing gates. |
| I22 DTO single-source generator skeleton | `accepted` | `db97d478` on `codex/redundancy-consolidation` | Added the standard-library-only declarative DTO schema package (`tools/maintenance/dto_schema/`): schema model with extension slots (`python_name`/`readonly`/`hidden`/`json_key` reserved), an X-macro parser, a `--check`/`--write` generator CLI with a machine-readable manifest, and a README describing the three-step adoption flow for new DTO families. Piloted on the two existing X-macro inventories (89-field flight-shaping, 135-field EffectsEvent with its 48/87 grouping): regeneration is byte-identical, `--write` is idempotent, and an injected default-value drift is caught with a nonzero exit and unified diff. A freshness architecture gate joined the maintained smoke manifest. This is the foundation for the phased DTO single-sourcing program (episode, world-batch, engagement, command, GPU families). Final diff: 13 files, 958 insertions, 4 deletions (code and tests: 10 new files +952; remainder is this register update and the registry refresh). | Generator `--check` reports both pilots up-to-date on the landed tree and the freshness gate passes (`1 passed`). Maintained smoke: `408 passed`, `45 subtests passed` (407 baseline plus the freshness gate). Focused execution/engagement regressions: `82 passed`, `226 subtests passed` in the stream worktree. Ruff and `git diff --check` passed. Per owner instruction no independent review agent was dispatched; the orchestrator verified staged-blob-identical transplantation and reran the generator check, the gate, and the landing smoke. |
| I23 Episode-evaluation DTO single-sourcing | `accepted` | `9d23b947` on `codex/redundancy-consolidation` | W2 critical-path iteration of the unified-infrastructure blueprint. Twelve new schemas (233 fields) put the safety/termination, conditional-objective, mission-nav/observation/step-info, and remaining reward families under single-source governance: the field blocks of four mission-runtime headers and the corresponding `bindings_episode.cpp` sections now expand from generated X-macro lists, and fourteen generated Python builders (plus package init) join the unified freshness command (`generate.py --check`, 29 artifacts, drift-tested). `safety.py` migrates its sixteen mechanical config assignments to the generated builder; derived/conditional assignments deliberately stay hand-written. Read-only products DTOs generate no assigner and mixed assigners skip read-only fields. Registered residuals: three contiguous execution-composition blocks (8-field outcome, 8-field input prefix, 9-field product prefix) deferred to a later family. Final diff: 54 files, 2,393 insertions, 560 deletions (code: 10 modified files +315/-554 plus 41 generated/schema/test files; remainder is this register update and the registry refresh). | Independent review (dispatched for this critical phase) verified all 233 macro-expanded fields exact against the pre-change headers (type/name/default/member order), the full 687-attribute binding surface exact by static and runtime comparison, and the safety migration mapping; its two blocking findings (builder freshness outside the main gate; crashing assigners on read-only products) were repaired and the final re-check approved with zero blockers. Focused execution/mission regressions: `136 passed`, `234 subtests passed`; `ef_test_all` passed; maintained smoke on the landed tree: `408 passed`, `45 subtests passed`. Ruff and `git diff --check` passed. |
| I24 Tasking-contracts neutral-layer extraction | `accepted` | `70e75454` on `codex/redundancy-consolidation` | W2 critical-path iteration (runtime-substrate step B-1). Extracted the profile-independent slice of the `gym_envs`-to-`python.rl` tasking entanglement into the new zero-dependency package `python/tasking_contracts/` (mission command/phase vocabulary, bridge views and command-chain sync seams, `LeaderDecisionState`, timing coercion, and the scripted controllers). Every moved name stays importable from its old path through re-export shells proven identical by an `assertIs` suite. Eleven previously entangled `gym_envs` files are now free of `python.rl` imports and `gym_envs.leader_env` no longer pulls `python.rl.runtime.leader_window_runtime` at import time; fourteen files keep a governance-ledger residual (profile-dispatch functions that must reach the `python.rl`-internal air/ground/naval adapters), locked by an exact-allowlist AST gate that fails on both new references and stale entries. Two pre-existing bridge guardrail assertions were repointed at the relocated canonical definitions without weakening. Registered follow-ups: dependency-inversion or lazy package-init for the residual dispatch (B-2), and reviewer-noted AST-gate blind spots (relative/dynamic import forms). Final diff: 44 files, 1,986 insertions, 1,285 deletions (code: 30 modified files +257/-1,281 plus 11 new files with 1,723 lines; remainder is this register update and the registry refresh). | Independent review (dispatched for this critical phase) verified the moved definitions line-identical against their pre-change sources, confirmed the fourteen-file residual ledger exact against an independent AST scan, endorsed the do-not-force-dispatch adjudication (a dependency-inversion registry is not a lower-risk alternative), and confirmed the guardrail rewrites preserve intent with the raw-write check strictly stronger; verdict approve with zero blockers and three non-blocking notes (gate blind spots, private-helper wording, corrected statistics). Focused regressions: leader+scenario `120 passed`, world-batch `113 passed, 1 skipped`, runtime failure set byte-identical to baseline. Maintained smoke on the landed tree: `426 passed`, `45 subtests passed` (408 baseline plus 18 new gate/compat tests). Ruff and `git diff --check` passed. |
| I25 Unified architecture program plan | `accepted` | `d5e4b7cf`, amended by the design-principles commit on `codex/redundancy-consolidation` | Documentation iteration. Established the [Unified Architecture Program](../unified_architecture_program/README.md) as the frozen roadmap for the remaining architecture-level unification (tracks T1-T7: DTO single-source completion, runtime substrate unification, C++ structural boundaries, exact-runtime alignment, declarative configuration, test-infrastructure rationalization, final residual audit), with this register retained as the single iteration ledger. Added the plan pair to the strict bilingual surface (`STRICT_PLAN_SUBTREES`) and to the `docs/plan` index alongside a previously missing index entry for this consolidation plan. Final diff: 8 files, 183 insertions, 7 deletions, register and registry refresh included. | Bilingual registry now covers 78 pairs, all synced; maintained link audit: 158 documents, 2,608 links, zero issues. Maintained smoke unchanged. Ruff and `git diff --check` passed. Documentation-only iteration; landing gates run by the orchestrator. Program-plan amendments landed as follow-up commits `44627be9` (design principles and performance boundary), `c55175ff` (global invariants and baseline critique), `72a52cf0` (track rederivation), and `779a821b` (SCAL-derived adaptation tracks T8-T11). |
| I27 Gym-env package-init lazification and import-gate hardening | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T2 (B-2). Converted the three eager `gym_envs` package inits (`scenario_loader` 11 names, `universal_env_parts` 44, `leader_env_parts` 52) to PEP 562 lazy resolution with `__dir__`/`__all__` preserved, and demoted the module-level `python.rl` residual imports in the fourteen ledgered files to in-function deferred imports; the unused `ScriptedC2TaskManager` module alias was removed after a zero-consumer scan. Import-only `python.rl` module count for the gym-env entry surfaces is now zero, while real profile dispatch still lazy-loads on first use (air 17, naval 18 with `naval_adapter` confirmed). Both import gates were hardened after review-replayed bypasses: the runtime-bootstrap gate dropped its literal pre-filter for full-AST scanning with PEP 328 relative-import resolution, cross-line dynamic-import detection became a file-level capability-times-target alarm with an explicitly audited allowlist, and the tasking gate gained constant folding, `import_module` alias tracking, and module-level classification of default-argument expressions; thirteen review-derived bypass fixtures now pin the coverage. One deliberate semantic restoration is recorded: `AdaptiveKLPPO` import errors raise immediately again instead of being swallowed into a PPO fallback. Side-effect timing note: bare `universal_env_parts` package import no longer runs `configure_repo_imports()`/`set_log_level`; both run on first lazy access of `common`, and no bare-package side-effect consumer exists. Final diff: 21 files, 1,147 insertions, 283 deletions (code and tests: 18 files +1,141/-279; remainder is this register update and the registry refresh). | GPT review round 1: needs-repair (three gate-bypass blockings: relative-import pre-filter skip, cross-line dynamic import, constant-fold/alias/default-argument blind spots); all bypasses were converted to fixtures and re-verified by the reviewer replaying its own constructs — final verdict approve with zero blockers, gate cost measured at 10.8s (~7% of smoke) and judged acceptable. Gates plus compat suite: `25 passed`; world-batch/leader regressions: `160 passed, 1 skipped, 22 subtests` (baseline-identical); compat assertIs suite `299 passed, 1 skipped, 26 subtests`. Maintained smoke on the landed tree: `435 passed`, `45 subtests passed` (426 baseline plus nine tasking gate fixtures). Ruff and `git diff --check` passed. |
| I26 World-batch DTO family single-source (five of six sub-families) | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T1, critical period. Moved the world-batch/facade DTO vocabulary mirrored field-by-field between `world_batch_contracts.h`, `runtime_facade_types.h`, `platform_capability_contracts.h` and `bindings_runtime.cpp` onto `tools/maintenance/dto_schema` single-source ownership: five of the six planned sub-families (platform capability/spawn 3 classes/32 fields, typed spawn/reset 3/24, world setup/assignment 10/66, runtime capability/config/meta 7/86, runtime window/experiment 15/132) -- 38 classes, 340 fields total; the facade packets/views sub-family (~211 fields) is deferred with a recorded scope list. Header field blocks and the corresponding `def_rw` binding segments expand from 38 new schemas; 8 classes keep hand-written bindings because their pre-existing registration order diverges from header declaration order (each carries a `NOTE(I26)` marker, `dir()` parity verified), and `RuntimeWindowActionRequest` is skipped whole (never-bound hidden member plus pre-existing order divergence). The four owning files shrink by a net 437 lines (+313/-750); 115 new files add 4,034 lines (76 generator products +2,461, 38 schema sources +1,502, one shared test helper +71). Seven pre-existing source-text-scanning test files were adapted (six equivalent-expansion fixes through the new `tests/support/xmacro_text.py` helper, which reuses the schema parser, plus one freshness-registry expansion); one generated include was named `resident_device_output_descriptor.inc` to avoid a pure include-path false positive in the GPU boundary guard, whose semantics are untouched. Code and generated payload: 127 files, 4,643 insertions, 758 deletions; the landed commit additionally carries this register entry and the registry refresh. | GPT review round 1: approve with zero blockers. The reviewer independently re-derived the ABI evidence rather than trusting the implementer: all 340 field declarations mechanically compared against the pre-change headers (order, type tokens, default-value expressions byte-equivalent modulo whitespace; the 184 bare declarations rendered as `= {}` are all class types with equivalent default construction), confirmed the 8 hand-written classes are exactly the set whose old registration order diverged, reproduced cross-build `dir()`-plus-defaults parity 38/38 with its own probe, re-ran `generate.py --check` (105/105), focused regressions (`7 failed, 308 passed, 1 skipped, 25 subtests` -- all seven pre-existing machine reds, three reproduced on a clean-HEAD worktree), full smoke (426/45 at the 779a821b base), and ruff. Three non-blocking notes: the implementer report's generated-artifact tally (91 files/+2,947) was inaccurate and is corrected in this entry; "seven scanning-test fixes" is precisely six expansion fixes plus one registry expansion; the expansion helper renders five explicitly brace-initialized fields as bare declarations, a cosmetic note with no current consumer. Landed-tree maintained smoke: `435 passed`, `45 subtests passed`. |
| I28 Machine-baseline-red repair and weapon-realism residual governance | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T6. Tests-only iteration in two repair rounds (19 files, +459/-63, all under `tests/`). Round one fixed the test-infrastructure baseline reds across nine files: the compatibility-quarantine allowlist matcher now normalizes path separators, the winsock harness link line gains `-lws2_32` (the flecs candidate-path resolution already existed at HEAD, so the link flag is the only genuinely new piece), and the GBK probe-decoding failure is repaired (no PDF-related claim; that historical item has no object on this baseline). Round two adjudicated the 45 long-standing `weapon_guidance_realism` junit failures under a build compiled from this exact tree (`CMO_BUILD_DIR` verified to load the worktree `ef_py`; failure set reproduced identically across three runs): they collapse to 33 unique methods in six groups (primary-response selection drift, proximity-projection spread, cross-subsystem overspill -- e.g. the G3 hitboxes case reads `mil_thrust_n` 64510 against an expected steady 76310, loss-state escalation/saturation, aero/fuze response drift, mechanism-calibration drift), every one adjudicated as product-behavior or calibration drift with zero test-infrastructure-fixable items (fixed seeds, three-run-identical, semantic-level deltas). Governance is per-method with no blanket skips: 25 methods carry `pytest.mark.xfail(strict=True)` with group, root cause, and a `registered residual, owner: unified architecture program T6 ledger` pointer; 4 all-failing subTest methods add `unittest.expectedFailure` folding; 4 mixed-pass subTest methods use plain `expectedFailure` (pytest 9 would flag passing subtests as XPASS(strict)), with probe evidence that unexpected successes still hard-fail. | GPT review round 1: needs-repair (four blockings: classification had to run on a matched build, the 45 failures needed complete adjudication, governance had to be per-method, and two wording overclaims). Repair re-review: approve -- the reviewer independently verified the matched-build pyd load, re-ran the weapon suite (`167 passed, 33 xfailed, 217 subtests`, zero failed), lifted the markers with `--runxfail` on three groups to confirm the underlying failure signatures still match the adjudications, replayed the anti-red probes (unexpected success hard-fails), and reconciled every register number. Three residual observations, none blocking: the four plain-`expectedFailure` nodes carry their reason only in source comments (T6 ledger should index those node ids), `xfail(strict)` alarms on recovery but not on accepting drift as a new baseline (T6 ledger process duty), and the thrust-drift example belongs to the G3 hitboxes case rather than the group's first-listed red (wording corrected in this entry). Validation on the source worktree: weapon suite matched-build `167 passed, 33 xfailed, 217 subtests passed`; companion combos `23 passed, 1 skipped` / tools `170 passed, 4 skipped` / support `21 passed` / clock+boundary `9 passed`; maintained smoke at the 779a821b base `426 passed, 45 subtests`; ruff and `git diff --check` clean. Landed-tree verification recorded below in this entry: weapon suite against the I26-matched build and the maintained smoke both green. |
| I29 SCAL conformance census and baseline amendments (T0) | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T0. Read-only census plus baseline-amendment drafting; no code changes. New bilingual census document `docs/plan/unified_architecture_program/scal_conformance_census_20260720.md`/`.zh.md` (document kind `reference`: descriptive census register, not a standalone review, contains no adjudications) records per-invariant G1-G6 conformance against the maintained tree. Corrected G1 finding: maintained direct-construction bypasses are zero -- the facade construction in `python/rl/runtime/world_batch/adapter.py` is the only maintained cross-boundary constructor, `UniversalEnv` is already a fail-fast shell pinned by the WP24 gate, and raw-kernel injection survives only in contract fixtures under `python/testing/contracts/`; the true residual G1 gap is the untyped `ScenarioLoader.sim` seam (wide call surface pending contract typing). The SCAL baseline document gained the six amendments derived from the program critique: SS1.5 Experiment face, SS6.1 stage-contract amendment, SS7.1 stage-contract enforcement, SS15 kernel invariants, SS16 representation strategy, SS17 Learning-face architecture note; the program README pair links the census. Payload: 6 files, 398 insertions, 2 deletions (4 amended documents +209/-2 plus the two census files); the final commit adds this register row and the registry refresh. | GPT review round 1: needs-repair (P1: census overstated maintained G1 bypasses as two paths by misreading the already-degraded `UniversalEnv`; P2: census misfiled as document kind `review`); after repair the reviewer re-verified every corrected claim against code evidence (rg construction scan, WP24 gate presence, facade-only construction, loader seam) and issued approve with zero blockers. Documentation-only iteration: bilingual audit and maintained link audit clean after registry refresh; `git diff --check` passed; maintained smoke on the landed tree re-run green. |
| I30 Typed experiment definition and air-combat matrix single-source | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T5 first slice, aligned with the SS1.5 Experiment-face amendment. New single-owner package `python/experiment/` (pure standard library, zero bootstrap side effects: bare import loads no `ef_py`/`gymnasium`/SB3): `definition.py` freezes `Experiment = ScenarioRef x ConfigComposition(base+delta) x SeedSpec x EvaluationProtocol` with fail-fast construction and an `ExperimentRegistry` as the G5 registration socket (duplicate registration and dangling base/protocol references raise immediately; dangling scenario paths are syntax-validated with existence pinned by the freshness gate); `composition.py` owns deterministic merging (base key order preserved, delta-new keys appended in delta order, recursive for mappings, whole-value replacement otherwise); `air_combat_matrix.py` expresses the 24-file `examples/config/training/active/air_combat/` matrix as one base `air_combat_1v1_hmoe_execution_v1` plus 24 deltas, registering only the dimensions actually used (six scenarios, empty seed sets, protocols `smoke` x2 / `probe` x22 -- no speculative fields). Generator `tools/maintenance/experiment_matrix/generate.py` (`--check`/`--write`/`--manifest`) reproduces all 24 files byte-for-byte on first check (24/24 up-to-date, zero normalizations or waivers); parity required modeling three serialization dialects observed in the existing corpus (ten entries write `learning_rate` as decimal `0.00003`, three entries expand scalar arrays element-per-line, four trailing-key conventions plus CRLF checkout). Freshness gate `tests/architecture/governance/test_experiment_matrix_freshness.py` (check, 24-output pinning, directory-to-registry equality, isolated-copy drift injection, restoration) joined the smoke manifest. Naming deviates from the plan sketch deliberately: `.gitignore` line 85 ignores `experiments/` at any depth, so the owner lives at singular `python/experiment/` and the generator under `tools/maintenance/experiment_matrix/`. Payload: one tracked file gains one line (smoke manifest) plus seven new files totaling 1,890 physical lines (owner package 1,254, generator 218, two test files 418; 1,670 counting non-blank lines only). The matrix README's five pre-existing stale filenames were left untouched (out of scope, noted as residual). | GPT review round 1: approve with zero blockers. Independently verified: first-run 24/24 byte parity plus SHA-256 spot checks and a drift-injection red on an isolated copy; the three dialects confirmed as observations of the existing files (decimal-literal census 10/14, exactly three expanded arrays, tail-key rules 24/24) with the matrix unchanged against the base commit; bare-import purity, deep-freeze and fail-fast semantics, G5 scale (no speculative fields), scenario pairing against README/training commands/scenario metadata; new tests `41 passed`, with runners `86 passed`; smoke `436 passed, 45 subtests passed` (baseline 435 plus the freshness gate); ruff and `git diff --check` clean; write set disjoint from the I26 landing. One claim graded partially true and restated in this entry (dangling `ScenarioRef` is syntax-validated only). Three non-blocking hardening notes carried as residuals: the generator does not `json.dumps`-escape object keys (a hypothetical key containing a quote would emit invalid JSON), the literal-override comparison inherits Python `True == 1` equality so a bool-vs-int drift could slip, and the gate pins scenario existence but not the full experiment-to-scenario mapping (a swap between two valid scenarios would pass). Landed-tree maintained smoke recorded below. |
| I31 World-batch DTO family single-source completion (sub-family F: facade packets/views) | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T1, closing the sixth and final planned sub-family of the world-batch/facade DTO vocabulary. Per-class verification corrected the scope inherited from I26 (the "~211 fields" figure was a hand-over rough estimate, not a census): sub-family F is 17 classes, 123 fields. 16 classes / 108 fields moved into `tools/maintenance/dto_schema` single-source ownership (11 facade-owned plus 5 contracts-owned whose physical definitions live in `src/runtime/contracts/runtime_dto_contracts.h`, with generated includes under `contracts/detail/` per the established convention); header field blocks and the matching `def_rw` binding segments both expand from 16 new schemas, and all 16 classes had binding-registration order already equal to header declaration order, so no hand-written-binding exceptions were needed. `ExecutionBatchStepResult` (15 fields) is recorded held: its `std::vector<std::array<double, 4>>` field has an angle-bracket comma the C preprocessor would mis-split into an extra macro argument (the preprocessor pairs only parentheses), and a type-alias workaround would break token-for-token type equivalence, so the class stays fully hand-written. `ObservationSchemaVersionParts` is a never-bound internal helper and stays out of schema ownership. Four source-text boundary tests were adapted through the existing `tests/support/xmacro_text.py` expansion helper with assertion payloads unchanged; four `nb::class_<T>(m, "T")` markers were narrowed to `nb::class_<T>` prefixes, each still uniquely anchored. Payload: 57 files, 1,814 insertions, 295 deletions (9 modified files +280/-295 plus 48 new files: 16 schema sources, 16 generated includes, 16 generated Python builders). | GPT review round 1: approve with zero blocking and zero non-blocking findings -- all ten verified claims held. The reviewer mechanically re-derived the ABI evidence (16 classes' old declarations against schema and generated includes, field-for-field; old binding registration order equal to declaration order 16/16), reproduced the held-class rationale with its own preprocessor experiment (macro receives four arguments where three are declared; an alias would compile but change type tokens), confirmed the never-bound helper, ran its own isolated-subprocess parity probe across the two builds (19/19 classes: 16 migrated plus 3 sentinels, `dir()` and recursively expanded defaults identical), re-ran `generate.py --check` (all up-to-date), the incremental build, `ef_test_all`, focused regressions (`6 failed, 309 passed, 1 skipped, 25 subtests` with all six reds reproduced on an isolated pre-change baseline: five flecs static-lib link signatures and one pre-existing `common.ef_py` attribute gap), and maintained smoke on both the changed tree and the isolated baseline (`435 passed, 45 subtests` each, matching the 45e0d5e9 baseline). Write set confirmed disjoint from the I30 landing; the `runtime_dto_contracts.h` write-set extension is warranted by the five entity definitions and the pre-existing `DTO_HEADER` test constant. Landed-tree maintained smoke recorded below. |
| I32 WorldBatchCore stage-contract base and execution mode plugin (first slice) | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T2 first slice. New `python/rl/runtime/world_batch/core.py` (+573 physical / 500 non-blank lines): eight batch-step stage contracts carrying the full SS6.1 field set -- authoritative `P<n> <Name>` semantic-stage identifiers with honest cross-stage spans (`command_sync` spans P2 TaskingIntent/P3 CommandDelivery, `reward_episode` spans P10 ObservationExport/P1 WorldSetup and declares the post-launch-assessment and episode-autoreset event-driven sub-stages), read/write sets audited against the actual `step_wait` code, clock domains, G4 six-layer information declarations, and extension points. The contracts are load-bearing rather than decorative: `step_wait` carries one `# [stage:<name>]` anchor per stage with a source-structure test pinning anchor sequence equal to the declared stage order, and import-time validation cross-checks each stage's extension points against the plugin hook bindings (three failure branches each carry a negative test). `ExecutionModePlugin` plus a G5 registry (duplicate/unknown-mode fail-fast); `StandardExecutionPlugin` genuinely routes the three measured execution-mode divergence points (mainline-vs-standard behavior update, mainline command-sync skip, air-combat hybrid event finalization); the domain finalizer is injected at plugin construction by `vec_env.py`, keeping `core.py` at zero `gym_envs` import nodes (AST-walk test) and zero import opcodes in all six hook bodies (disassembly-pinned). Cooperative and leader modes register as thin stubs; their internal-logic migration onto the plugin interface is the recorded next T2 slice (~350 measured duplicate lines). Payload: 4 files, 1,199 insertions, 21 deletions (`__init__.py` +22, `vec_env.py` +23/-21, plus the new core and its 72-test suite at +581 physical lines) -- an infrastructure-investment slice per the program's stated economics. | GPT review round 1: needs-repair with three P1 blockings -- a behavior drift (the hybrid `truth_before=None` case skipped the event-finalization call the old inline code made unconditionally, and a new test had pinned the drift), stage contracts using self-invented underscore stage names with misdeclared read/write sets and no wiring to `step_wait` (decorative data), and a per-step lazy `gym_envs` import inside core violating G2 one-way layering. The same round also reclassified the implementer's claimed smoke evidence: the "2218 passed" figure was a hand-expanded near-full `tests/` run whose reds are pre-existing baseline items, while the actual manifest smoke was `436 passed, 45 subtests`. Repair re-review: approve -- the reviewer independently mock-tested the None path (unconditional call restored, arguments verbatim-equal to the old inline code), verified the authoritative stage vocabulary and the three read/write-set corrections against `step_wait`, confirmed the anchor-sequence test and import-time extension-point validation are real wiring, re-disassembled all six hooks (zero import opcodes) and re-ran every gate (focused suites `270 passed` = 198 baseline + 72 new / same 5 pre-existing flecs.h environment reds / 1 skipped / 22 subtests; manifest smoke `436 passed, 45 subtests`; ruff and `git diff --check` clean). Landed-tree maintained smoke recorded below. |
| I33 Engagement DTO family single-source (T1) | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T1. Per-class census corrected the scope inherited from the program plan (the "~445 fields/29 classes" figure was exactly the `engagement_contracts.h` hand-written field total, already excluding the earlier-migrated `EffectsEvent`, but missed the two engagement classes physically co-located with the world-batch family in `runtime_facade_types.h`): the true engagement family is 30 classes, 481 fields (28 classes/445 fields in `engagement_contracts.h` plus `EngagementBatchRequest`/`EngagementEventPacket`, 2 classes/36 fields). All 30 moved into `tools/maintenance/dto_schema` single-source ownership in one pass (no batching needed), bringing the registry to 98 schema modules / 197 generated artifacts: every class's pre-existing binding-registration order in `bindings_runtime.cpp` was already equal to header declaration order, so zero hand-written-binding exceptions were required, matching the I26/I31 established macro/`.inc`/named-local-variable convention throughout. The two `InformationStateSource`-typed provenance fields on `EngagementEventPacket` reuse the multi-argument `make_information_state_source(...)` default-value pattern established by I31. Per SCAL amendment (b), the `stage`/`owner_stage` vocabulary fields migrate as plain strings with a documenting comment marking them as the future stage-contract anchor; no stage-contract mechanism is introduced. Two adjacent items were surveyed and deliberately left untouched: `RecentEngagementEvents` (`src/core/engine/engagement_event_types.h`, 14 fields, identically clean shape) sits outside this iteration's declared write-set boundary and is recorded as the natural next candidate; the thirteen `Engagement*Record`/`IEngagementEventRecorder` types in `src/core/interfaces/engagement_event_recorder.h` were confirmed never Python-bound and are correctly out of the cross-boundary DTO vocabulary. Source-text boundary tests were adapted in three files, each differently: `test_engagement_contract_shape.py` moved onto the shared `tests/support/xmacro_text.py` expansion helper and relaxed its struct-body regex tail from `\n\}};` to `\n?\}};`; `test_facade_engagement_evidence_gates.py` only gained an expansion call; and `test_runtime_dto_contracts.py` received the same regex relaxation as a review-driven repair. Root cause: `expand_header_field_incs` consumes the `#include` line's trailing newline, so a fully macro-owned struct ends with its last field glued to `};`, and the strict `\n\}};` form skips past it and greedily swallows neighbouring structs. Registered residuals for a future T6 pass: the helper newline defect plus the two remaining strict-regex sites currently green only by accident of header layout (`test_typed_platform_spawn_contracts.py`, `test_policy_contract_shape.py`). Payload: 98 files, 4,166 insertions, 1,150 deletions (8 modified files +473/-1,150 including the three test adaptations; 90 new files totalling 3,693 lines: 28 contracts `.inc` + 2 facade `.inc` + 30 schema sources + 30 generated Python builders). | GPT review round 1: needs-repair -- one blocking finding plus register corrections. The blocking regression: `test_runtime_dto_contracts.py::test_device_resident_output_descriptor_stays_additive_export_only_surface` (green at the 4aeb64b0 baseline, deterministically red on the changed tree, and outside the iteration's focused/smoke selections, which is why the original all-green claim coexisted with it) -- macro-izing the two facade classes extended the strict regex's over-match of the `RuntimeCapabilities` body to 14,642 characters, swallowing `DeviceResidentOutputDescriptor` and its forbidden tokens. The same round mechanically verified all 30 classes' ABI (declaration order, type tokens, defaults byte-equivalent) and registration orders, reproduced parity 32/32 with its own probe, and corrected two register figures (98 schemas not 120; 3,693 new-file lines not 3,153). Repair re-review: approve -- the match body converged from 14,642 to 1,214 characters bounded at the struct's own edge, a forbidden-token injection counter-example on an isolated copy proved the guard still trips, the four `test_wp22_*` directory reds reproduced identically on an isolated clean-baseline worktree (pre-existing machine reds), and every register number passed final audit. Focused regressions `345 passed, 1 skipped, 25 subtests` with the six pre-existing machine reds unchanged; maintained smoke `436 passed, 45 subtests`; `generate.py --check` all up-to-date; `ef_test_all` 8/8; ruff and `git diff --check` clean. Landed-tree maintained smoke recorded below. |
| I34 Cooperative/leader shared-duplicate sink onto the world-batch substrate | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T2 second slice. Re-measured the I32 duplication ledger, then sank the cooperative/execution shared duplicates into a new `python/rl/runtime/world_batch/_shared_ops.py` (253 physical lines): the four-chain command diff-plus-submit kernel (`diff_single_entity_command_chain`/`submit_command_chain_assignments`, parameterized over the execution handle-scalar and cooperative world-dict iteration styles), observation-dict assembly, `normalize_seed`/`save_obs_to_buffer` (verbatim twins), and backend-mode resolution (parameterized; the execution-side per-loader `use_compiled_execution_step_runtime` check stays in its mixin override, preserving the two callers' distinct availability semantics). Modified-surface net deletion 64 lines (cooperative -52, vec_env -43, visual mixin -3, core +28, `__init__` +4, compat shell +2); production net +189. `CooperativePlugin`/`LeaderPlugin` replace the I32 stubs in the registry but are honestly a naming promotion staging later migration: hooks equal base-class defaults and no production call site consumes them yet (cooperative `step_wait` still inlines `update_behaviors`; the leader group never calls `resolve_execution_mode`) -- docstrings state exactly that. Three residuals deliberately kept un-unified: step-evaluation batch preparation (cross-slot uniformity check and per-slot time-step sourcing), the VecEnv attr/method interface (world-fallback logic), and the step inner loop plus reward/episode management (cooperative latching semantics fundamentally differ). The monkeypatch seam for `project_world_*` moved from vec_env/cooperative module attributes to `_shared_ops` module attributes (late-bound, patch-effective); the landing includes the two review-caught seam repairs -- the `world_batch_vec_env` compat shell now re-exports `project_world_*` from their canonical definition module `command_chain_cache` (the shell had broken with ImportError, affecting the maintained `examples/viz` import chain), and the cooperative observation test's patch surface was adapted. Payload: 11 files, 536 insertions, 263 deletions (net +273); tests +84 lines with 12 new contract tests (core suite 72 to 84). | GPT review round 1: needs-repair with two execution-proven blockings, both outside the implementer's focused-suite selection -- the compat shell `world_batch_vec_env` raised ImportError on import (its `project_world_*` re-exports pointed at `vec_env`, which no longer imported them), and `tests/runtime/multi_agent/test_cooperative_vec_env_observation.py` patched now-removed cooperative module attributes (AttributeError red). The same round verified behavior equivalence function-by-function (four-chain diff ordering and guards, cooperative unconditional dict write-back proven semantically equivalent at both read points, backend-resolution caller divergence preserved, obs assembly reshape no-op) and graded the plugin claim honestly (registered, hooks equal defaults, no production consumer). Repair re-review: approve -- shell diff confirmed minimal (imports re-pointed at canonical `command_chain_cache`, `__all__` untouched, shell attribute `is`-identical to the canonical function, `examples/viz` import chain restored), multi_agent `40 passed` green, three docstrings verified against code facts, the full-repo `project_world_` sweep found all ten reference sites correctly placed, and a `leader_phase_manager_approach_arm` contract failure was reproduced on an isolated baseline checkout (pre-existing environment red; the `tests/gpu` import-order test is likewise a pre-existing `build-gpu`-absent environment red). Gates re-run independently: focused `282 passed` (270 baseline + 12 new) / same 5 flecs reds / 1 skipped / 22 subtests; manifest smoke `436 passed, 45 subtests`; contract gate 10/10; ruff and `git diff --check` clean. Register numbers in this entry follow the reviewer's corrected final audit. Landed-tree maintained smoke recorded below. |
| I36 T6 residual hardening pack | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T6, closing three registered review residuals plus one retained-artifact pollution defect in one pass. (A) `tools/maintenance/experiment_matrix/generate.py`: object keys now serialize through `json.dumps` (was raw `f'"{key}"'`, illegal JSON for quote-bearing keys); the literal-override comparison gained a recursive type-identity-aware `_strict_json_equal` (a bare `!=` silently passed a bool-typed override against an int value under `True == 1`); the freshness gate gained a full 24-entry experiment-to-scenario pairing-table assertion (existence-only checks could not catch a swap between two valid scenarios). Three dedicated negative tests, each proven red against the pre-fix generator and green after. (B) `test_release_signoff_gate_cli_writes_default_artifacts` was the one test in its file not using `tmp_path`; its argument-less CLI default wrote into the real `res001_release_signoff_20260531` retained directory and CRLF-checkout sha256 recomputation rewrote all three files on every run -- the root cause behind two earlier worktree-"contamination" false alarms. Redirected onto the CLI's existing `--output-dir`/`--report` flags plus a zero-IO static assertion pinning the `DEFAULT_OUTPUT_DIR`/`DEFAULT_REPORT_PATH` constants; `source_release_signoff.py` (I19's preserved Windows CRLF write convention) untouched. (C) New bilingual `reference` ledger `docs/plan/unified_architecture_program/t6_residual_ledger.md`/`.zh.md`, linked from the program README, indexing: I28's 33-method/six-group weapon-realism inventory with the four plain-`expectedFailure` node IDs named, I33's xmacro newline defect plus the two still-latent strict-regex sites, I31's `ExecutionBatchStepResult` hold, I33's `RecentEngagementEvents` hand-off (candidate designation recorded in I33's register row; I35 migration in progress), the (B) fix itself, and a five-item local-environment red list re-verified at c2952d61. Payload: 7 files, 642 insertions, 6 deletions (5 modified +177/-6 plus the two ledger documents); four review-mandated ledger text corrections were applied at landing (see review column). | GPT review round 1: approve with zero blockers and four non-blocking ledger text corrections, all applied at landing: the ledger had swapped the decorator shapes of the two `expectedFailure` quartets (the mixed-pass four are plain `expectedFailure`; the all-failing four carry the stacked strict-xfail -- now stated per the I28 register), one repaired-file path was mis-directoried (`build_system` corrected to `runtime_facade`), the `RecentEngagementEvents` provenance sentence was disambiguated to name I33's register row, and one mixed-language artifact was fixed in the zh version. The reviewer independently replayed the pre-fix generator (JSONDecodeError and DID-NOT-RAISE reproduced), probed `_strict_json_equal` with twelve boundary cases (conservative rejections only, no reverse leaks), verified 24/24 byte parity with SHA-256 spot checks against its own I30-review records, reconstructed the CRLF sha-drift chain character-for-character (`822c496d` to `809dfe67`), ran the signoff-gate file twice with retained files hash-stable, re-verified ledger facts including the four node IDs and group table against live runs, and reproduced the smoke algebra (`438 passed, 1 failed, 45 subtests`, the sole failure being the expected bilingual-registry flag that this landing's registry refresh resolves -- simulated green in memory by the reviewer). Landed-tree maintained smoke after registry refresh recorded below. |
| I35 Command/tasking maintained-batch-contract family single-source plus RecentEngagementEvents pickup (T1 close-out) | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T1, closing the last DTO bucket. Per-class census corrected the scope inherited from the program plan: the ECS component definitions and Core/Air/Naval/Ground domain slices for MissionCommand/TaskOrder/LeaderIntent/PilotReport live under `src/components/**`, outside this iteration's write-set boundary; the actionable family is the maintained-batch-contract transport layer in `src/runtime/contracts/world_batch_contracts.h` -- 15 classes/82 fields (four `*MaintainedBatchContract` aggregates, four standalone Air/Naval directive structs, seven three-field `World*Assignment` carriers) -- plus the I33-handed-off `RecentEngagementEvents` (`src/core/engine/engagement_event_types.h`, 14 fields, bound in `bindings_core.cpp`): 16 classes/96 fields moved into `tools/maintenance/dto_schema` single-source ownership in one pass, bringing the registry to 114 schema modules / 229 generated artifacts. Twelve classes are fully macro-expanded on both sides (binding-registration order equal to declaration order); the four `*MaintainedBatchContract` classes are a new held-binding variant: their bindings have always omitted exactly the trailing ground field (`ground_static_task`/`ground_static_status`; archaeology confirms zero references in the old binding text), so the schema owns the full field list (`.inc` and ABI complete) while the binding stays hand-written under a `NOTE(I35)` marker without exposing any new Python attribute. GPU packed views were surveyed with zero command/tasking references under `src/gpu/**`; `mission_command_codec.h/.cpp` was surveyed as the concrete instance of the T1 "JSON codec alias" risk and contains only free functions over JSON, hence no schema target. Three source-text boundary tests adapted (shell-guard expansion pass with nine field-line pins restated in the expansion helper's bare-declaration rendering; four binding-style markers narrowed to the type-only prefix; sixteen freshness-gate registrations); one wp24 text-scan red caused by the new binding style was fixed in the same pass. Payload at landing bytes: 57 files, 1,803 insertions, 208 deletions (9 modified files +255/-208 plus 48 new files totalling 1,548 lines: 16 schema sources, 16 generated includes, 16 generated builders). | GPT review round 1: approve with zero blockers and three non-blocking corrections, all reflected in this entry. The reviewer mechanically verified all 16 classes' ABI (header/schema/`.inc` three-way byte equality including alias tokens and enum defaults), independently confirmed the four held classes expose no ground field on either build's `dir()` while their `.inc` files carry the full field list, re-verified the never-bound archaeology, reproduced parity 18/18 (16 migrated plus 2 sentinels, enum-folding probe) on a freshly rebuilt `ef_py`, and re-ran `ctest` (8/8), smoke (`436/45` at the 48c86c4b base), ruff and `git diff --check`. Corrections: the implementer's "127 artifacts" figure matched no real count and is restated as 114 schemas/229 artifacts; register line counts are restated at landing bytes (the reviewer observed a benign +2-line drift in `bindings_runtime.cpp` during its interrupted review window, re-audited everything against current bytes, and the landing verified change-for-change identity against exactly those bytes); a superset regression sweep surfaced one additional pre-existing machine red beyond the known ledger (`test_wp12_runtime_facade_does_not_gain_a_second_maintained_injection_api`, reproduced on a clean baseline worktree), which this landing adds to the T6 residual ledger. Landed-tree maintained smoke recorded below. |
| I37 Xmacro expansion-helper newline fix and strict-regex unification | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T6, closing the helper defect registered by the I33 review. The fix is a single regex change: `tests/support/xmacro_text.py`'s `_INC_INCLUDE_RE` no longer consumes the `#include` line's trailing newline, so a fully macro-owned struct renders like a hand-written one (newline before `};`) for both `expand_header_field_incs` and `expand_binding_field_incs`, which share the pattern. Four targeted unit tests were added (`tests/support/test_xmacro_text.py`, built around the real `engagement_entity_ref.inc`), each proven red against the pre-fix helper -- including an exact reproduction of the neighbour-struct swallow -- and green after. The two regex relaxations forced by the old behavior (`test_engagement_contract_shape.py`, `test_runtime_dto_contracts.py`) are restored to the strict `\n\}};` form; the I35 bare-declaration pins in the shell guard were verified orthogonal and untouched; the two latent strict-regex sites (`test_typed_platform_spawn_contracts.py`, `test_policy_contract_shape.py`) are now correct by construction rather than by header-layout luck, re-verified green with zero changes; a repo-wide scan shows zero `\n?\}};` residues in Python. The T6 ledger's section 2 is marked fixed-at-I37 with a new evidence subsection, and the bilingual registry hash for the ledger pair was refreshed in the same pass. Payload: 7 files, 355 insertions, 32 deletions (6 modified plus the new 107-line unit-test file). | GPT review round 1: approve with zero blockers and two non-blocking transcription corrections, both applied at landing. The reviewer independently replayed the red/green construction on an isolated probe tree (4 tests red on the pre-fix helper, green after; binding-side pinned by the fourth test), re-ran the nine affected boundary files (`64 passed, 1 failed`), re-scanned the strict-regex unification, verified the ledger/registry synchronization against the official normalization (its own initial raw-byte mismatch was its tooling error), and re-ran smoke (`439 passed, 45 subtests`, no drop) plus governance suites (20/20). The one failure drove the review's key lineage triage: `test_wp24_python_command_chain_business_writes_use_maintained_contracts` is green at 48c86c4b and red from c2952d61 onward -- an I34-introduced guard-adaptation gap (command-chain writes sank into `_shared_ops.py`, which the guard's synthesized scan set does not cover; behavior tests 23/23 green; `adapter.py` carries its tokens at all commits, so the implementer's "not yet threaded through adapter.py" phrasing and its "pre-existing environment red" grading were both corrected in the ledger at landing, and the gap is registered as ledger section 6 attributed to I34 with the repair direction of extending the guard scan set). Landed-tree maintained smoke recorded below. |
| I38 C++ include-direction gate and facade result-projection dedup (T3 first slice) | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T3 first slice: the executable boundary facts the physical link-unit split needs. New `tools/architecture/cpp_include_graph.py` scans every `#include` edge under `src/**` (387 files, 1,065 maintained edges, zero unclassified) with compiler-order resolution (including-file directory first, then `src/` root), classifying into 16 fine-grained groups whose ground truth is each `src/**/README.md`'s own Allowed/Forbidden/Dependency-Direction text, rolled up into the T3 coarse layers. A naive 19-violation count was refined per-edge against the README texts down to six real pre-existing violations, all genuine structural couplings rather than include-path mistakes (a `components/` DTO holding Kalman-seeker filter state by value; `WorldBatchRuntime` owning `ExecutionEpisodeController` batch state and including `gpu/` visual/broadphase headers at three sites; a contracts DTO borrowing mission-owned batch-prep types through an X-macro splice -- one candidate looked statically removable but broke the `ef_py` build on verification and was restored). All six are ratcheted in `tests/architecture/fixtures/cpp_include_direction_allowlist_20260720.json` with file+line+include-text fingerprints, each carrying an attribution and a pointer to the physical-split slice. New gate `tests/architecture/governance/test_cpp_include_direction.py` (7 tests, added to the smoke manifest) enforces zero new violations plus three coarse-layer assertions (facade zero inbound, content direction limits, tools/tests zero inbound), an allowlist-sanity check, and a synthetic-tree negative self-test; a real-file inject-red/revert-green drill was also run. Facade result-projection duplication was measured across all eight `runtime/facade/*.cpp` files: `runtime_facade_packet.cpp`'s two export paths now share `for_each_distinct_export_world_index` and `filter_valid_world_refs` helpers (net +11 lines on the file; the win is single-sourced logic, not line count); two larger candidates were measured and explicitly deferred -- the twin typed-platform-spawn materializers (~255 lines) are deliberately locked textually-independent by an existing WP20/WP22 boundary guard, and the counterfactual parent/branch mirroring (~110 lines) sits on the replay critical path this track names as its own risk. No DTO, ABI, or public-header changes. Payload: 5 files -- 2 modified (+55/-44) plus 3 new files totalling 746 lines (444/233/69). | GPT review round 1: approve with zero blockers and three non-blocking corrections, all applied at landing. The reviewer independently recomputed the census (387/1,065/0, four self-built resolution cases correct), spot-verified five README-authorized exclusions against the README texts, code-verified all six violations' structural nature (including the X-macro-spliced dependency), re-ran the gate (7/7, ~6-8s, judged an acceptable ~6% of smoke), assessed the line-number fingerprint brittleness as intentional ratchet design with bounded maintenance cost, replayed the negative self-test, verified the two projection helpers line-by-line semantically equivalent (first-seen ordering, identical predicates, anonymous-namespace-only), confirmed the WP22 mutual-exclusion guard really does forbid sharing the two materializer bodies, and re-ran ctest 8/8 plus smoke `446 passed, 45 subtests` (439 baseline plus the seven gate tests). Corrections: the new-file line count is 746 not 665; a dangling docstring reference to an uncommitted one-off census script was removed at landing; and three pre-existing reds absent from the T6 ledger were reconciled and added at landing (the diagnostics script-governance red, plus two air-combat calibration-drift reds that the reviewer reproduced against the 2026-07-18 pre-change binary -- a stronger inherence proof than same-binary stash comparison). Landed-tree maintained smoke recorded below. |
| I39 wp24 command-chain guard scan-set adaptation | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T6, closing ledger section 6 (the I34-attributed guard-adaptation gap). `test_wp24_python_command_chain_business_writes_use_maintained_contracts` now splices `python/rl/runtime/world_batch/_shared_ops.py` into both of its local scan variables (vec_env synthesis and cooperative) inside the test function itself, with a `NOTE(I39)` comment documenting the I34 sink -- the shared `helpers.py` source table is untouched, so its only other consumer is unaffected, and the assertion sets are byte-unchanged (the negative legacy-token loop now also scans `_shared_ops.py`, making the guard strictly stronger). Negative self-proof: an out-of-tree rehearsal script intercepted only `_shared_ops.py` reads with an in-memory copy downgrading one maintained write to its legacy form, and the guard went red as expected with the worktree byte-untouched. The T6 ledger's section 6 is marked fixed-at-I39 with a new 6.1 evidence subsection. Payload: 4 files at landing -- the guard test (+16/-2), the two ledger documents, and the bilingual-registry hash refresh performed at landing per the iteration brief. | GPT review round 1: needs-repair with a single finding -- the final write set left the ledger pair's bilingual-registry hashes unrefreshed, so manifest smoke on the source worktree read `438 passed, 1 failed` (the registry flag raised by the ledger edit itself; green on an isolated 745ce1ce baseline), while the implementer's reported `439/45` had been measured before the ledger edit. Per the iteration brief the registry refresh was explicitly a landing-side duty, so no repair round was dispatched: the landing performed the refresh, corrected the 6.1 verification block's smoke line to the accurate account, and re-verified the landed tree green. Everything else was verified by the reviewer first-hand: the three-segment scan-set structure, the splice correctness on both variables with zero assertion loosening (boundary analysis: newline separation, no false-positive or false-negative edge found, `_shared_ops.py` carries all six maintained tokens and zero legacy tokens), `helpers.py` untouched with its other consumer green (14 passed across both files), an independently reconstructed negative rehearsal (different sabotage method, guard red, no residue), target file `3 passed`, `runtime_facade` directory `67 passed, 4 failed` (the known wp22 quartet, zero new), wp12 unchanged, ruff and `git diff --check` clean. Landed-tree maintained smoke recorded below. |
| I40 Cooperative/leader live wiring onto the execution-mode plugin | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T2 third slice, closing the I34 residual "plugins registered but no production consumer". Cooperative `step_wait` and leader `step_indices` now route their post-step behavior-update through `_mode_plugin.update_post_step_behavior` and gate their post-behavior command-chain sync on `skip_post_behavior_command_sync`, with the plugin resolved once at construction via `resolve_execution_mode`; both plugins keep base-class default hooks, so the routed behavior is verbatim-equal to the removed inline code (the `update_behaviors(sim_time, truth=..., inst=..., sync_to_kernel=False)` call including keyword forms is unchanged, and the new sync gate is constant-false for these modes). The two modes deliberately occupy only two of the three execution decision points: `finalize_post_step_truth` is not routed, recorded as a considered decision rather than an omission -- the cooperative constructor fail-fasts on the air-combat-hybrid action mode (pre-existing ValueError naming the missing gate/finalization contract), the leader group's own stepping path has no finalizer or air-combat reference (its inner `WorldBatchVecEnv` carries a `StandardExecutionPlugin` that owns hybrid finalization on its own path), and routing a guaranteed no-op per slot would only add hot-path overhead; a source-pin test asserts `step_wait` contains no finalize call so any future unlock must edit the test explicitly. Behavior equivalence is proven at three layers: exact-argument mock pinning (a precise call tuple, not a loose assert-called), call-site source-structure pinning, and `is`-identity pinning of both plugins' hooks against the base class (any future override turns red and forces an explicit decision plus dis coverage). `core.py` changes are docstring-only (+24/-18), bringing both plugin descriptions in line with the new facts. Payload: 4 files, 179 insertions, 32 deletions; core contract tests grow 84 to 99 (15 new items across 14 functions). | GPT review round 1: approve with one non-blocking count-caliber correction, applied in this entry. The reviewer line-verified all three production diffs (arguments verbatim-equal including `sync_to_kernel=False`; the sync gate constant-false; leader `sync_command_chain(target_indices)` preserved), confirmed the cooperative hybrid rejection is pre-existing at the baseline commit and judged the non-migration decision correct (construction-time fail-fast is the guard; a per-slot no-op call would be pure overhead), verified the mock tests pin an exact argument tuple, confirmed the sole loader instance-attribute patch seam still works through the plugin's attribute lookup, re-ran every suite (core 99 green including I32's 84; `world_batch`+`runtime/facade` 250 passed with the same 5 flecs reds -- restating the implementer's "240 to 255" pass-plus-fail figure in pass-count caliber as 235 to 250; multi_agent plus leader 87 green; wp24 guard 3/3) and self-ran the manifest smoke the implementer had omitted (`446 passed, 45 subtests`). `_shared_ops.py` untouched; the ten `project_world_*` reference sites unaffected; write set disjoint from I41. Landed-tree maintained smoke recorded below. |
| I41 Include-direction ratchet convergence: six-item evaluation and the Missile-EKF relocation | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T3 second slice. All six of I38's ratcheted include-direction violations received a full consumer/binding census and a written verdict. One converged: (a) `components/weapon_common.h`'s `Missile` embedded `SeekerEkfState`/`SeekerEkfParams` by value with exactly four touchpoints repo-wide and zero Python bindings, so both structs moved byte-identically into a new components-owned leaf `src/components/combat/common/missile_seeker_state.h` (29 lines); `kalman_seeker.h` includes it back (models-to-components is policy-allowed), `weapon_common.h` swaps to the new leaf, and `default_guidance_model.cpp` gains the direct include the relocation's broken transitive chain used to provide. Five deferred with upgraded evaluated-design-gap attributions: the three `gpu::` edges and the `ExecutionEpisodeController` batch-ownership edge are multi-site functional couplings on the WP4 hot-path/GPU seam (pointed at T4 exact-runtime alignment), and (f) -- `WorldExecutionEpisodeStepRequest`'s borrowed `StepEvaluationBatchConfig`/`EnvState` -- got the deepest census: 57 `def_rw` bindings in `bindings_episode.cpp` plus ten mission-owned aggregate types embedded by value in `EnvState`, so a verbatim move would only invert the violation and a contracts-owned mirror would duplicate the nested-type graph; deferred as a T1-DTO-completion-scale migration, not a T3 mechanical move. Allowlist net 6 to 5 with the five surviving entries' reasons upgraded (`allowlist_version` v2); T6 ledger gained section 7 with the full evaluation matrix. The iteration's focused regression also surfaced a pre-existing red in `test_ground_runtime_lifecycle_bridge.py`, lineage-triaged by the review as the I34 sink's sibling guard gap (green at 48c86c4b, red from c2952d61; the I39 repair covered only the wp24 guard file) -- registered in ledger section 6 at this landing and fixed by I42. Payload: 7 files, 441 insertions, 41 deletions (6 modified plus the new 29-line leaf; zero CMake changes). | GPT review round 1: approve with zero blockers and three non-blocking corrections, all applied at landing. The reviewer verified the relocated structs byte-identical raw (comments and banner included), independently re-swept the touchpoint census (exactly the four named files, no fifth consumer; the `default_guidance_model.cpp` transitive-chain repair legitimate; models-to-components confirmed within the I38 policy and the components README's leaf charter), replayed the ratchet's stale-fingerprint detection (the old allowlist against the current tree yields exactly one stale entry and zero unexpected -- the gate does detect the fix itself), recomputed the (f) census (57 `def_rw` = 15 + 42; the ten embedded types verified) and endorsed the deferral verdicts (with one wording note: edge (b) counts three call sites plus a type-construction site rather than "four call sites"), reproduced the kalman doctest (3 cases, 17,423 assertions, zero failures), ctest 8/8, gate 7/7, and reconciled all six focused-regression reds against the ledger. Corrections: the ledger section 7.4 zero-bindings sentence contradicted section 7.3's own citation of the (f) bindings and was rewritten at landing ((b)-(f) types do carry bindings elsewhere but those edges were untouched; only (a)'s types are never bound); the new file is 29 lines not 24 (merged payload +441/-41); and the expected bilingual-registry smoke flag from the ledger edit was resolved by this landing's registry refresh. Landed-tree maintained smoke recorded below. |
| I42 Ground-bridge command-chain guard scan-set adaptation | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T6, closing the sibling guard gap registered in ledger section 6 at the I41 landing (I34-attributed: the guard asserted vec_env/cooperative import `build_kernel_mission_command` directly from `bridge.py`, while the I34 sink moved that call into `_shared_ops.py`; the I39 repair covered only the wp24 guard file). `test_batch_envs_use_tasking_bridge_for_command_chain_sync` now folds `_shared_ops.py`'s source text onto each per-file scan (`read_text() + "\n" + shared_ops_text`, the same local-splice pattern as I39, single read, `NOTE(I42)` comment restating the framing per the sink facts) and runs its nine original `assertIn`/`assertNotIn` checks unchanged -- five positive tokens all present in the sunk call chain, four forbidden legacy tokens all absent, with the negative loop now also covering the module that physically performs the writes (strictly stronger). Negative self-proof rehearsed out-of-tree: intercepting only `_shared_ops.py` reads with an in-memory copy renaming `build_kernel_mission_command` turned the guard red as expected, with the worktree file byte-untouched. The ledger gained section 6.2 with the fix-and-reverification evidence. Payload: 3 files, 212 insertions, 1 deletion (guard test +23/-1 plus the bilingual ledger subsection). | GPT review round 1: approve with one non-blocking count correction applied at landing (the ledger had written "eight" assertions in four spots across both languages; the guard actually runs nine -- five positive plus four negative -- and the token-presence phrasing was restated precisely). The reviewer verified the splice diff assertion-for-assertion (zero loosening; the -1 line is the replaced `text =` assignment), ran the subtlest correctness point in real Python (no maintained setter name contains its legacy counterpart as a substring, all four pairs False), independently reproduced the negative rehearsal with a different sabotage form (renaming a maintained setter; guard red, clean revert, file hashes identical), confirmed the target file `4 passed` against an isolated baseline where the sole red was exactly this node, re-ran the mission directory (`90 passed, 8 subtests`) and smoke (`445 passed, 1 failed, 45 subtests` -- the sole red being this very ledger pair's registry flag, with the landing-side refresh convention evidenced by the I39/I40/I41 landing commits each carrying the registry file), and explicitly distinguished this from the I39 blocking (the implementer here reported the pre/post-edit numbers accurately rather than overclaiming). Landed-tree maintained smoke recorded below. |
| I43 T4 first slice: per-step builder double-ownership census and exact-runtime plan re-freeze | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T4 first slice; documentation-only (+257/-11 across the two exact-runtime plan documents after the review-driven wording refinement). A repo-wide census of hand-written per-step DTO construction under `python/**`/`gym_envs/**` against the landed C++ paths found the execution-step hot path is three coexisting tiers, each with a real production trigger or dedicated regression consumer: (1) `compute_full_step` as the true default orchestrator, consuming the sub-products of one aggregated `compute_execution_episode_runtime` call term-by-term (genuinely per-term compiled calls surviving on cache-miss and escape-hatch paths -- wording refined at landing per review); (2) the opt-in `execution_step_batch_prepare` batch layer; (3) the opt-in `execution_episode_controller_mainline`/`shadow_compare` cutover, default-off and feature-incomplete (post-launch assessment explicitly disabled under mainline). Zero class-(A) retirements at this commit -- both retirement hypotheses the census tested were refuted by concrete evidence, honestly recorded. One narrow pair was precisely adjudicated: `prepare_step_evaluation`'s frame-vs-episode `elif` dispatch is production-unreachable (both enabler predicates share one gate and one unconditionally-compiled binding unit) yet exercised by a dedicated monkeypatch test with independent C++ contract consumers underneath, so it is held, not retired. The plan documents were re-frozen (status line, WP1/WP2/WP4 landed-fact notes, a new census section with the explicit retirement trigger: mainline covering post-launch assessment plus all flight-shaping options and becoming default). | GPT review round 1: approve with zero blockers, two non-blocking wording items (the tier-1 phrasing was refined at landing from "per-term compiled calls as the real default" to "per-term consumption of the aggregated compiled product", since the default path makes one aggregated call -- the reviewer verified every census claim against code: the three tiers' flags and defaults, the mainline post-launch disable at `_air_combat_post_launch_mixin.py:51-52`, the shared-gate unreachability argument including zero conditional compilation in `bindings_episode.cpp`, and the WP1/WP2/WP4 fact notes) and one handover warning: during the review's final window the worktree received concurrent writes to four non-I43 code files (pure deletions, -172 lines); the reviewer re-verified all census facts against HEAD blobs unaffected, and the landing extracted exactly the two document files. Verification: focused regressions `491 passed, 1 skipped, 254 subtests, 5 failed` (the four wp22 reds plus the lazy-binding gap, all ledger-known; the ground-bridge red is green since I42); ctest 8/8; maintained smoke `446 passed, 45 subtests` unchanged (the exact-runtime plan pair sits outside the strict-bilingual registry, verified against `STRICT_PLAN_SUBTREES`); ruff and `git diff --check` clean. Landed-tree maintained smoke recorded below. |
| I44 T5 second slice: opt-in report envelope and second argparse batch | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T5, completing the track's remaining two deliverables on I30's Experiment-face owner and I20's shared-argument-group precedent. Argparse batch: a repo-wide survey of `tools/**` plus the operator entry points landed three groups -- (1) the I20 trio (`add_probe_run_args`/`add_model_load_args`/`add_json_out_arg`) extended to nine previously-deferred partial-match call sites including `evaluate.py` (whose `--algo` enum rides a new backward-compatible `choices=` passthrough), (2) a new `add_kces_before_report_args` group covering the four KCES before-report consumers, (3) a new `add_retained_gate_output_args` group in `manifest_integrity.py` covering the three independent-review retained-gate CLIs. Five surveyed families deferred with stated reasons (the ~26-30-file evidence-packet family, kill-chain field-set mismatch, `--collector_action` genuine divergence, `--fire_range_m` time-boxed, `--repo_root` touching freshness generators). While exercising `required=True` against a dual-alias dest for the first time, a latent I20 defect was found and fixed: `add_dual_option` registered two independently-required actions on one dest, making required-dual unusable (either spelling alone was rejected); the pair is now a required mutually exclusive group -- either spelling satisfies the requirement, `--help` stays byte-identical, and the docstring records both semantic shifts (neither-form error text; both-forms now raises the mutual-exclusion error where the old unreachable path would have let the later value win -- wording added at landing per review). Report envelope: new `python/experiment/report_envelope.py` (pure stdlib, zero bootstrap side effects) wraps a verbatim payload with `envelope_schema_version`/`tool_id`/`generated_at`/`git_rev` (best-effort)/`experiment_ref` (opaque, deliberately not resolved against `ExperimentRegistry` -- vocabulary only, staying out of T10's evidence scope); `--report-envelope` defaults off with the disabled path returning the same object, adopted by `naval_station_policy_eval.py` and `calibration_admission_audit.py` (the latter gaining the two-line I16-convention sys.path shim). Payload: 49 files, 2,331 insertions, 160 deletions (20 modified +299/-160, 7 new substantive files at 1,197 non-blank lines, 22 pinned `--help` golden fixtures). New tests deliberately not promoted into the curated smoke manifest, per that manifest's explicit-whitelist intent and I30's precedent. | GPT review round 1: approve with zero blockers, two P3 wording items (both applied at landing: the docstring note above, and this entry states the independent-review subset figure as implementer-reported since the reviewer could not reconstruct its exact selector -- all member tests verified green individually) and two ledger additions (applied at landing: the `platform_spawn` spdlog collection error and the order-sensitive `source_evidence_governance` pre-existing red, both reproduced with write sets incapable of causing them). The reviewer replayed the I20 defect on the pre-fix helper (required-dual factually unusable, hence zero compatibility loss), verified `--help` byte-identity for the required mutually-exclusive rendering empirically, rebuilt `evaluate.py`'s pre-migration parser from the HEAD source and matched `format_help()` byte-for-byte (behavior a strict superset: the new `--train-config` alias), spot-checked group help-text overrides (date-stamp examples, stdout wording), independently re-ran the envelope negative tests plus a bare-import probe (no `ef_py`/`gymnasium`/`stable_baselines3`/`torch` in `sys.modules`), endorsed the smoke non-promotion call, and recomputed every figure (112 new tests = 16+23+33+6+26+8; `tests/tools`+`tests/experiment` 322 passed/4 skipped; `tests/eval` 14/2/18; smoke `446 passed, 45 subtests` unchanged; ruff and `git diff --check` clean). Implementer-reported subset not independently reconstructed: independent-review/retained-write selection at 83 passed/4 skipped. Landed-tree maintained smoke recorded below. |
| I48 T10 first slice: evidence-and-replay spine census | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T10 opening move; documentation-only, two new bilingual reference documents (`docs/plan/unified_architecture_program/t10_evidence_spine_census_20260721.md`/`.zh.md`, 207 EN + 177 ZH = 384 lines). A descriptive census of the five evidence surfaces at this baseline -- trace ids, packet ancestry (`InformationStateSource`), snapshot versions, replay gates, and the worldline/counterfactual face -- recording per surface the vocabulary, producers, consumers, test pins, and the gap to "replayable by construction", plus an eight-item additive vocabulary-alignment checklist (VA-1..VA-8), a seven-step later-slice order with dependency annotations, and the additive-only red line (existing evidence fields are pinned by member-order ABI, JSON aliases, retained-artifact hashes, and tests; all alignment lands as new fields/producers). Key verified findings: the kernel's trace allocator is monotone only within one event-store epoch (reset on `clear()`/clock rewind) and the maintained adapter tags packets with the placeholder `trace_ids=[1]` while `parent_trace_id` is hardcoded zero on export; packet `snapshot_version` is a per-export `index+1` sequence (reset each export; counterfactual snapshots pin the constant 1 -- no run-global monotone counter exists, so a monotone-producer prerequisite slice was inserted); `InformationStateSource` travels under six field names and the lineage vocabulary is implemented twice (C++ contract types plus Python `generation_request.py`); the raw counterfactual facade owns two synthetic `ReplayEnvelope` producers while the maintained adapter/live-run path has none, and it mints several ids itself (`worldline:runtime:*`, `worldline:baseline`/`branch`, `comparison_id`, snapshot-derived `replay:facade:*`) with only some request fields caller-supplied; restore is three-tiered (WP15 `WorldlineBranchMetadata` metadata-only; `RuntimeFacade` writes back host-owned kinematics; resident/GPU/full-clone rejected). No optional read-only test was added (the surfaces carry six smoke-listed pins already; pinning the placeholders would cement exactly what T10 must replace). | GPT review round 1: needs-repair with four P1 factual errors (the census had called the per-export snapshot sequence a real monotone version, called `ReplayEnvelope` test-fixture-only, called all worldline ids caller-supplied, and called restore metadata-only) plus revisions (trace-epoch qualifier, provenance count five-to-six, VA-2/5/6/8 sharpening, a slice-order inversion, an overstated pin claim, and line-count corrections). The repair round re-verified every pointer first-hand at the baseline and restated all of them; the repair re-review (fresh reviewer) then cleared each item against source (`runtime_facade_packet.cpp` reset sites, both synthetic producer construction sites and their call sites, the id-minting lines, the kinematics write-back and rejection branches, the allocator epoch reset), reproduced smoke `445 passed, 1 failed, 45 subtests` with the sole red being this document pair's expected bilingual-registry flag (resolved by this landing's registry refresh), re-verified the link audit (164 documents, 2,652 links, zero issues), recomputed both normalized registry hashes to exact agreement, and issued approve with zero blocking and zero non-blocking findings. Landed-tree maintained smoke recorded below. |
| I45 T8 first slice: G4 information-state layer declarations and truth-leak inventory | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T8 first slice; zero runtime behavior change (declarations are pure module-level metadata plus a static gate). New stdlib-only facility `python/architecture/information_layer.py` (dependency direction `gym_envs -> python.architecture <- python.rl`) publishes the six-layer/P0-P10 vocabulary verbatim from the I32 stage-contract whitelist, a G5 registry of eight declared consumers, and a shared checker; eight maintained observation/reward modules carry `INFORMATION_LAYER_CONSUMED`/`INFORMATION_LAYER_PRODUCED`/`SEMANTIC_STAGE` constants (mission_observation, air_combat, naval, safety, shaping_inputs, objectives, the active `universal_env_parts/observations.py` assembler, and `navigation_runtime/waypoint_rewards.py`). The gate `tests/architecture/information_state/test_g4_layer_declarations.py` (14 tests) enforces presence plus authoritative vocabulary with a both-direction cross-pin to I32, reads both the whitelist and `core.py` by AST so it runs without `ef_py` or a build, accepts tuple-form declarations only (list-form pinned red), and is proven load-bearing. The bilingual reference `t8_g4_truth_leak_inventory.md`/`.zh.md` registers a C1-C19 consumer census and a TL1-TL20 truth-leak inventory (15 leak / 4 exempt / 1 diagnostic; 11 leaks declared this slice, 4 deferred with rationale -- including the shared route-guidance helper whose products feed both reward inputs and autopilot targets). Payload: 13 files, +803 (8 tracked modules +98; 5 new files 705 lines). | GPT review round 1: needs-repair with two P1 census gaps -- the first-round C16 had misread the still-active `build_universal_observation` (called from `cooperative_world_batch_vec_env.py:627` and `multi_agent_runtime.py:190`, reading `truth.x/y/contacts/rwr_warnings`) as a dead path, and the direct waypoint reward-input consumer `build_waypoint_step_state` was missing -- plus four non-blocking items (list-form declarations slipped the AST extractor; the gate imported the `world_batch` package and thereby loaded `ef_py`, contradicting its own claim; two count errors). The repair round split C16 into the dead constructor versus the active assembler (now C17/TL18 with declarations), declared the waypoint consumer (C18/TL19), adjudicated `guidance.py` as deferred (C19/TL20), tightened the extractor to tuple-only with a pinning test, rewrote the gate to read `core.py` by AST with a blocked-`ef_py` self-proof, and corrected both counts. Repair re-review (fresh reviewer): approve with zero findings -- declarations verified against actual reads, the tuple/list probe and the no-`ef_py` run independently reproduced (`14 passed` with `ef_py` absent from `sys.modules`), the census re-swept with no third missed consumer, all counts recomputed, adjacent suites `110 passed, 1 skipped, 5 subtests`, smoke `445 passed, 1 failed, 45 subtests` with the sole red being this pair's expected registry flag (resolved by this landing's refresh), and the five policy_execution/runtime_facade reds re-confirmed as the ledger's pre-existing wp12/wp22 nodes. Landed-tree maintained smoke recorded below. |
| I46 T3 third slice: `ef_core` physical link-unit split, first unit (`ef_content`) | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T3 third slice, opening the physical link-unit split with its cleanest leaf. `content/` has 18 project include out-edges (16 to header-only `components/**`, 2 content-internal, zero upper-layer; `core/interfaces/` appears only on content's in-edge side), and its sole translation unit `unit_definition_loader.cpp` (~1.9k lines) has no dynamic initialization, self-registration, or constructor attributes -- the only file-scope state is two anonymous-namespace `constexpr const char*` constants; the public `load_unit_definitions_json` is pulled by an explicit undefined reference from `simulation_kernel.cpp` via `default_unit_factory.h`. The layer is now a separate `ef_content` STATIC archive linked PUBLIC one-way by `ef_core`, with zero consumer edits (the same `src/` public include directory is re-exported) and a byte-identical compile environment (verified by comparing the TU's actual compile command against a temporary reconfigure of the pre-split CMake). Link order was verified on every downstream link line (`libef_core.a` before `libef_content.a`, MinGW single-pass safe; archive membership confirms no double compilation). A CMake-text isolation gate in the smoke-member `test_cmake_target_readiness.py` pins the split: content sources stay out of `ef_core` (variable or literal path form, path-segment matched with `./`/`${CMAKE_*_SOURCE_DIR}` normalization), `ef_content` links no back-edge under any scope across all its `target_link_libraries` calls, its source set (add_library plus every later `target_sources`) stays the `${EF_CONTENT_SOURCES}` group, and the forward edge must exist -- the parser strips comments, tokenizes, expands `${VAR}` transitively, and masks both double-quoted strings and CMake bracket arguments so quoted or bracketed pseudo-commands neither false-positive nor satisfy assertions; eleven negative cases pin the blind spots closed across the repair rounds. One comment line in the new CMake block was corrected to the measured out-edge facts. Payload: 2 files, +678 (CMakeLists.txt +36; the gate file +642 with 13 new tests, five pre-existing tests byte-unchanged). | Four review rounds. Round 1 (approve-quality substance, one blocking): the split itself verified end-to-end -- census, symbol chain, compile-environment equality, archive membership, 10/10 link-line order, ctest 8/8, cross-build parity, focused regression with only ledger reds, smoke -- but the original fixed-layout text gate had reproducible false negatives/positives; the round also adjudicated the smoke +1 as acceptable (assertions added to an existing smoke-member file, distinct from promoting new files). Rounds 2-4 hardened the gate iteratively, each round independently re-verified with adversarial variants: round 2 closed literal-path smuggling, second-call back-edges, and comment/reflow sensitivity (before/after probes proving each legacy blind spot); round 3 closed string-literal pseudo-commands and equivalent path prefixes plus `target_sources` aggregation; round 4 closed CMake bracket-argument pseudo-commands (validity proven via `cmake -P`) and confirmed the single-quote non-boundary in-code. Final state approved with zero findings: gate file 18 passed, adversarial variants (nested `]]` inside `[==[`, unclosed brackets, quotes inside brackets, `[[` inside strings) all correct, smoke `459 passed, 45 subtests` on the source tree, ruff and `git diff --check` clean, the drift-quarantine stash untouched. One disclosed environment side effect: the round-2 implementer installed gymnasium/pytest-subtests into the global Python to run smoke (no repository impact, verified). Landed-tree maintained smoke recorded below. |
| I47 T9 first slice: authority-model census, registered agency vocabulary, and ratchet gate | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T9 first slice with zero C2 behavior change (five all-new files, 2,433 lines; no existing file touched). The maintained tasking-authority surface was censused into 14 gated scatter sites (A1-A15 minus one re-export later excluded on unified standards) plus two runtime-face cross-references and the out-of-scope policy-network face, classified across role/scope/delegation/arbitration/doctrine and a `gating` dimension introduced during review (activation and fire-eligibility gates resolve enable/disable, not producer competition). `python/tasking_contracts/agency_registry.py` is the single declarative vocabulary owner (pure stdlib, frozen, no ef_py/python.rl/gym_envs imports, zero production consumers): the SCAL five-part `AgentRole` schema with all nine roles carrying full schema fields, mirrors of the compiled authorities (`AuthorityScope`, `CommandRelationship`, `CoordinationMode`, all six `NavalWarfareRole` members, the three action-interface scopes including `mission_command`), delegation carriers, merge-policy/arbitration vocabulary with the documented `human > policy > scripted > diagnostic` source-priority order (architecture-design SS ~450), fire-eligibility gates, and a `DoctrineFamily` vocabulary placeholder. The ratchet gate (`test_authority_registry_gate.py`, 21 tests, plus a fingerprint fixture) pins per-file token counts with word-boundary matching, scans whole directories with AST/tokenize stripping of comments/docstrings/re-export plumbing while still catching prose folklore, cross-checks the registry against the compiled headers through a comment-proof extractor (C++ backslash-newline splicing before comment recognition, quote-aware stripping), and pins exact category sets for the review-re-adjudicated sites. Call-site convergence onto the vocabulary is deferred to a domain-evidence-reviewed later T9 slice. | Four review rounds, each a fresh independent reviewer. Round 1: needs-repair -- mirror gaps (missing `mission_command`, undeclared naval roles, schema-less roles, no authority-comparison gate), probe-word-distorted classifications, and four reproducible ratchet blind spots. Round 2 fixed those and round-2 review verified them but found the extractor comment-spoofable, the synonym blind spot merely documented, and A14's arbitration grading source-unsupported. Round 3 fixed those (state-machine comment stripper, word-boundary synonym tokens with regenerated fingerprints, A14 re-graded to `gating` on precise code evidence -- the fire mask is a holder-blind eligibility conjunction at `mission_observation.py:281-300` while A13 stays arbitration for its holder-id selection -- and exact-category pins whose re-flip proofs bite); round-3 review cleared all of it but caught the line-continuation comment gap, fixed in round 4 by splicing backslash-newlines before comment recognition (compiler-order semantics; the three real headers contain zero splices so extraction is byte-identical), with adversarial variants (trailing-space non-splice, escaped backslash, block-comment splice) verified compiler-consistent. Round-4 review: the P1 cleared with all variants correct, agency 21 passed, neutral layer 20 passed, smoke `445 passed, 1 failed, 45 subtests` (sole red being this census pair's expected registry flag, resolved by this landing's refresh), one P3 (the census JSON still carrying the superseded 406-407 projection line numbers) applied at landing per the reviewer's exact prescription and the gate re-run green. Landed-tree maintained smoke recorded below. |
| I49 T11 first slice: content-pipeline census | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T11 opening move; documentation-only, two new bilingual reference documents (`docs/plan/unified_architecture_program/t11_content_pipeline_census_20260721.md`/`.zh.md`, 251 EN + 195 ZH = 446 lines). A four-face census of the content-loading surface against the staged `P0 ContentCompile` model: (i) the unit-definition loader -- 1,881 lines with the sole hand-written mapper `parse_unit_json` at 782 lines, 430 `.value` calls, a semantic top-level key surface of 106 (54 directly accessed plus 52 read by the missile-tuning helper that receives the whole entry), five codec escape-hatch families pinned by line (fuze/fuse dual spelling, flat/nested engine paths, four sensor variants, three-source missile-tuning merge, string-or-object dependency polymorphism), feeding `UnitDefinition` (58 direct members) and `MissileTuningDefinition` (56 fields), consumed by `DefaultUnitFactory::spawn` (619-1439, 821 lines, capability-gated hand materialization) over a 27-definition-file disk surface (11 unit platforms, 12 modules, 3 weapons, 1 facility; 29 JSON minus two evidence descriptors excluded by direct-parent-path comparison only); (ii) the scenario chain, already the most P0-like staged pipeline (parse, validate, merge/resolve, ingest, re-validate, typed `CompiledScenario`, materialize via `spawn_unit`), meeting face (i) only through the `type_name` string (gap G-A); (iii) the T5 experiment matrix, adjudicated out of T11 scope; (iv) validation/defaults/versioning -- structural-only unit parsing with uncaught `std::stoi`/`get<std::string>` throw paths outside the filesystem-only catch, scenario `TypeError`/`ValueError` split, no top-level content schema version. Gap matrix G-A..G-E (headlined by G-B: the hand-written parse feeding schema-owned output contracts), a seven-step later-slice order, and red lines including content-JSON freeze, ABI, escape-hatch preservation, and the I31 angle-bracket-comma X-macro blocker with `default_loadout` (`std::unordered_map<int, std::string>`) named for explicit adjudication before table-driving the loader. No optional convergence performed. | GPT review round 1: needs-repair -- the reviewer confirmed most of the census (line counts, mapper range, `.value` tally, escape hatches, G-A single channel, G-B asymmetry, the P0 sub-stage "analysis lens" labeling) but found four P1 facts that would have misled later slices (the load surface missed 16 module/weapon/facility files; the key count missed the helper's 52; both DTO member counts were wrong; the red lines missed the I31 comma blocker) plus three P2s (incomplete error-shape census, spawn range off by the closing brace, over-strong exclusion wording). The repair round fixed all seven with per-item source re-verification (27 files enumerated by category with cross-reference proof from `f16c_block50.json`; the two key sets compared element-wise disjoint with `max_flight_time_s` verified live at top level in `aim_120c.json`; members mechanically recounted; the I31 ledger precedent cited). The repair re-review (fresh reviewer) independently recomputed every figure (29-2=27 with the exact module split, 54+52 with zero intersection, 58/56, 619-1439/821), verified the `default_loadout` type against the I31 held precedent, reproduced smoke `458 passed, 1 failed, 45 subtests` with the sole red being this pair's expected registry flag (resolved by this landing's refresh), recomputed both normalized registry hashes to exact agreement, and issued approve with zero findings. Landed-tree maintained smoke recorded below. |
| I50 T8 second slice: declared observation-view convergence | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T8 second slice, the convergence move the I45 inventory's §5 called for. A new stdlib-only view owner `gym_envs/observation_view.py` (221 lines, 16 layer-tagged faces: own-ship field/attr reads, the C1-variant and naval-variant target-track lookups, own-missiles count, support-unit shared-tactical-picture reads, engagement evidence, unit liveness/health, contacts, RWR warnings, and explicitly diagnostic `debug_*` faces) is sunk at the `gym_envs` parent-package layer per G2 -- `scenario_loader` and `universal_env_parts` both consume it downward and `universal_env_parts` keeps zero lateral imports of `scenario_loader`. The eight declared consumers (C1, C4-C8, C17, C18) migrate onto the view, structurally converging 11 declared truth leaks (TL1/2/4/5/7/8/10/11/12/18/19 flipped to converged-via-declared-view in the bilingual inventory; §4 distribution now 11 converged / 4 leak-deferred / 4 exempt / 1 diagnostic; new §6 slice record with §6.5 repair note). `python/architecture/information_layer.py` gains the view-owner and converged-consumer registry tuples, and a new AST truth-read-ban gate (`tests/architecture/information_state/test_g4_truth_read_ban.py`, 196 lines, 12 tests) bans raw `truth.<attr>`/`getattr(truth, ...)` reads in migrated consumers with load-bearing in-memory negative proofs, a line-precise `g4-diagnostic-truth-read` whitelist, a view-owner-exclusion proof, and honest detection-boundary notes. C17's compiled-kernel whole-truth pass-through and the deferred C11-C14/C19 paths stay untouched by design. 13 files, +837/-200. | GPT review round 1: needs-repair -- one proven P1 behavior drift (the shared target-track face silently normalized C5 naval's `target_id <= 0` guard to `int(target_id) <= 0`; the reviewer's isolated probe showed baseline `0` vs changed `None` for `target_id=0.5` with an id-0 track, and `TypeError` vs match for string ids) plus one P2 layering violation (C17's migration added the first-ever `universal_env_parts -> scenario_loader` lateral import, against G2; the view owner originally lived inside `scenario_loader`). Repair round: a dedicated `naval_target_track` face token-for-token replicating the naval original (C5 delegates to it; C1 keeps its own variant -- the two baseline guards genuinely differ), plus the G2 sink to `gym_envs/observation_view.py` with all imports, registry paths, gate text, and bilingual doc references moved and zero old-path residue. Repair re-review (fresh reviewer) approved with zero findings: 16 view faces enumerated with none unreferenced, 4 replica faces AST-equal to their baseline originals, 117 consumer functions AST-checked with zero mismatches, the C1/C5 guard split proven from `git show` baseline sources with three-way probes (baseline/view/consumer) identical on both drift scenarios, dual-tree behavior comparison identical (target selection 60 passed/15 subtests/4 failed -- the four pre-existing NumPy `asarray(copy=)` machine reds; broad selection 255 passed/8 subtests/4 collection errors from missing `stable_baselines3`, runnable-node-set SHA256 equal across trees), info-state gate 26 passed with the negative proofs verified load-bearing, and smoke 458 passed/45 subtests/1 failed with the sole red the expected bilingual-hash landing flag (resolved by this landing's registry refresh). Landed-tree maintained smoke recorded below. |
| I52 T11 second slice: content escape-hatch schema survey and draft | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T11 second slice, per the I49 census's later-slice order step 2: docs + schema draft, no parse change. Three new files: the bilingual survey (`docs/plan/unified_architecture_program/t11_content_schema_survey_20260721.md`/`.zh.md`, 407/338 normalized lines) and a machine-readable, deliberately un-wired draft (`tools/maintenance/dto_schema/drafts/content_unit_schema_draft.json`, 2,162 lines) mirroring the T1 `Field` model with content extensions (`forms_aliases`, `polymorphic`, sentinel legend). The survey specifies all 106 semantic top-level keys (54 direct + 52 via the missile-tuning helper, disjointness re-proven) key-by-key: read site under the call-line convention (multi-line statements cite the actual `.value(...)`/`parse_vector(...)` call line, now written into §0), target DTO member with line, measured distribution across the 27 definition files ([U11/M12/W3/F1]), sentinel semantics with a dedicated PRESET category (sensor 30 km, sonar 25 km, engine/aero tuning defaults, landing-gear literal preset, flight-model block fallbacks, embarked-air-ops block default), and escape-hatch family membership now including the nested dual forms (fuze radius triple alias plus trigger_logic|trigger_mode, failure_modes array|object plus weights overlay, damage-geometry nested-vs-flat dual path). Also registers 7 present-but-unread top-level keys (4 underscore annotations plus `ew_suite_ref`/`rcs_profile_ref`/`rcs`, which the loader never assigns -- factory-side parsing acts on empty refs; adjudication routed to the next slice; the 106 total is unchanged by definition), and the X-macro comma-blocker table: exactly one member across all 58+56 DTO members (`UnitDefinition::default_loadout`, `std::unordered_map<int, std::string>`, `unit_definition.h:190`), recommended HELD per the I31 precedent -- the explicit pre-adjudication input step 4's table-driving requires. | GPT review round 1: needs-repair -- the skeleton held (key sets, distribution, unread keys, single comma blocker, un-wired draft all independently recomputed) but five P1 groups were found: five helper read-site line numbers off by one, the sensor four-form family mis-drawn as one mutually-exclusive chain (`sensor_ref` is an independent `if` that stacks with the later three-way chain, proven live by the DDG-51 file carrying both), `name` mis-marked required (it falls back to `type_str`), four codec preset defaults mis-classed as value-init `{}`, and missing nested dual-form escape hatches. Repair round 1 fixed all five and proactively surfaced four more same-class facts in the full-table recheck (landing-gear preset, per-mount sensor/sonar presets, literal-preset legend note, flight-model block fallbacks). Re-review (same reviewer, independent recount) confirmed those but caught nine remaining helper line numbers still citing multi-line assignment start lines; repair round 2 fixed the nine under the newly-written call-line convention and self-found six more same-type references in the guidance-alias section. The final confirmation pass re-ran the full-table extraction under the strict on-line-literal criterion: 106 keys three-way consistent (EN/ZH/JSON), all 310 line references (198 read sites + 106 target declarations + 6 guidance aliases) zero-mismatch, bilingual counts equal, generate --check fully up-to-date with a clean generated tree (structural proof the draft stays un-wired), link audit clean, and smoke 458 passed/1 failed/45 subtests with the sole red the expected unregistered-pair flag resolved by this landing; approve. Landed-tree maintained smoke recorded below. |
| I51 T10 second slice: additive evidence glossary | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T10 second slice, per the I48 census's later-slice order step 2: documentation only, zero code and zero field changes. Two new bilingual reference documents (`docs/plan/unified_architecture_program/t10_evidence_glossary_20260721.md`/`.zh.md`, 370/313 lines) forming the authoritative id/version vocabulary over the five evidence surfaces: 69 fields total (13 uint64 identity, 27 string identity, 9 untyped ancestry `*_ref`, 12 state-slice version, 8 format/schema version), each with declaration pointer, producer census (who assigns, when it resets, declared default vs production-path actual value, caller-constructible vs facade/kernel-produced), consumer/test pins, and VA-1..VA-8 mapping. Key recorded facts: the engagement event store's single resettable counter mints ids for every kernel event family it records (12 minting sites across 11 families plus the effects four-tuple, lethality-chain ids landing in `LethalityChainHeader.event_id`; reset by `clear()` and clock rewind); export barrier values differ from declared defaults (`kExportBarrierSequence = 1` vs default 0); numeric-into-string textual crossings are non-exhaustively catalogued (worldline ids, restore snapshots, the world-batch adapter's `obs:{world}:{entity}` composition, packet-provenance version embeddings); and the vulnerability schema-version pair is split by real field names (`vulnerability_evidence_schema_version` on EffectsResult/EffectsEvent vs upstream `evidence_schema_version` with its descriptor-to-profile-to-adjustment-to-result copy chain). The optional dto_schema metadata sub-task was evaluated and dropped with a scoped rationale (no semantically-appropriate existing channel; a dedicated attribute is a model/generator code change outside this slice's zero-code red line). The census's two small factual errors found during verification (ancestry field-name prefixes; `window_commit` belonging to `barrier_detail`) are recorded in §5 without editing the immutable census. | GPT review round 1: needs-repair -- four P1 (systematic declaration line-number offsets from miscounting 4-line `.inc` headers; the "all uint64 identities share the engagement allocator" overclaim; DTO-default vs export-actual confusion on `barrier_sequence`; several fields mis-marked caller-only despite raw-facade producers) plus two P2 (7 census-named fields missing; over-strong "no doc-only channel" argument) and a CRLF P3 adjudicated as directory precedent. Repair round 1 regenerated all pointers by script, narrowed the allocator claim, split declared-vs-actual values, added the facade producers, added 7 rows (62 to 68), and rewrote §6. Re-review (fresh reviewer) verified 68/68 rows and the 10 targeted facts but found three more P1 by extended search (allocator set still missing 9 minting families; "only two textual crossings" still absolute; wrong field name for the vulnerability schema version) plus the zh-companion linking the English architecture doc. Repair round 2 enumerated all 12 minting sites with owning-function proof, made crossings non-exhaustive with the adapter and provenance embeddings added, split the vulnerability pair by real names with the full copy chain (68 to 69), and fixed both zh links. The confirmation pass verified all 12 minting sites against a full-file counter census (15 increments + 1 reset, no 13th site; the training-projection family proven to have no store recording path), re-ran the full-table recomputation (69/69 rows, 182 resolvable pointers per language, sets equal), re-ran smoke on the post-I50 HEAD (458 passed/1 failed/45 subtests, sole red the expected unregistered-pair flag resolved by this landing), and issued approve. Landed-tree maintained smoke recorded below. |
| I53 T9 second slice: mechanical authority-vocabulary convergence | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T9 second slice: the zero-semantic-risk mechanical subset of the call-site convergence the I47 census's §7 deferred. Per-site adjudication of all fourteen censused scatter sites (new census §3.2 matrix, EN/ZH) found the narrow A1-A14 mechanical subset **empty** -- every site is a compiled `ef_py` enum access (the registry is a mirror of that compiled truth), a schema-owned DTO field-name key (T1's category, not T9's), or authority control-flow/prose folklore itself (the deferred semantic material) -- with a one-off equivalence proof (24/24: registry mirrors equal the live compiled enum sets; every site literal is byte-identical to its registered vocabulary, now presented in four columns: enum mirrors / delegation carriers / ROE pattern fields / role-author fields). The review's own maintained-surface hunt then found the one true mechanical site *outside* the A-list -- `python/rl/runtime/agent_shim.py` locally replicating the registry's five-entry merge-policy set -- which the repair round converged: `ALLOWED_MERGE_POLICIES` now *is* the registry's `MERGE_POLICIES` tuple (identity, not copy), the five named `MERGE_*` constants keep their literals for greppable call-site vocabulary, and a new anti-drift unit test pins identity, tuple type, order, and each name's value against the registry (smoke count moves 459 to 460 because the test file already sits in the smoke manifest). Ratchet fixture and gate untouched by proof: agent_shim lies outside the census scan roots and merge-policy strings are not probe tokens. The census's A6 row was corrected twice along the way: "leader precedence" (naval has no `leader_intent`) became "config-over-runtime override", which the re-review also disproved (loading.py binds `scenario_data["mission_command"]` and `loader.mission_cmd` to the *same dict* at load, re-synced at finalize), landing on the final wording -- authority/ROE-field re-reads are an idempotent pseudo-priority over one shared mapping, and whether runtime rebinding (e.g. the multi-agent clone-restore path) creates a real separation is explicitly unadjudicated and parked in the precondition column. Matrix conclusion in all three places: narrow 0/14, plus 1 hunted convergence; semantic convergence stays deferred with per-site preconditions. 4 files, +262/-16. | GPT review round 1: needs-repair -- the hunt found the missed agent_shim mechanical site (overturning the "empty subset" claim), the equivalence-proof wording over-claimed carrier/doctrine coverage for two role-author fields, and A6's stated arbitration source didn't match the source. Repair round 1 implemented the convergence (option B with the anti-drift test), fixed the proof columns, and rewrote A6 -- whose new "config-over-runtime" framing the fresh re-reviewer then also disproved against loading.py's same-dict binding (single remaining P2). Repair round 2 landed the final idempotent-pseudo-priority wording with the runtime-rebinding question parked as a precondition. The confirmation pass verified the same-dict facts line-by-line (loading.py:113-117/:242, naval_profile re-read fallbacks), confirmed A4/A5's genuine leader-intent precedences survived untouched, re-verified identity/behavior equivalence against the baseline, and approved with one P3 (the fixture's A6 `semantic` annotation text lags the corrected wording; purely descriptive, gate-insensitive -- owed to the next fixture refresh, noted here so it is not lost). Landed-tree maintained smoke recorded below. |
| I55 T11 third slice: staged unit compile behind the current API | `accepted` | same branch; hash recorded in the next register refresh | Unified-architecture program T11 third slice, per the I49 census's later-slice order step 3. The unit-definition load path is reorganized into declared `content_compile` passes -- `parse_pass` (the historical loading walk, relocated verbatim) -> `validate_pass` (new, zero-behavior) -> `resolve_pass` (explicit pass-through declaration) -- with `load_unit_definitions_json` reduced to a thin orchestration layer and `spawn()` materialization untouched. The hand-written mapping is preserved byte-for-byte: `parse_unit_json` and all 16 helpers have zero-diff bodies; `load_file` gains only an optional raw-entry capture sink (two success-path push_backs, error paths untouched); the relocated walk differs only by declared normalizations (local name, sink argument, commit position); the transactional commit semantics (temp vector, no partial commit on any failure class) are unchanged. The new `validate_pass` produces structured unknown-top-level-key diagnostics against a hard-coded whitelist of 109 keys (I52's 106 read keys plus the 3 present-but-unread refs, plus the underscore-annotation convention) and is called-and-discarded on the default path -- no throw, no reject, no mutation, no logging; the I52 draft JSON stays un-wired (zero build/runtime references). `resolve_pass` declares the status quo -- reference resolution stays deferred to the factory (7 consumption sites annotated), the pass only counts deferred refs read-only, and the `ew_suite_ref`/`rcs_profile_ref` dead edges are recorded as-is. New files join `ef_content`/`ef_test` in CMake; a 7-case/49-assertion C++ suite covers whitelist positives/negatives, diagnostic fields, non-object input, resolve invariance, and unknown-key-does-not-change-loading. 5 files, +516/-7. | GPT review: approve with zero findings. The reviewer independently re-proved the pure relocation (16/16 helper bodies and `parse_unit_json` extracted and compared equal; the walk equal after only the three declared normalizations; `load_file` equal after removing exactly the two capture blocks; transactional semantics verified across malformed-JSON/descriptor/filesystem failure classes), recomputed the whitelist as exactly 109/109 against both the I52 survey and the draft (zero missing, zero extra; all 46 real top-level keys across the 27 definition files recognized), and ran an independent dual-build parity harness: baseline (EF-w2) and changed (EF-w4) `ef_py` builds each load all 27 definitions and generate the full database with identical snapshot SHA-256 (`c7720934...`), plus a synthetic fixture re-proving the three-source missile merge, fuze/fuse last-write, sensor preset, and loadout mass across both builds. Gates: ctest 8/8, C++ 120 cases/18,802 assertions, CMake readiness 18 passed, include-direction 7 passed, smoke 459 passed/45 subtests, ruff and diff-check clean; the 4 naval failures are the pre-existing NumPy 1.26 `asarray(copy=)` machine reds, reproduced identically on the baseline. Landed-tree maintained smoke recorded below. |

### I2 Residual Disposition In I3

- The repository-root README pair remains deliberately direct-reviewed. The
  maintained link audit already covers it, so one special pair did not justify
  a second registry path model.
- The standalone realism-authority content now routes to the existing gradient
  realism, source-admission, and lifecycle owners instead of duplicating them.
- The six legacy divergences are closed by four evidence-backed baseline
  refreshes, removal of copied air-combat archive detail, and restoration of
  the missing review route.
- Legacy archive normalization and evidence compaction remain P7 work and were
  not folded into I3.

## Surfaces That Must Not Be Deleted Without Additional Gates

The following are not deletion candidates merely because they are large, old,
or duplicated:

- public or compatibility APIs before internal and plausible external callers
  have a migration path and deprecation boundary;
- tests before their behavioral assertions are replaced by equal or stronger
  coverage and the relevant bug history is preserved;
- frozen configs, canonical scenarios, accepted evidence, source-rights
  records, third-party inputs, and provenance manifests;
- generated artifacts consumed by a maintained release or validation path until
  clean regeneration is proven;
- archive records solely because they are stale or verbose;
- ignored/private/local workspaces not admitted into the tracked scope;
- user-owned unrelated worktree changes;
- behavior that has no characterization test until that behavior is first
  measured and its intended boundary is decided.

Deletion of any evidence or archive package requires a consumer/reference scan,
rights and provenance review, and proof that a smaller retained set preserves
the same bounded claim.

## Commit And Review Discipline

- Keep every iteration on `codex/redundancy-consolidation` unless the user
  explicitly changes the branch strategy.
- One iteration equals one coherent commit.
- The independent reviewer must not edit the implementation under review.
- Review must ask whether functionality was removed and whether the change
  duplicates an existing owner, not only whether tests pass or code is shorter.
- Passing narrow tests does not prove repository-wide compatibility.
- If an iteration discovers a materially different problem, record it as a
  later candidate instead of expanding the current write set.
- Do not commit with unresolved blocking findings, failing required validation,
  or unexplained deletions.

## Stop Conditions

The consolidation program may be declared complete only after a final audit
proves all of the following:

1. maintained entry surfaces have one named owner and no known broken internal
   navigation;
2. no confirmed unreachable production body or unowned compatibility path
   remains in the active tree;
3. remaining duplicate schemas/helpers have either one shared owner or an
   explicit documented reason for separate representations;
4. remaining archive/evidence duplication is required for provenance, rights,
   reproducibility, or bounded acceptance;
5. remaining candidates are classified as intentional, held behind a named
   compatibility decision, or demonstrably higher-risk/cost than their benefit;
6. focused tests, maintained smoke/build gates, document gates, and final
   independent review pass;
7. the final residual report identifies what was deliberately retained and why.

Operationally, run at least two consecutive residual-audit passes after the
last material consolidation. The second pass must find no new high-confidence,
safe consolidation candidate. Absence of a textual duplicate alone is not
proof of completion; caller, behavior, documentation, and evidence ownership
must also be audited.

If progress requires an external compatibility decision, unavailable source
rights, or behavior that cannot be characterized from the repository, mark the
candidate `held` with the exact missing authority. Do not delete it to satisfy a
line-count target.

## Related Authority

- [Document Lifecycle Policy](../../standards/governance/document_lifecycle_policy.md)
- [Agent Document Authority Map](../../agent/rules/document_authority_map.md)
- [Standards Maintenance Policy](../../standards/governance/standards_maintenance_policy.md)
- [Subagent Usage Policy](../../standards/governance/subagent_usage_policy.md)
