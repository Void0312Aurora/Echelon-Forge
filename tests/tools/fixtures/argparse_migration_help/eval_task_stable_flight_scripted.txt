usage: eval_task.py [-h] --scenario SCENARIO [--episodes EPISODES]
                    [--max_steps MAX_STEPS] [--seed SEED]
                    [--action_mode {full,takeoff2,takeoff4,naval_station3,air_combat_hybrid_v1}]
                    [--include_visual] [--include_proprio]
                    [--no_randomization] --task
                    {stable_flight,takeoff_roll,centerline,waypoint_nav}
                    --backend {world_model,scripted}
                    [--warmup_steps WARMUP_STEPS] [--alt_tol_m ALT_TOL_M]
                    [--spd_tol_mps SPD_TOL_MPS] [--hdg_tol_deg HDG_TOL_DEG]

Unified stable-flight task evaluator (task=stable_flight, backend=scripted)

options:
  -h, --help            show this help message and exit
  --scenario SCENARIO
  --episodes EPISODES
  --max_steps MAX_STEPS
  --seed SEED
  --action_mode {full,takeoff2,takeoff4,naval_station3,air_combat_hybrid_v1}
  --include_visual
  --include_proprio
  --no_randomization
  --task {stable_flight,takeoff_roll,centerline,waypoint_nav}
  --backend {world_model,scripted}
  --warmup_steps WARMUP_STEPS
                        Ignore first N steps when computing hold fractions.
  --alt_tol_m ALT_TOL_M
  --spd_tol_mps SPD_TOL_MPS
  --hdg_tol_deg HDG_TOL_DEG
