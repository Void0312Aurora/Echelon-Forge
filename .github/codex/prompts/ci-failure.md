Diagnose the failed GitHub Actions workflow run.

Use the available workflow metadata, repository state, and log files. Identify the most likely root cause and the smallest useful next step.

Avoid speculative rewrites. If the evidence is insufficient, say what additional log or command would be needed.
